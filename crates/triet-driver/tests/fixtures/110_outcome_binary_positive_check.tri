// EXPECT: 0
// OP.3: ~+ constructor for BinaryOutcome T~E — JIT now compiles.
// Parse→typecheck→lower→MIR-verify→borrowck→JIT all pass.
// Verifier checks: ReturnShape::BinaryOutcome, arity=2, disc≠0.
// main() returns 0 (Outcome function is compiled but not called — caller OP.4).

function might_fail() -> Integer~Integer =
    ~+ 42

function main() -> Integer = 0
