// ERROR: heap Outcome deferred
// HP.1: Outcome constructor with heap payload (String).
// Producer stores {ptr@8, len@16, cap@24} in 32-byte slot.
// Passes typecheck → lower → MIR-verify; JIT rejects at HP.2 guard.

function greet() -> String~Integer =
    ~+ "hello"

function main() -> Integer = 0
