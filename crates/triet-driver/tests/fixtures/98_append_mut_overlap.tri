// ERROR: E2440
// ADR-0049 Lát 5: exclusive mutable borrow — two &0 mutable cannot coexist.
function main() -> Integer {
    let m = "hi";
    let a = &0 mutable m;
    let b = &0 mutable m;
    append(a, 33);
    return 0;
}
