// ERROR: E2423
// Phase 2 R-borrowck-still-refuse: a MULTI-LEVEL field extraction
// (`tp.mid.leaf` — two projections) stays REFUSED (E2423). Phase 2 only unlocks
// SINGLE-level struct field move-out (`single_field` → Some); deeper extraction
// is out of scope (defer). The `_ =>` arm of the move-out match catches it.
struct Leaf { s: String }
struct Mid { leaf: Leaf, k: Integer }
struct Top { mid: Mid, t: Integer }
function main() -> Integer = {
    let lf = Leaf { s: "hi" };
    let md = Mid { leaf: lf, k: 1 };
    let tp = Top { mid: md, t: 2 };
    let x = tp.mid.leaf;
    return tp.t;
}
