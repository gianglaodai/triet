// EXPECT: 42
// Minimal ternary with 2-arm match (no ~0).

function ternary_succeed() -> Integer?~Integer =
    ~+ 42

function main() -> Integer =
    match ternary_succeed() {
        ~+ x => x
        ~0 => -1
        ~- e => -999
    }
