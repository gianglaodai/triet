// EXPECT: 1
// ADR-0049 Lát 5: append multiple bytes, trigger realloc (cap=1→doubling),
// verify content with eq. Content teeth: if append writes wrong byte/offset,
// eq returns -1 instead of 1.
function main() -> Integer {
    let m = "";
    append(&0 mutable m, 104); // h
    append(&0 mutable m, 101); // e
    append(&0 mutable m, 108); // l
    append(&0 mutable m, 108); // l
    append(&0 mutable m, 111); // o
    return eq(m, "hello");
}
