// ERROR: E1037
// APP.1: ~-> with tail-expr body (Mode 1 map) is rejected.

function fail() -> Integer~Integer =
    ~- -99

function propagate() -> Integer~Integer =
    fail() ~-> |e| e  // tail-expr, not `return` — Mode 1 rejected

function main() -> Integer = 0
