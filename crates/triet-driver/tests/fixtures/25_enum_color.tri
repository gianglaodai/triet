// EXPECT: 1
enum Color { Red, Green, Blue }

function map_color(c: Color) -> Integer =
    match c {
        Color::Red => 1,
        Color::Green => 2,
        Color::Blue => 3,
    }

function main() -> Integer = {
    let c = Color::Red;
    return map_color(c);
}
