// ERROR: heap-nullable T? not yet lowered
// Nợ #3 (ruling β): a `let s: String? = ~0` typechecks cleanly (the type is
// well-formed; `~0` is the null arm) but the MIR verifier refuses it — the
// Bậc A nullable repr is a single-i64 sentinel that cannot hold a heap
// fat-pointer. This pins the verifier's local-scan arm (the let-bound local
// carries type Nullable(String)). Poison: comment the local_decls scan in
// Body::verify → this flips from clean refuse to JIT miscompile.
function main() -> Integer = {
    let s: String? = ~0;
    return 0;
}
