// EXPECT: -1
// Fixture 41: get out-of-bounds — index too large returns null (Elvis → default).
function main() -> Integer {
    let v = vector_new();
    let v1 = push(v, 10);
    return get(v1, 99) ?: -1;
}
