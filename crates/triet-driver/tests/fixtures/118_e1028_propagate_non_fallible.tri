// ERROR: E1028
// APP.1: ~-> in non-fallible function (returns Integer, not T~E).

function succeed() -> Integer~Integer =
    ~+ 42

// Enclosing fn returns plain Integer — propagate requires Outcome.
function bad_propagate() -> Integer =
    succeed() ~-> |e| return 0

function main() -> Integer = 0
