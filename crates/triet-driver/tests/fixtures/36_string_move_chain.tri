// EXPECT: 1
// Triple move chain with eq observable.
// Phase 4.3c: eq wired through typechecker — no more constant return.
// M2 ensures each let creates a genuine Assign with zeroing.
function main() -> Integer {
    let a = "x";
    let b = a;
    let c = b;
    return eq(c, "x");
}
