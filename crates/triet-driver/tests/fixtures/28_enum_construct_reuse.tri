// EXPECT: 5
// Per SPEC §10.1: Integer is Copy type. Constructing
// SomeInt(x) copies x — x stays valid after construct.
enum OptionInt { SomeInt(Integer), NoneInt }

function main() -> Integer = {
    let x = 5;
    let a = SomeInt(x);
    return x;
}
