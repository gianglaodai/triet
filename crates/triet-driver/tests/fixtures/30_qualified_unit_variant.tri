// EXPECT: 2
// Pin: qualified unit variant with disc≠0 (Status.Red = disc 1).
enum Color { Red, Green, Blue }
enum Status { Active, Red, Inactive }

function main() -> Integer = {
    let c = Status.Red;
    return match c {
        Active => 1,
        Red => 2,
        Inactive => 3,
    };
}
