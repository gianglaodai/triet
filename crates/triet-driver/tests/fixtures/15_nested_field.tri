// EXPECT: 42
// ADR-0060: nested field access p.inner.val — now supported
struct Inner {
    val: Integer,
}
struct Outer {
    inner: Inner,
}

function main() -> Integer {
    let p = Outer { inner: Inner { val: 42 } };
    return p.inner.val;
}
