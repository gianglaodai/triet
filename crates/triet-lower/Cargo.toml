[package]
name = "triet-lower"
version = "0.1.0-dev"
edition = "2024"
description = "Triết AST → MIR lowering — flattens typed AST into flat MIR control-flow graph"

[dependencies]
triet-mir = { path = "../triet-mir" }
triet-syntax = { path = "../triet-syntax" }
thiserror = "2"
miette = { version = "7", features = ["fancy"] }

[dev-dependencies]
triet-parser = { path = "../triet-parser" }
triet-typecheck = { path = "../triet-typecheck" }
