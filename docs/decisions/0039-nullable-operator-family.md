# ADR 0039 — Nullable `?`-family of operators (`?+>`, the fate of `?0>`, forbidden `?->`)

**Status:** **Approved — DESIGN LOCK** (Author, 2026-06-05; Mentor O verified ADR-0020 §3.1 flatten + lexer precedent).
**Implementation:** **DEFERRED** — awaiting nullable/Outcome lowering (Level B/C).
The backend cannot yet lower `?.`.

## Context

Author's original
