---
name: campaign_path_separator_and_partial_move
description: "✅ CLOSED — ADR-0070 Partial-move (ZST/Capability→heap-scalar→heap-STRUCT→enum-field→MULTI-LEVEL projection-path) + ADR-0071 Path `::`/`use`/enum-variant. §AMEND Phase 3 WO-0075 `bd614f3` (2026-06-29): partial_moves→Set<Vec<String>>, fixpoint-hole fixed, E2424 sub-path lock. READ if touching partial_moves / projection_path / prefix_conflict / fixpoint merge / borrowck move-state."
metadata: 
  node_type: memory
  type: project
  originSessionId: b937933d-79d7-4b00-9795-d55815fb3258
---

# Campaign: Partial-move (ADR-0070) + Path-separator/use (ADR-0071)

Session 2026-06-26 (role: Mentor O). Two red ledgers, G supervising, cleared back-to-back. origin/main = `c831274` synced.

## ✅ ADR-0070 Partial-move & Struct-ZST — SEALED, commit `d3aa4ce`
`let v = hw.vga` field-level move-state = the core of the Borrow-Checker. **Decision:** raise move-state from per-Local→**per-Place** (`partial_moves: BTreeMap<Local, BTreeSet<String>>` in BlockState, union-merge monotone → fixpoint converges). Sawtooth scope: ONLY ZST/Capability fields (heap-field-move deferred, No-Box — requires JIT dynamic drop-flag). E2420 reused (no new code minted). **0B true-ZST sizing** (O's checkpoint ruling: 8B would betray the ADR-0069 sealed "0 bytes"). 3 files: checker.rs (Δ3 capability-allow + heap-refuse E2423 kept + 6 use-site invalidations) · lib.rs (gate C allow-list + sizing 0,1) · mir_lower.rs (leaf-less non-copy struct→Drop no-op). 6 fixtures 279-284. **O verified 5 independent red teeth** (P-field-key·P-merge union→intersection·P-Δ3-heap no-panic·P-reread·Step3-JIT) + byte-identical. D's Step-0 probe caught 8B-vs-0B + mixed-struct offset (run→105). Schema §10 HardwareToken destructure-move RUNS FOR REAL. G signed off.

## ✅ ADR-0071 Path `::` + `use` + enum-variant `::` — SEALED, commit `4a7da96` (Slice 1) + `c831274` (Slice 2)
**Supersedes ADR-0005** dot-path+Python-import. **Crystal-clear AST: `::` is STATIC (path/type/enum-variant) · `.` is DYNAMIC (field/method).** Giang settled on PA-B Rust-model + brace-group `use a::{x,y}` + mandatory-qualify. G ruled Reading A "kill it with no mercy."
- **Slice 1** (`4a7da96`): lexer `::`(ColonColon)+`use`, kills `import`/`from`. `Item::Use{path,group}` schema-first (codegen, NOT hand-edited generated code). Resolver routes down the 2-old-paths (empty group→Whole bind-leaf, non-empty→From) — E2100/E2101/visibility/as preserved. 4 teeth (P-colon-token·P-longest-match·P-old-keyword·P-resolver). Driver does NOT run the resolver → resolve-correctness test lives in triet-modules (LAW 5 split, O verified-correct-architecture).
- **Slice 2** (`c831274`): `Color::Red`(+payload)→`Expr::EnumLiteral` (a node that already existed) + `Pattern::EnumVariant{name:Some}`. **Killed 3 covert mechanisms:** ① pattern guess-hack (check.rs:892 scrutinee-scan) ② expr in-scope-enum-scan (resolve_enum_variant + 2 call sites) ③ 3 dot-hacks (MethodCall/FieldAccess/Call-FieldAccess→variant). **E1018 AmbiguousEnumVariant RETIRED** (its sole emitter = the scan). Bare unqualified→E1002 everywhere; import-bound `use X::{V}` is spared (env.lookup happens BEFORE the scan). **§2.A:** enum-match Variable-arm = catch-all binding (symmetric with scalar has_scalar_catch_all ADR-0064 §8) — consequence "bare=binding 100%", refuse-narrow heap-payload catch-all. Cleaned up dead `expr_resolutions` (rule #4: field+4 consumers+threading+type+21 call sites; check() 4-tuple→3). Swept ~30 fixtures+examples+docs.

### ⚔ O's verify-don't-trust lesson (Slice 2 — worth recording)
- **Tooth label lied:** D labeled the tooth "P-pattern-guess-resurrect" (re-add typecheck guess). O dug in: the guess-hack is now **INERT** — lower routes Variable→catch-all based on the AST, WITHOUT consulting the resolution → re-adding the guess = a no-op, the tooth is **VACUOUS**. The REAL load-bearing guard = **§2.A catch-all** (poisoning it→293 E1026). → relabeled P-catch-all + **sharpened 293**: scrutinee `Color::Red`=arm-name → binding/variant both land on 99 (no distinction); changed scrutinee to `Color::Red` with a bare arm `Green` (DIFFERENT name) → binding catches Red=99, sneaky match-on-name→E1026. Don't trust the label, trust the tooth's bite.
- **Crude grep nearly went wrong:** poison P-scan/P-dot → error MOVED typecheck→lower (E1002→"undefined local variable" / E1015 disappears); grep "undefined" matched both → seemed unflipped. The harness `e.contains("undefined name")` was the correct fix (294+29 fail). Verify at the HARNESS level, not crude grep.

## ✅ SEALED — Gate-Discipline Infra `9263501` (O verified in blood 2026-06-26 on a new machine + G signed off)
O verified HEAD=`4004df8` (infra `--stat` empty vs `9263501` → identical). **4 blood teeth, restore cp byte-identical (md5 matched):** A-clean-exit0 (gate exit 0, `0·0·289·0`) · **A-real-red** (inject `let unused=1`→build produces 2 warnings, gate exit 1 — NOT a rubber stamp) · **B-no-flake** (`cargo test --workspace` ×10 = 10/10 clean) · **B-teeth-alive** (neuter `FREE_COUNT.fetch_add`→2 FAILED, assert chain survives under Mutex). Fix A root cause correct (set-e+pipefail was suppressing grep-empty→false exit1; capture `$?` + explicit verdict). Fix B `unwrap_or_else(into_inner)` guards against poison-cascade, reset under-lock. **G SIGNED OFF. Next: STEP 2 read-side heap.**

## 🔧 (history) PENDING — Gate-Discipline Infra (committed `9263501` [PENDING O-VERIFY], machine switch)
G ruled: clean up infra BEFORE opening read-side heap. D submitted + **committed `9263501` to switch machines** (commit message clearly states PENDING O-VERIFY, NOT yet signed off): **Fix A gate.sh** (drop `set -e`, explicit verdict: clippy-clean-grep-no-match no longer produces a false exit-1; respects cargo's rc; exit0⟺clean — the sanity gate ALREADY exits=0) · **Fix B** 6 counting files add `TEST_LOCK: Mutex<()>` + reset-under-lock (vector/hashmap/nullable_map/string_drop/string_match_move/block_tail). D claims 4 teeth (A-clean-exit0·A-real-red·B-no-flake 10×·B-teeth-alive). **⚠️ NOT YET verified in blood by O — next session FIRST TASK: verify the 4 teeth (especially A-real-red = gate is NOT a rubber stamp + B-no-flake cargo test --workspace 10× in a row) on commit `9263501`, restore byte-identical; passes → G signs off; fails → D fixes.** If revert is needed: `git revert 9263501`.

## ✅ STEP 2 SEALED — Read-side heap-SCALAR field move-out, commit `2323e0d` (2026-06-26, O verified + G signed off)
`let s = p.name` runs for heap-scalar (String/Vector/HashMap). **3 deltas:** Δ1 borrowck `checker.rs` guard `Capability(_) || is_any_heap()` writes `partial_moves` (reuse/whole-base→E2420 via existing `partial_move_invalidates`) · Δ2 JIT `mir_lower.rs` Assign heap-field-move: syncs String fat-ptr len@8/cap@16 (free receiving cap → garbage=UB) + tombstones the leaf at the base slot (Drop of base→ptr=0 no-op) · **Δ4 lower `lib.rs`** (OUTSIDE the original WO, O ADMITTED missing the lower layer during recon): field-read temp carries the real type → gets a heap slot + Drop; an Unknown temp = LEAK. **NARROWED (G's ruling):** heap-STRUCT field-move struck from the guard (`Struct(_)` removed) + the JIT Struct-arm deleted — because construction-into-field is a pre-existing DOUBLE-FREE (ADR-0067, verified exit 134) → a silent bomb that can't be tested. **O verified in blood — 6 teeth poison→red→restore:** count→2 (Δ2), count→0 leak (Δ4), 296/297→green (Δ1 rec), 300 coffin-lid re-add Struct→lost E2423, 295/299 sibling-alive, 298 multi-level refused. Gate `0·0·295·0`. **Recon-lesson: O's refutation of "type-prop String→Unknown" is correct-typecheck-wrong-lower; the MIR probe must inspect the temp's type at local_decls, not just see `_3 = move _0.name`.**

## ✅ CLOSED — WO-0075 Multi-level extraction (ADR-0070 §AMEND Phase 3, origin `bd614f3`, G signed off 2026-06-29)
MAJOR SURGERY on borrowck core: `partial_moves: BTreeMap<Local, BTreeSet<String>>` → `BTreeSet<Vec<String>>` (projection-path). `let x = h.inner.x` OPENED. **2 separate commits:** C1 `3826924` fixes an **EXISTING fixpoint-hole** (O found it during recon of the merge; fixpoint check :520/541 only compared var_states+active_loans, NOT partial_moves; a partial-move does NOT set base→Moved → the delta gets dropped → UAM slips through the back-edge in a loop = UNSOUND, latent even at single-level), GREEN INDEPENDENTLY first; C2 `bd614f3` the feature. Cascade: `single_field`→`projection_path()->Option<Vec<String>>`; `prefix_conflict(p,m)=p[..min]==m[..min]` (exact/ancestor/descendant/whole-base DEAD, sibling LIVE); §F sub-path reassignment LOCKED with a **new E2424**. JIT Site-G widened `[Field]`→`all-Field` (`walk_projections` absolute-offset at any depth); Lower Site-H no-op (`place_result_type` already loops multi-level — MIR probe pinned this). **9 teeth O verified in blood, independently (cp-snapshot, control variable):** G🩸 fixpoint-loop (`got:[]`, guards the back-edge); F⚔ merge-union (`got:[]`, guards the diamond with ≥2 preds); B+D prefix exact-only→RED **A/C/E/F GREEN=control is exact enough**; H runtime (`left:2 right:1`); E2424-lock (`got:[]`). IRON LAW #3: `cannot_move_multilevel_field_out` retargeted→non-Field Payload E2423 (negative coverage PRESERVED, the ADR flips the behavior); fixtures 298/302 `*_e2423`→`*_run` real-allocator witness. Backward-compat 100%. **Lesson: recon-open-heart-surgery pulled up a dormant tumor (fixpoint-hole), fixed in the same commit series as the feature.**

## Deferred debt (red ledger — G+Giang settled as open)
- **heap-STRUCT field move-out** (NEW, STEP 2 narrowed): `let m = o.inner` (Inner contains heap) — REFUSED E2423. Blocked by the ADR-0067 construction-into-field double-free. Reopen ONLY when construction is sound + there's a run fixture. Fixture 300 nails the coffin lid shut. **⚠️ STALE: ALREADY OPENED at Phase 2 `5e54233` (2026-06-27) + Phase 2b enum-field `e0b1ed7` + Phase 3 multi-level `bd614f3` — this debt is CLOSED.**
- **match-arm bind heap payload move-out** (STEP 2 carved out separately): `match get(){~+ s => s}` move OUT — blocked by a DIFFERENT Lower blocker: calling a function returning a nullable-aggregate → `lowerer does not support Identifier`. Separate recon.
- **ADR-0068 Box/recursive** (G DEFERS again): allocator + iterative-drop + typecheck self-ref. A monster, no allocator = suicide.
- **No-Box completions:** `enum{Rec(Wrapper)}` payload-struct-containing-heap. (~~heap-field partial-move~~ ✅ Phase 2/2b · ~~multi-level `hw.a.b`~~ ✅ Phase 3 WO-0075.)
- **Phase 3 remaining deferrals:** non-Field projection move-out (Index/Deref/Payload — still E2423) · sub-path reassignment open (`h.inner=fresh` after moving `h.inner.x` — still E2424-locked).
- ADR-0070 cosmetic: legacy name `ImportPath/ImportName` after `import` died (rename to UsePath/UseItem is an optional follow-up).

[[mentor_o_persona]] [[colleague_d_persona]] [[campaign_truc_b_heap_in_aggregate]] [[feedback_teeth_never_git_checkout]] [[feedback_poison_must_be_red]]
