# Memory index

## Project context
- **★★★ 2026-07-27(d) — 🏁 `HashMap.drain()` LANDED qua PA-2 DESTRUCTURING-ONLY DESUGAR (ADR-0089 §AMEND-2).** origin/main `816a729`+docs, gate `0·clean·0·522·0`. Giang chốt "Tuple lowering"; **O recon BÁC CẢ KHUNG của nhãn**: bức tường 2 (enumerate-shim key-less) **KHÔNG PHẢI bức tường** (drop-glue `:1940`/`:2038`+rehash `:6583` ĐÃ walk key-less lọc `state==1` từ lâu — cơ chế CÓ, chỉ chưa expose); bức tường 1 (Tuple) **không nên phá mà nên ĐI VÒNG**. 💀 **PA-1 Tuple-hạng-nhất G ĐẠP CHẾT:** `MirType::` match tại **729 site** ⇒ thêm 1 variant = **tự gieo lại họ bug "match exact QUÊN variant"** vừa quét xong (6 thành viên, 2 trong lưới an toàn) + chạm B-γ/B-β. 🔑 **`for (k,v)` cần HAI BIẾN trong thân vòng, KHÔNG cần GIÁ TRỊ tuple** — PA-1 xây kiểu hạng nhất rồi lập tức phá ra làm hai, trả 729 site cho trung gian không ai giữ. **PA-2 = 0 variant mới, 0-hit.** 🔒 **Bất biến: Tuple SỐNG ở front, CHẾT tại lower** (`MirType::Tuple`=0). ⚠️ **`grep -c Tuple` trần KHÔNG phải tiêu chí** — lowerer BẮT BUỘC match `Pattern::Tuple:2036` = chính thiết kế; **D bác proxy thô của O, D ĐÚNG**. 🔑 **Chuỗi 4 bước move-out** (mirror `remove:6824`): surface K+V → zero key-cell → **`state→2`** → **`len--`**. **MỘT cờ `state` đóng CẢ BA tử huyệt** (drop-glue chỉ walk `state==1`): move-out sound · break-mid · container-survives. Cursor **O(N)** (`cap=1000,len=10`: 1000 vs 10.000). 🔑 **SENTINEL: cursor-stop `-1` ≠ `NULL_SENTINEL` i64::MIN** (miền cursor ≥0) — CẤM trộn. 🩸 **O verify 3 mũi ĐỘC LẬP: P1** `state→2`→`1u8` ⇒ **9v6 / 10v8 con trỏ lặp = double-free THẬT** (**lần đầu lịch sử dự án: move-out ĐỒNG THỜI heap-key × heap-value**) · **P2** bỏ `len--` ⇒ **3v0** · **P3** fail-open ⇒ 527-530 đỏ + **fixture CŨ 510 đỏ lây**, 520-525 xanh. ⚔ **P2 "không đỏ" → D báo TRUNG THỰC + TỰ cắm răng `len==0`, không bịa mũi giả** (drain dừng theo `state` không theo `len` = (b) test yếu). ⚔ **"Tháo guard" = fail-OPEN không phải fail-closed** — D tự phát hiện làm sai hướng, sửa, báo cả 2 lần đo. 🩸 **O SAI 2 TIÊU CHÍ, D bác cả 2 bằng số**: proxy `grep -c Tuple` + "530 fixtures" (nhầm **số hiệu cao nhất** với **TỔNG FILE**=522, trong khi chính O đã đo `ls|wc -l`=511 cùng phiên). Cùng gốc "hành động trước khi đo", nay ở tầng **tiêu chí nghiệm thu**. Fence lát 1: K∈{scalar,String} V∈{scalar,String,Vector,HashMap}; refuse E1054 (**4 nghĩa** — nợ tách sau); ngoài for-guard→E1015. → [[campaign_hashmap_drain_pa2]]
- **★★★ 2026-07-27(c) — 🏁 ADR-0088 LANE A: hàng rào `E1055` nested-nullable `T??` 2 TẦNG / 2 LỚP NGUỒN + 9 RĂNG CƯA.** origin/main `d9b659a`+docs, gate `0·clean·0·511·0`. Giang chốt mở; **O recon 20 probe BÁC khung "double-nullable = vấn đề `get`-family"** — `T??` viết TRỰC TIẾP chưa ai đo: **KHÔNG UB** (mọi đường fail-closed) **nhưng 5 MÃ LỖI cho CÙNG một khái niệm**, gồm 🔴 **E1190 = mã ICE "please report compiler bug"** cho input hợp lệ cú pháp (vi phạm ADR-0086) + message MIR **sai hướng** ("heap-nullable/Struct?/[Fix] Remove the heap field" cho `Integer??` không heap không struct). 🎯 **PHÁT HIỆN LÕI: 0 RĂNG CANH** — refuse cũ ký sinh vào **allow-list** `is_lowerable_nullable_payload` (`triet-mir:1796`; `Nullable` không có tên ⇒ refuse *by default* = **may mắn cấu trúc**), grep `??` trong dòng code corpus = **0 hit** ⇒ ngày Lane B nới allow-list thì 7 đường lọt JIT **câm, gate xanh** (SPOF-một-lớp kiểu WO-SPOF-1). **HAI lớp nguồn:** Lớp A `resolve_type` **2 BẢN SAO** (`check.rs:1365`+`check_resolved.rs:597`) · Lớp B `check_call` sau `substitute` (`let x=pop(v)` KHÔNG annotation, shape chỉ có sau substitution). ⚖ **D bác vị trí WO của O — ĐÚNG** (`env.rs` còn `TypeParameter` trừu tượng → `check_call` là chokepoint duy nhất). ⚖ **O BÁC giao thức verify của G — G RÚT:** G lệnh "poison allow-list → 7 fixture đỏ"; O đo TRƯỚC, chứng minh sẽ KHÔNG đỏ (typecheck bọc kín tầng MIR) ⇒ 1-mũi đẩy người verify vào bẫy rồi buộc **bịa mũi giả cho nổ** → G duyệt **2 mũi độc lập**. 🩸 **O verify 3 mũi + ĐẶC HIỆU HAI CHIỀU: tắt Lớp A→6 đỏ / tắt Lớp B→3 đỏ (6+3=9, hai lớp TÁCH BẠCH) / nới allow-list→chỉ unit test đỏ, 0 fixture.** 517/518 dưới poison **lộ lại đúng ICE cũ** ⇒ guard mới chính là thứ giết E1190/E1141. Răng chứng minh **tầng harness** (`E9999`). ⚔ **BẤT BIẾN MỚI: message runtime CẤM chứa mã lỗi tầng khác** — D tự bắt chuỗi `"(E1055)"` trong msg MIR làm poison **xanh giả** qua `.contains(code)`, tự sửa, 0 vết bịa. 🩸 **Vết O: sót hạng mục ④ Documentation của G khi soạn WO** → O tự làm docs. **⏸️ LANE B HOÃN VÔ THỜI HẠN** (sentinel `i64::MIN` chỉ 1 bit null; chưa use-case = xây cầu chưa có sông). §AMEND-1 đính chính thân bài: `contains` với `V=Integer?` → **E1041**, KHÔNG phải "được cho qua". → [[campaign_adr0088_lane_a_nested_nullable]]
- **★★★ 2026-07-27(b) — 🏁 PHÁO ĐÀI #6: ADR-0089 Slice 2d (`&0 mutable Vector` drain) — borrow-receiver, container-survives.** origin/main `75e5c6a` synced, gate `0·clean·0·501·0`. Code `014442e`+`2dcc9b6`, ADR `75e5c6a`. Drain QUA mượn-độc-quyền-mutable: caller GIỮ buffer rỗng (§2d.2 — Vector handle=buffer-ptr chung, `len--` caller thấy, KHÁC String len-ở-stack) · break-mid caller-drop sound (tombstone len-- để survivors 0..len cho owner drop, §2d.3) · form-fence chỉ `BorrowExclusiveMutable`+T-non-null (khác→E1053/E1051). **3 điểm chạm:** typecheck form-aware `check.rs:759` + lower Reference-unwrap `lib.rs:2373` + **JIT fat-detect Reference-unwrap `mir_lower.rs:3909`** (D tự bắt lỗ ngoài scope phase-1→DỪNG đúng ranh giới→O verify hole THẬT→G duyệt Phương án A→phase-2 mở điểm chạm #3, mirror idiom proven `_get_copy:3967`). **Verify-don't-trust cắt CẢ G:** bắt lệch `MirType::Reference` G viết tuple→thực struct `{form,inner}`. **★ POISON-KHÔNG-ĐỎ→phát hiện no-drop 2-LỚP:** poison-1 push_owned KHÔNG đỏ (sai lớp); chokepoint `is_copy(Reference)` (mir:736, dùng CẢ lowerer + JIT Drop:3397)→`Drop not supported` **fail-closed** (KHÔNG silent double-free). Poison form-guard→492/507 ĐỎ · JIT unwrap→506 ĐỎ (scalar 505 OK=bán kính đúng). D bác O 0, dừng-đúng-scope 1. → [[campaign_iteration_slice2d_borrow_drain]]
- **★★★ 2026-07-27 — 🏁 PHÁO ĐÀI #5: ADR-0089 Slice 2c (`!!` ForceUnwrap) — lower đồng cấu Elvis.** origin/main `c77d674` synced, gate `0·clean·0·496·0`. Code `aa500ab` (D) + ADR-0041 §AMEND-Slice2c `c77d674` (O). `expr!!` (`T?` force-unwrap): **trap-on-null** `Terminator::Trap`/SIGILL (no-merge) · present **PA-3c identity move-out** (source=named-local non-Copy heap-scalar → borrowck Moved `checker.rs:975` → đọc lại **E2420** = chữ ký move, giết alias double-free) · scope scalar+heap-scalar (String?/Vector?/HashMap?) · **fence** `Struct?`/`Enum?` → **E1100** (defer ownership projection). 1 điểm chạm arm `lib.rs:4508`, front-half đã wire (gap kinh điển). **Recon bắt gap + verify-don't-trust cắt CẢ G:** bác E2403→**E2420** thực · bác "String=Struct(\"String\")" (MirType String là variant RIÊNG mir:490) → belt `is_string_repr` thừa, D bỏ đúng. **O poison 2 mũi ĐỎ** (tháo Trap→signal 4→None · tháo fence→corpus 501/502 succeeded 3/7 + double-free 134). 🥅 Bẫy: grep-rồi-`tail -25` cắt cụt corpus FAILED→suýt kết luận vacuous; chạy RIÊNG `--test integration_tests` lộ ngay. D bác O 0 lần, subprocess-isolate 496/498 trap ĐÚNG convention. → [[campaign_iteration_slice2c_force_unwrap]]
- **★★★ 2026-07-26(b) — 🏁 PHÁO ĐÀI #4: ADR-0089 Slice 2b (Drain) — `for item in v.drain()` consuming move-out.** origin/main `780e081` synced, gate `0·clean·0·488·0`. Tiêu thụ `Vector<T>` move-out MỌI T (heap MỞ: `Vector<String>`/`Vector<User{String}>` — khác Slice 2a refuse E1053 copy=alias). **Zero-shim:** desugar vòng `pop_front` proven (347/351/338), O(N²) correctness-first. 2 điểm chạm: typecheck `check_for_stmt` (.drain()=for-guard-ONLY, infer chỉ receiver→standalone E1041; ref→E1053; `Vector<T?>`→E1053; else→E1052) + lower drain-arm (pop_front+present-test+PA-3c unwrap, continue→hdr no-step). **🥇 GOLD: Tombstone `len--` DOUBLY LOAD-BEARING** (O poison: tháo→full-drain HANG VÔ HẠN [điều kiện DỪNG] + break-mid double-free [survivor]). 6 điều kiện thép ✅ (491-494 + 487/488 allocator thật + counting). **D cắt-ngang mid-verify** (không malice)→O verify độc lập + dọn (restore poison `da3a0d80` KHÔNG-vào-commit + sửa docstring giả-thuyết-sai `STR_FREES==6`→thực-tế-hang), code D nguyên vẹn. Bài học: infinite-loop=tín-hiệu-poison drain→timeout-bound mọi lệnh có-thể-hang (Giang bắt "chạy lâu"). → [[campaign_iteration_slice2b_drain]]
- **★★★ 2026-07-26 — 🏁🏁🏁 3 PHÁO ĐÀI: ADR-0084 verify (`9ff47c1`) + ADR-0089 Iteration Slice 1 (`85371a6`) + Slice 2a (`adfe8f9`).** origin/main `adfe8f9` synced, gate `0·clean·0·479·0`. **0084:** O bóc mẽ harness gộp-đa-pha — 386 E2450 VACUOUS (CLI thật→E2400), G "đâm nách typecheck" dummy-param→E2450 sạch, poison chase→would-dangle=chase SOLE-GUARD. **Slice1:** loop/break/continue+for-Range, loop-context+step-block, 3 guard E1052/E0009/E1143 (D bác giả định ADR bằng data), răng break_drop_counting FREE 3→2. **Slice2a:** for-item Vector copy-sugar scalar+bare-copy-Struct, infallible-get raw-shim, heap→E1053, handle-alias→tái-dùng-local, răng container-FREE=1. **O đào asymmetry bẫy-câm-MỚI** (is_copy_aggregate broad→Vector<CopyEnum> lower E1100) + dead-code push_owned redundant. Bài học: harness≠thực · luật#12 stale-binary cứu 2× · push_owned idempotent · emit_shim_call:1783 gánh ownership. → [[campaign_iteration_slice1_2a]]
- **★★★ 2026-07-25(h) — 🏁 WO-MethodCallFatReturn (ADR-0065 §14.7 đóng copy #3 is_fat_ret) + TRIAGE sổ nợ.** `45919cd`, gate `0·clean·0·463·0`. Copy #3 `MethodCall:5501` KHÔNG unwrap Nullable→Enum/Struct?/Enum? method-return E1100 over-refuse (fail-closed). Mirror copy #2. O poison granular orthogonal (enum-path vs struct-path độc lập). **TRIAGE 5 mũi:** P1 Vector-return=FALSE-ALARM của O (rút) · KM-P1b=ZOMBIE · N1 widening=nhãn-lệch policy-hole · Panic-C=bất-khả-từ-source · ADR-0084 nợ-verify+tooth-386 VACUOUS. Bài học: refuse-over-guess áp CẢ claim O (lần 12 "hành động trước khi đo"). → [[campaign_methodcall_fat_return_and_triage]]
- **★★★ 2026-07-25(f) — 🏁 WO-GetRefNullableRefuse: REFUSE get-family V=Nullable (E1051) + defer ADR-0088.** `36dad8e`, gate `0·clean·0·462·0`. Nhãn "get_ref V=Nullable" BỚI RA design cliff double-nullable: `get`→`V?`; V=`T?`⇒`T??` outer(key-found) vs inner(stored-null) xung đột→silent-flatten=wrong-result câm. G chọn (B) refuse sạch+defer: guard `exprs.rs:1189`→**E1051**. O poison→3 fixture FAIL đặc hiệu + BONUS log lộ MIR cũng vỡ. → [[campaign_typed_collections]] [[feedback_cham_ma_chac_pattern]]
- **⚰️ 2026-07-25(g) — TRIAGE: `&0 Enum tiêu thụ` = ZOMBIE (chôn, KHÔNG WO).** Giang mở lane này; recon-trước-WO (cứu LẦN 4 phiên) → đã đóng toàn diện bởi **Lát A `d4486ad`** (match qua `&0`/`&0 mutable` Enum: disc + scalar/heap/aggregate payload bind + E1026 exhaustiveness survive reference wrapper) + móng borrowck NLL `7cd387d`. **6 probe THẬT** (unit→2·scalar→42·heap-String→5·aggregate-Struct→7·`&0 mutable`→3·`&0` local→2) + **6 fixture** (394/397/398/399/408/409). `match` là đường tiêu thụ DUY NHẤT của enum (không builtin đọc khác như String len/eq/concat). Nhãn sổ tự thú "'tiêu thụ' concern chưa rõ" = concern mơ hồ, KHÔNG hố. **Quy luật lặp: nhãn backlog KHÔNG đáng tin** (giống zombie "Enum? param SIGILL 132" phiên 07-25c). Chốt sổ ca sau 3 WO thật + 1 zombie. Sổ nợ còn LẠI toàn campaign nặng cần ADR+recon-đầu-ca-tươi: method-call Struct?/Enum? return E1100 (fail-closed KHÔNG UB) · ADR-0088 double-nullable · Deep-Clone · drain · N1 widening.
- **★★ 2026-07-25(e) — 🏁 WO-Robust-InstResults: khai tử class ICE `inst_results[i]` panic-index (Track B rule #1).** origin/main **`3fc170f`**, gate `0·clean·0·458·0`, 3 commit. 10 site `inst_results(call)[i]` trần trong `mir_lower.rs` → helper checked `nth_call_result(...)?` → **`JitError::CallResultArityMismatch{callee,expected,got}`** (struct variant G chốt, Display "please report" mirror `Internal`; ICE KHÔNG đổ lỗi user). 🦷 Teeth hand-built MIR: shim `has_return:false` + CallDispatch `dest` non-empty+`ReturnShape::Scalar` (ép đọc `[0]`) → `Err` **tiến trình sống**; **🩸 O TỰ re-poison site 4764** (`nth_call_result?`→`[0]` trần) → PANIC `index out of bounds len 0 index 0`, restore md5 `da3a0d80` khớp. **⚖ G SỬA LƯNG O:** O đánh giá bom-chờ "live-risk thấp (poison-only)" → G bác "poison-only hôm nay = ICE ngày mai khi lỡ đục sai Lowerer; **Tôn chỉ 3 SOUNDNESS TRƯỚC SYNTAX — vá crash TRƯỚC mở feature**". O nhận sai lane. Nhóm B (host-ISA expect)/Nhóm C (D-JIT-OOM layout unwrap) = PARK (abort môi trường/OOM chính đáng, G đồng ý). D=Sonnet 5 agent MỚI (bản cũ chết spend-limit giữa WO, tree sạch chưa chạm code), 0 bế tắc. Lane kế G ký mở: **get_ref V=Nullable**. → [[campaign_typed_collections]]
- **★★★ 2026-07-25(d) — 🏁 WO-JIT-PRINT / ADR-0087: stdout HOẠT ĐỘNG LẦN ĐẦU sau rewrite.** `25cb2cc`, gate `0·clean·0·458·0`. 4 overload (owned move+free / `&0` borrow no-free) × 4 extern-C shim (memory-responsibility hardcode vào symbol name) + `emit_shim_call` nhánh `ReturnShape::Unit` (G bác trick throwaway-i64 O) + typecheck `declare_overload`. Bài học: recon-O sót typecheck env → D cứu 2× qua LUẬT-4. O re-poison T1→child double-free glibc. Nợ: read_line · f-string · buffering. → [[campaign_jit_print_adr0087]]
- **★★★ 2026-07-25(c) — 🏁 &0 String READ-BORROW COVERAGE (len/eq/concat) + TRIAGE chôn 2 zombie + 1 phantom.** `5dd2aeb`, gate `0·clean·0·458·0`. Nhãn backlog KHÔNG đáng tin: "Enum? param SIGILL 132"=ZOMBIE (đóng `ccb8db3`); borrow-params `&+ T`=PHANTOM (ADR-0022 §4.1 `&+`=unique-OWNER move, share=BYOS FROZEN). Front: `&0 String` coverage — concat MIRROR nhánh `is_reference`→concat_sret (`mir_lower.rs:3968`). Teeth FREE-count dedup len=1/eq=2/concat=3. Vết O: nói quá "G-state đầy zombie". → [[campaign_str0_coverage_and_triage]]
- **★★ 2026-07-25(b) — 🏁 WO-SPOF-3: CANARY BỊT SPOF `builtin_shim_meta.arg_consumes` (Threat-1) + CHÔN Threat-2 bất-khả.** `c22a751`. G tách 2 threat O gộp nhầm: T1 canary `shim_arg_consumes_spof_canary.rs` pin bảng-vs-free-thật; T2 (bảng vs Lowerer `!consumed`) CHÔN bất-khả (M3 load-bearing, tắt→baseline tự double-free). Teeth 2 chiều FREE 1→2/2→1. 🩸 Vết O: trượt shape ×3 "đừng thiết kế test bằng trí tưởng tượng". D bác/DỪNG LUẬT-4 ×2. → [[campaign_shim_meta_spof_adr0085]]
- **★★ 2026-07-25 — 🏁 WO-SPOF-1: BỊT SPOF REFUSE container-`Nullable(Struct-heap)` (Phương án B, ADR-0065 §15).** `0285bf2`. 🔑 O BẬT G bằng số đo: không live-UB reachable (WO-5 đã bịt); hố thật = SPOF một-lớp (struct-field/enum-payload verify sót nhánh Vector/HashMap). Fix container-scan 2 loop. Poison `FAIL 459/460`. Vết D: gate tóm tắt→O REJECT "Dán Raw hoặc cút". → [[campaign_spof_container_nullable]]
- **★★ 2026-07-24(c) — 🏁 FRONT-B WO-1: PANIC-AUDIT `mir_lower.rs` NHÓM A + `JitError::Internal` + `cargo fmt --check` vào gate.** `e2db04d`. 9 production site → Internal(4)/env-keep(2)/OOM-defer(3); tooth 3c `unreachable pattern` thật. Vết O: thứ-tự-trình-bày (gate chìm) → luật báo-gate-lên-đầu. → [[campaign_front_b_panic_audit_group_a]]
- **★★ 2026-07-24(b) — 🏁 FRONT-A: `LowerError` HỆ MÃ LỖI (ADR-0086, enum 8-mã `triet::lower::E11XX` + miette; E1190 ICE).** `dbde2d5`. 🩸 O tự bắt recon-nửa-vời (8 ctor→~47 construction) TRƯỚC khi ra WO. Nhịp 2b KHAI TỬ (canary stale). → [[campaign_front_a_lower_error_codes]]
- **★★★ 2026-07-24 — 🏁🏁 ADR-0085 BỊT SPOF `builtin_shim_meta` (Nhịp 1 P-exist + 2a self-loan-exclusion).** `f6b569f`. Bảng 8-entry + cổng `Body::verify()` `__triet_`→`UnknownShim` P3.5. LUẬT 18 dính O ×3 (poison cứu cả ba). D bác O 2/2. Nợ Nhịp 2b treo. → [[campaign_shim_meta_spof_adr0085]]
- **★★★ 2026-07-20 — 🏁🏁🏁 "QUÊN NULLABLE" KHÉP + GIẾT UB `free(1)` + FULL-SRET NULLABLE AGGREGATE (Lát A+B).** `51edd0e`. Họ "match exact QUÊN Nullable" = 6 thành viên (2 trong lưới an toàn). UB sống: `Vector<Leaf?>`→SIGABRT 134. §15 tu chính (local/pop `Nullable(Struct-heap)` SOUND). D bác O 12/12. → [[campaign_forgot_nullable_sweep]]
- **★★★ 2026-07-19 — 🏁🏁 5 WO: họ "QUÊN Nullable" × 3 vị trí + rỉ câm shim-mượn. 🔴 KHUI SPOF `arg_consumes`.** `aa9e584`. Lowerer push_owned + JIT M3 đọc CÙNG bảng ⇒ không defense-in-depth. Luật: oracle cũng là giả định (dedup con trỏ), răng ở tầng harness. D bác O 8/8. → [[campaign_nullable_position_and_temp_ownership]]
- **★★ 2026-07-18(b) — 🏁 PA-A NULLABLE-ENUM-IN-AGGREGATE.** `5c713c4`. Silent-miss (`Nullable(Enum)` tra `struct_map`→`mid.m`=42). G đảo vai N3=soundness/N1=policy-gate. D bác WO O 5/5. → [[campaign_nullable_enum_aggregate_pa_a]]
- **★★★ 2026-07-18 — 🏁🏁 ĐẠI PHẪU MÓNG BORROWCK + `&0 Enum` (4 mặt trận).** `7cd387d`. Hai bệnh ngược chiều che nhau (under-refuse Assign / over-refuse Drop-reference); engine đúng, INPUT rác. Fixture 102 UAF thật. D bác O 4/4. Móng-không-mượn-gạch-tính-năng. → [[campaign_borrowck_nll_foundation]]
- **★★★ 2026-07-15→17 — CHUỖI GET_REF/GET-BY-VALUE ĐÓNG (ADR-0079 §AMEND + ADR-0082 §AMEND-3).** `8981135` gate 385 · `006b6c7` 381 · `12adc0b` 361. get_ref aggregate zero-copy quét E1041/E1049; get-by-value Copy-aggregate non-destructive, heap-bearing→E1049. 🩸 **8B-aggregate deref-thin dính LẦN THỨ BA** → SIGSEGV. **O tự đính chính WO 2 lần** (failure-mode `&0` dangling = lọt câm, KHÔNG SIGSEGV). **D tự hạ claim** double-free→leak (ngược vết bịa). → [[campaign_typed_collections]]
- **★★ 2026-07-03→13 — CHUỖI TYPED-COLLECTIONS (ADR-0080/0082/0083).** key-typed `HashMap<String,V>` `381979e` · read-side get-borrow `96f4241` · Slice A/B/C element+value aggregate `1e49058`/`c22da0a`/`6d9e144` · value move-out + `pop_front` `3e0975d`/`5462c5b` · key-aggregate struct+enum `1c08a67`/`91c273a` (gate 331→354). 🩸 Bài học: **hash-content vô phương tooth bằng roundtrip cap-nhỏ** (16-align mask) · **hai bug che nhau** cho "2 giả sound" · **stale binary → LUẬT rebuild trước khi chạy binary** · §6 fnptr-trước-stride = lá chắn size-collision. → [[campaign_typed_collections]]
- **★ 2026-06-29→30 — OWNERSHIP FIELD-MOVE-OUT KHÉP + cụm heap-aggregate.** `4165c18` gate 308 · WO-0073/74/75/76 `3738eb5`/`e0b1ed7`/`bd614f3`/`994afc8` gate 303-306. **💀 "dynamic drop-flag" SỤP — O bác G: SLOT TỰ LÀ CỜ** (static tombstone, 0 `brif`). Recon mổ tim lôi **fixpoint-hole ngủ đông** (UAM lọt back-edge). **O bắt D bốc phét "SIGSEGV" thực=LEAK câm.** Module-system Facade: **O bác CẢ Giang LẪN G, G nhận sai #2** → verify-don't-trust áp cả Mentor G. → [[campaign_truc_b_heap_in_aggregate]] [[campaign_path_separator_and_partial_move]]
- **★ 2026-06-19→27 — ADR-0062→0072 (heap-nullable · CFG-tail · match-literal · nullable aggregate · trait Tier1 · partial-move · path `::` · expected-type propagation).** gate 211→303. ADR-0072 giết `c.sig.return_type` proxy toàn cục. ⛔ **ADR-0068 Box/recursive CẤM CỬA.** → [[campaign_aggregate_nullable]] [[campaign_expected_type_propagation]] [[campaign_heap_nullable]] [[campaign_typecheck_exhaustiveness]]
- ★ 2026-06-22(b) — ĐỊNH VỊ: TERNARY-FIRST, gỡ "AI-first". `8ab55b8`. Coherence VISION §8 = đại số Ł3 xuyên null/logic/capability. [[doc_highlights_and_ternary_seeds]] [[project_vision_os_capable]]
- ★ 2026-06-17(c) — QUYẾT ĐỊNH: GIỮ keyword `return` (G định trảm, O phản biện `~->`/ADR-0020). [[lang_return_keyword_survives]]
- ★ 2026-06-09→12 — Bậc D fat-pointer ABI · MirType/borrowck-MIR-NLL · Outcome `T~E` 2-reg + sret · ADR-0059 `&0` heap · ADR-0060 nested `a.b.c`. Chi tiết: [handoff 06-12](handoff_2026_06_12_adr0060_nested_aggregate.md) · [06-11 `&0`](handoff_2026_06_11_muiC_adr0059.md) · [06-11 CFG/Outcome](handoff_2026_06_11_adr0055_tail_expr.md) · [06-10 heap-Outcome](handoff_2026_06_10_op2_dong.md) · [06-10 trả nợ](handoff_2026_06_10_op1_dong.md) · [06-09 Bậc D](handoff_2026_06_09_bac_d_closed.md)
- [★ THỰC TẠI REWRITE — ĐỌC THỨ HAI](project_rewrite_reality_2026_06_04.md) — backend v0.2-v0.10 đã XÓA, 13 crate, bắt đầu lại.
- [docs/HIGHLIGHTS.md + backlog tam phân](doc_highlights_and_ternary_seeds.md) — I✅/II🎯/III🌱. #2 rounding + Outcome-disc-là-Trit đáng ADR.
- [Future — Comparable trait + ?-family](future_comparable_trait_and_monad_gap.md) · [sized ternary ints](future_sized_ternary_ints.md) — defer.
- [Future — ternary placement syntax +T/T/-T](idea_ternary_placement_syntax.md) — O bác 4 lỗ (placement không-cực → loãng coherence §8). PARKED tới khi mở lại ADR-0068.
- [Triết — tầm nhìn OS-capable](project_vision_os_capable.md) · [tổng quan dự án](project_triet_overview.md) (crate list một phần stale) · [parser→schema-AST](project_parser_schema_migration.md)

## Personas (HAI persona, vai khác nhau — đừng lẫn)
- **[★ Mentor O — ACTIVE khi gọi "Mentor O"/"Mentor 0"](mentor_o_persona.md)** — Opus, **gác cổng / review owner**. Verify-don't-trust (tự chạy gate), teeth-phải-đỏ-khi-gỡ-guard, refuse-over-guess, không sửa hộ, admit khi báo động sai, tree đóng băng khi chấm.
- **[★ Đồng nghiệp D — Strict Colleague](colleague_d_persona.md)** — **implement-side, FILE GỐC DUY NHẤT**. 6 rule + Rule #7 refuse-over-guess + **4 LUẬT THÉP G** (① gate dòng đầu + khớp cây nộp · ①b pre-existing kèm stash-diff · ② fmt+clippy+test trước báo · ③ không xóa negative test · ④ bế tắc→hỏi O). Mẫu: báo đẹp hơn thực + đổ lỗi hạ tầng.

## User
- [Webapp dev, vision-driven, defers technical decisions](user_role_webapp_dev_visionary.md)
- [Tiếng Việt + tone non-engineer](user_communication_vietnamese.md) — reply tiếng Việt, analogy webapp/Java.

## Feedback
- **[⚔ Verify PRODUCER trước CONSUMER](feedback_verify_producer_before_consumer.md)** — flip field type mà producer còn đẻ repr cũ = CHƯA migrate. REJECT.
- **[★ CHU TRÌNH 7 bước + 4 vai](feedback_collaboration_loop.md)** — O=chốt verify KHÔNG code; nhận "tiến hành"→định teeth, chờ D nộp.
- **[⚔ poison-phải-đỏ](feedback_poison_must_be_red.md)** — fix cấu trúc: poison logic → test không đỏ → REJECT. Đừng tin tên test.
- **[★ Giao thức báo cáo G (5 mục)](feedback_g_report_protocol.md)** — O soạn gói 5 mục; đối chiếu thư-G-về trước khi ghi sổ.
- **[⚠ TEETH không bao giờ git checkout](feedback_teeth_never_git_checkout.md)** — `cp` snapshot /tmp TRƯỚC, khôi phục cp/Edit, KHÔNG git checkout/restore/stash.
- **[⚔ Failure-mode phải CHÍNH XÁC](feedback_failure_mode_precision.md)** — D bốc phét SIGSEGV khi thực = LEAK. 4 signal khác nhau (134 double-free / 139 bad-deref / FREE==0 leak / FREE==2). G ĐẠI KỴ Hollywood. aggregate field no-slot→SIGSEGV; heap-scalar/`T?` no-slot→leak câm.
- [Verify semantics before asserting](feedback_verify_semantics_before_asserting.md) — probe trước khi mã hóa ngữ nghĩa vào test.
- [Stability over speed](feedback_stability_over_speed.md) — quyết định kiến trúc có ADR.
- [Syntax — verbose + ~~dot paths~~](feedback_syntax_verbose_dot_paths.md) — `module` not `mod`. ⚠️ dot-path SUPERSEDED bởi ADR-0071 (`::` cho path).
- [Proactive tech-debt audit](feedback_proactive_audit.md) — suggest audit window gần version freeze.
- [No abbreviations — Java naming](feedback_no_abbreviations.md) — Vector not Vec, length not len, function not fn.
- [Explicit strictness](feedback_explicit_strictness.md) — panic-possible ops MUST be verbose methods with message.
- [Phương án A — defer cleanly](feedback_cham_ma_chac_pattern.md) — design cliff: ship diagnostic + ADR backlog, NOT skeleton.
- [Implementer's choice](feedback_implementer_choice.md) — author delegate implementation-internal; AI picks.
- [Quality over speed](feedback_quality_over_speed_v0_10.md) — AI=technical-quality owner, pre-commit self-audit.
- [Tiered Opus/DeepSeek workflow](feedback_tiered_opus_deepseek_workflow.md) — unsafe/ABI/IR = Opus-only.

## Reference
- [Source-of-truth docs](reference_spec.md) — SPEC/VISION/ROADMAP/TODO/ADR/CLAUDE.md đường dẫn.

## Triết language gotcha
- [Trit literals — suffix form](triet_trit_literals.md) — `1_trit/0_trit/-1_trit`, không `0t+` (cái đó là balanced-ternary Integer). SPEC §1.5.1.
