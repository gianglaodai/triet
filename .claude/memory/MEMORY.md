# Memory index

## Project context
- **★★ 2026-07-30(b) — 🏁 WO-Reference-Operand-Eq-Refuse: E1058 now fences EVERY operand passed through a reference.** `fdbd66d`, gate `0·clean·0·581·0`, 1 commit, +6 fixtures, **0 red rounds**. `Statement::Borrow` at `:3494-3501` returns an **ADDRESS** for slot-backed locals ⇒ `icmp` compares addresses ⇒ `&0 Struct` (all 5 forms) / `&0 Vector` / `&0 HashMap` / `&0 String?` / **`&0 payload-free Enum`** / `&0 Color?` → silently wrong `0` with exit 0. 🔑 **Twin siblings:** Task A missed `Reference` in the JIT, Task B missed it in typecheck — O's matrix was blind on **both sides of the same axis**. A scalar reference is correct **because of its representation** (`use_var` copies the value), not because of dispatch — MIR for `&0 Integer` vs `&0 Color` is **identical**, they diverge in the JIT. Owned `Color?`/`Integer?` through `match` are correct ⇒ the refusal belongs **only inside the `Reference` branch**; `Nullable(UserEnum)` needs its **OWN arm** (falling into the recursion makes payload-free enums return `false` = the hole reopens one layer down — G spotted it, O measured it). ⚔ **G-Law 19 caught G:** G disputed O's fixture numbering (demanding `584`, a gate of `589`); O refuted with `grep` — **575 files, highest number 584 ALREADY USED, 9 numbering gaps** ⇒ **the highest number is not the count**. 🔑 **G-Law 29 was born:** patching a `match` with N arms ⇒ **N orthogonal poison spears**; O's WO had only 3, and **P4 (added by O) was what proved 585-588 have teeth**. → [[campaign_reference_operand_eq_refuse]]
- **★★★ 2026-07-30 — 🏁🏁 TWO SILENT-WRONG-`exit 0` CAMPAIGNS: sret fat-String + `String ==` comparing pointers.** `d8fa041`, gate `0·clean·0·575·0`, 6 commits, +29 fixtures. ① **TWO ORTHOGONAL HOLES**: Hole A, a `projected→projected` move (`_0.s = move _1.s`) slipped past all three gates (`ty_total_size(String)==8` · `source.projection.is_empty()` · `dest.projection.is_empty()`) ⇒ a `dealloc` layout of **~94 TB** on every run; Hole B, `:3210` `matches!` dropped `Nullable(String)` ⇒ a `String?` field broke **without needing sret at all**. 🔑 **The 2×2 table `dest.projection × source.projection` dragged Hole B into the light BEFORE D typed anything** — patching only A would have kept G's fixture red forever. ② `Statement::BinaryOp:3462` **does not dispatch on type** ⇒ `"hi"=="hi"`→`0`; the blast radius was wider than the brief: `P{1,2}==P{1,99}`→`1`, `E::A(1)==E::A(2)`→`1`. The fix = content comparison (**ADR-0038 §4 locked: `==` stays, do NOT refuse String**) + the **E1058** fence + Task C `Reference{inner:String}` (`&0 String` carries a **StackSlot address**, not a heap `ptr`). ⚠ Refusing payload-free enums is FORBIDDEN (they are currently correct). ⚔ **4 CORRECTIONS from the same root — inferring shape instead of dumping: D 1 · G 2 · O 1** — G submitted to the measurements both times. 🦷 Blind spot 5 times over: ZERO/546 for sret-String · 10 `String?`-field fixtures **all `return 0`** · 0/555 for `String ==` · 0/569 for `&0 String` · 0/575 for `&0 Struct`. 🔑 The **paired `(ptr,cap)` oracle** (a bare pointer counter is blind to a garbage cap) and wrapping **both** `alloc` **and** `from_bytes` (an internal Rust call). 🩸 G-Law 19 caught O for the **3rd** time (grepping a short string that occurs inside a longer one); the hook printed "Push proceeding" while `ls-remote` had not moved — **only `ls-remote` is proof of a push**. ✅ D was honest **3 times in a row**. 🔴 New P1: the E1058 fence **does not cover operands passed through a reference** (`f(&0 P,&0 P)` with equal contents → `0`, verified pre-existing with a worktree at `e5db13c`) — twin siblings, O's matrix blind on **both sides of the same axis**. → [[campaign_sret_stringfield_and_string_eq]]
- **★★★ 2026-07-28 — 🏁 WO-Param-Aggregate-CopyIn: 3 UB FAMILIES (134/139/132) killed by ONE root fix, exposing a P0 "silent wrong" debt.** `2c75b60`, gate `0·clean·0·546·0`. O **refuted the campaign's own label** ("param aliasing is a bug" is WRONG — by-pointer is the deliberate KCN-1b ABI); the real disease = struct params excluded from `struct_slots` at `:2597` ⇒ **49 sites** blind. EVERY use of a heap-bearing param was UB (read a field only → 134 · move/forwarding → 139 · involving **an addition** → 132). Two steel probes: `:2944` → **139→134**; the marker `777` into `len@8` → **132→134**. Blind spot: the 533-fixture corpus had **ZERO** real uses (all 3 fixtures had `return 0` bodies) — a repeat of HP.3. The root fix is 53 lines of copy-in prologue; **Struct was the FOURTH missing aggregate ABI**, and patching case by case had failed **3 times**. 🔴 **New P0: sret returns `94060113734544` with exit 0, no parameters, SILENTLY WRONG through every CI gate** → `WO-SRet-Aggregate-StringField-Corruption`, a mechanism **O has not measured independently**. ⚖ D correctly refuted O (TWO `Deinit(arg)` sites, `:4462` vs `:4544`). ⚔ O's blemishes: undercounted "10" vs 49 · a `^FAIL` grep missed because of indentation. ⚔ O **enforced the raw-gate decree for the first time** (REJECTed D) and **refused G's order that "O add a fixture"** (D holds the pen exclusively). → [[campaign_param_aggregate_copyin]]
- **★★★ 2026-07-27(f) — 🏁🏁 TWO CAMPAIGNS: FIFO teeth for `.drain()` + killing the `aggregate move` double-free UB.** `bd0f4c7`, gate `0·clean·0·533·0`. ① **G REJECTED O's O(N) cursor drain INDEFINITELY**: a mid-loop `return` is its own exit edge (a different Drop set) ⇒ an epilogue-at-`ext` gets skipped = double free; Vector has no per-slot state byte like HashMap. O(N²) is a **performance debt, NOT a soundness hole**. The corpus had been **BLIND TO ORDER**: flipping to LIFO turned only 1 of 522 red, and that one only **by accident of a constant**. ② **UB ALIVE FOR 8 DAYS**: `let a: Leaf? = p` **AND** `a = p` (no `?` at all) → double free 134, because moving a heap-bearing aggregate lacked `Deinit(src)`; the label "policy hole, NOT UB" was applied twice and both times **INFERRED from the N1-enum case** rather than measured on structs; it survived because fixtures only used the Copy type `Pt{x,y}` (the HP.3 blind spot). Teeth must **dedup pointers** — a bare counter is blind to a double free. ⚔ O was wrong 4 times in the session and measurement caught all 4; ⚖ D correctly refuted O twice. → [[campaign_drain_fifo_teeth]] [[campaign_aggregate_move_tombstone]]
- **★★ 2026-07-27(e) — SPLIT the 4-meaning E1054 into E1056 (pattern) / E1054 (key) / E1057 (value)**, cascading pattern→key→value. G refuted O's "double-free UB": **134 = the D2 ADR-0044 Q4 canary** at `__triet_hashmap_insert:6620`, not drop glue. `d4baf60`, gate 522. → [[campaign_hashmap_drain_pa2]]
- **★★★ 2026-07-27(d) — `HashMap.drain()` LANDED via the PA-2 destructuring-only desugar.** PA-1 (first-class Tuple) was STAMPED OUT (**729 `MirType::` sites**). 🔒 Invariant: Tuple lives in the front end and **DIES at lowering**. First simultaneous move-out of a heap key AND a heap value. An O(N) cursor and one `state` flag closed all three death traps. `816a729`, gate 522. → [[campaign_hashmap_drain_pa2]]
- **★★★ 2026-07-27(c) — ADR-0088 Lane A: the `E1055` fence for nested nullables `T??`**, 2 source layers + 9 teeth. 🎯 Core finding: **0 teeth were guarding it** — the old refusal was a parasite on an allow list, i.e. structural luck. Lane B DEFERRED indefinitely. `d9b659a`, gate 511. → [[campaign_adr0088_lane_a_nested_nullable]]
- **★★★ 2026-07-27(b) — Slice 2d `&0 mutable Vector` drain** (borrow receiver, container survives), 3 touch points including JIT fat detection. ★ A poison that did NOT go red exposed a **two-layer** fail-closed no-drop. `75e5c6a`, gate 501. → [[campaign_iteration_slice2d_borrow_drain]]
- **★★★ 2026-07-27 — Slice 2c `!!` ForceUnwrap**: trap-on-null + PA-3c identity move-out (**E2420 = the move signature**), with a `Struct?`/`Enum?`→E1100 fence. `c77d674`, gate 496. → [[campaign_iteration_slice2c_force_unwrap]]
- **★★★ 2026-07-26(b) — Slice 2b Drain `for item in v.drain()`** moves out EVERY T, a zero-shim desugar to `pop_front`, O(N²) correctness first. 🥇 The `len--` tombstone is **DOUBLY load-bearing** (remove it → infinite hang + a mid-break double free). `780e081`, gate 488. → [[campaign_iteration_slice2b_drain]]
- **★★★ 2026-07-26 — 3 fortresses:** the ADR-0084 verification (386 E2450 **VACUOUS**) + Slice 1 (loop/break/continue + for-Range) + Slice 2a (for-item copy sugar). Lesson: the harness is not the reality; a stale binary saved us twice. `adfe8f9`, gate 479. → [[campaign_iteration_slice1_2a]]
- **★★★ 2026-07-25(h) — 🏁 WO-MethodCallFatReturn (ADR-0065 §14.7, the 3rd copy of `is_fat_ret`) + a debt TRIAGE.** `45919cd`, gate 463. Copy #3 at `MethodCall:5501` did not unwrap Nullable → method returns over-refused with E1100 (fail closed). **5-spear TRIAGE:** the P1 Vector-return was O's FALSE ALARM (withdrawn) · KM-P1b is a ZOMBIE · Panic-C is impossible · the ADR-0084 tooth-386 is VACUOUS. Refuse-over-guess applies to O's claims too. → [[campaign_methodcall_fat_return_and_triage]]
- **★★★ 2026-07-25(f) — 🏁 WO-GetRefNullableRefuse: REFUSE the get family for V=Nullable (E1051) and defer ADR-0088.** `36dad8e`, gate 462. A stray coverage label dug out the **double-nullable design cliff** `T??` (outer key-found vs inner stored-null → a silent flatten = a silent wrong result). G chose a clean refusal plus a deferral. O's poison showed the MIR breaks as well. → [[campaign_typed_collections]] [[feedback_cham_ma_chac_pattern]]
- **⚰️ 2026-07-25(g) — TRIAGE: "`&0 Enum` consumption" is a ZOMBIE (buried, NO WO).** Recon-before-WO saved us a 4th time: it was already fully closed by Slice A `d4486ad` and the borrowck foundation `7cd387d`; 6 probes and 6 fixtures prove it. `match` is the ONLY consumption path for an enum. **Recurring rule: backlog labels are NOT trustworthy** (4 zombies buried so far).
- **★★ 2026-07-25(e) — 🏁 WO-Robust-InstResults: killed the `inst_results[i]` panic-index ICE class (Track B rule #1).** `3fc170f`, gate `0·clean·0·458·0`. 10 bare sites → the checked helper `nth_call_result(...)?` → `JitError::CallResultArityMismatch`. 🩸 O re-poisoned itself → PANIC, and the restore matched by md5. ⚖ **G corrected O**: "a poison-only issue today is an ICE tomorrow — SOUNDNESS BEFORE SYNTAX, fix the crash BEFORE opening a feature." → [[campaign_typed_collections]]
- **★★★ 2026-07-25(d) — 🏁 WO-JIT-PRINT / ADR-0087: stdout WORKS FOR THE FIRST TIME since the rewrite.** `25cb2cc`, gate 458. 4 overloads × 4 extern-C shims (memory responsibility hardcoded into the symbol name) plus a `ReturnShape::Unit` branch (G rejected O's throwaway-i64 trick). O's recon missed the typecheck env → D saved it twice via LAW 4. Debts: read_line · f-strings · buffering. → [[campaign_jit_print_adr0087]]
- **★★★ 2026-07-25(c) — 🏁 `&0 String` READ-BORROW COVERAGE + 2 zombies and 1 phantom buried.** `5dd2aeb`, gate 458. "Enum? param SIGILL 132" = ZOMBIE; the `&+ T` borrow param = PHANTOM (`&+` is a unique-OWNER move, sharing is BYOS FROZEN). FREE-count teeth deduped 1/2/3. → [[campaign_str0_coverage_and_triage]]
- **★★ 2026-07-25(b) — 🏁 WO-SPOF-3: a canary sealing the `builtin_shim_meta.arg_consumes` SPOF.** `c22a751`. G separated two threats O had conflated; T2 was BURIED as impossible (M3 is load-bearing). Two-directional teeth FREE 1→2 / 2→1. 🩸 O's blemish: missed the shape 3× — "do not design tests from imagination". → [[campaign_shim_meta_spof_adr0085]]
- **★★ 2026-07-25 — 🏁 WO-SPOF-1: refuse container `Nullable(Struct-heap)` (ADR-0065 §15).** `0285bf2`. 🔑 O flipped G with measurements: no reachable live UB; the real hole was a single-layer SPOF. D's blemish: a summarized gate → O REJECTed with "Paste the raw gate or get out". → [[campaign_spof_container_nullable]]
- **★★ 2026-07-24(c) — 🏁 FRONT-B WO-1: PANIC AUDIT of `mir_lower.rs` group A + `JitError::Internal` + `cargo fmt --check` added to the gate.** `e2db04d`. 9 sites → Internal(4)/env-keep(2)/OOM-defer(3). O's blemish: presentation order (the gate buried) → hence the law to report the gate first. → [[campaign_front_b_panic_audit_group_a]]
- **★★ 2026-07-24(b) — 🏁 FRONT-A: `LowerError` error-code system (ADR-0086, 8 codes `triet::lower::E11XX`; E1190 = ICE).** `dbde2d5`. 🩸 D caught its own half-done recon (8 constructors → ~47 construction sites) BEFORE the WO was issued. → [[campaign_front_a_lower_error_codes]]
- **★★★ 2026-07-24 — 🏁🏁 ADR-0085 seals the `builtin_shim_meta` SPOF.** `f6b569f`. An 8-entry table plus a `Body::verify()` gate mapping `__triet_` → `UnknownShim`. **LAW 18 caught O ×3 in one session** (poison saved all three) ⇒ G's decree: every mechanism presented MUST include a MIR trace map. D refuted O 2/2. → [[campaign_shim_meta_spof_adr0085]]
- **★★★ 2026-07-20 — 🏁🏁🏁 "FORGOT NULLABLE" CLOSED + the `free(1)` UB killed + full-sret nullable aggregates.** `51edd0e`. The "match exact FORGOT Nullable" family had 6 members. Live UB: `Vector<Leaf?>` → 134. D refuted O **12/12**. → [[campaign_forgot_nullable_sweep]]
- **★★★ 2026-07-19 — 🏁🏁 the "FORGOT Nullable" family × 3 sites + a silent shim-borrow leak. 🔴 UNCOVERED the `arg_consumes` SPOF.** `aa9e584`. The lowerer and the JIT M3 read the SAME table ⇒ no defence in depth. Law: **the oracle is also an assumption** (demand pointer dedup). D refuted O 8/8. → [[campaign_nullable_position_and_temp_ownership]]
- **★★ 2026-07-18(b) — 🏁 PA-A NULLABLE-ENUM-IN-AGGREGATE.** `5c713c4`. A silent miss (`Nullable(Enum)` looked up in `struct_map` → `mid.m`=42). G swapped the roles: N3 = soundness, N1 = policy gate. D refuted O's WO 5/5. → [[campaign_nullable_enum_aggregate_pa_a]]
- **★★★ 2026-07-18 — 🏁🏁 MAJOR SURGERY ON THE BORROWCK FOUNDATION + `&0 Enum` (4 fronts).** `7cd387d`. Two opposing diseases masked each other (under-refusing Assign / over-refusing Drop-reference); the engine was right, the INPUT was garbage. Fixture 102 is a real UAF. D refuted O 4/4. Foundations do not borrow bricks from features. → [[campaign_borrowck_nll_foundation]]
- **★★★ 2026-07-15→17 — THE GET_REF / GET-BY-VALUE CHAIN CLOSED (ADR-0079 §AMEND + ADR-0082 §AMEND-3).** `8981135` gate 385 · `006b6c7` 381 · `12adc0b` 361. get_ref aggregate zero-copy swept E1041/E1049; get-by-value is non-destructive for Copy aggregates, heap-bearing → E1049. 🩸 The 8B-aggregate deref-thin bug landed for the **THIRD time** → SIGSEGV. **O corrected its own WO twice** (the `&0` dangling failure mode is a silent slip, NOT a SIGSEGV). **D downgraded its own claim** from double free to leak (the opposite of fabricating). → [[campaign_typed_collections]]
- **★★ 2026-07-03→13 — THE TYPED-COLLECTIONS CHAIN (ADR-0080/0082/0083).** key-typed `HashMap<String,V>` `381979e` · read-side get-borrow `96f4241` · Slices A/B/C element + value aggregate `1e49058`/`c22da0a`/`6d9e144` · value move-out + `pop_front` `3e0975d`/`5462c5b` · aggregate keys, struct and enum `1c08a67`/`91c273a` (gate 331→354). 🩸 Lessons: **hash content cannot be given teeth by a small-cap round trip** (16-align mask) · **two bugs masking each other** produced "2 false sounds" · **a stale binary → the LAW to rebuild before running a binary** · §6 fnptr-before-stride is the shield against size collisions. → [[campaign_typed_collections]]
- **★ 2026-06-29→30 — OWNERSHIP FIELD MOVE-OUT CLOSED + the heap-aggregate cluster.** `4165c18` gate 308 · WO-0073/74/75/76 `3738eb5`/`e0b1ed7`/`bd614f3`/`994afc8` gate 303-306. **💀 The "dynamic drop flag" COLLAPSED — O refuted G: THE SLOT ITSELF IS THE FLAG** (a static tombstone, 0 `brif`). Recon into the heart dug out a **hibernating fixpoint hole** (UAM slipping through a back edge). **D caught O overclaiming "SIGSEGV" when it was really a silent leak.** The module-system facade: **O refuted both Giang AND G, and G admitted error #2** → verify-don't-trust applies to Mentor G as well. → [[campaign_truc_b_heap_in_aggregate]] [[campaign_path_separator_and_partial_move]]
- **★ 2026-06-19→27 — ADR-0062→0072 (heap nullable · CFG tail · match literal · nullable aggregate · trait Tier1 · partial move · `::` paths · expected-type propagation).** gate 211→303. ADR-0072 killed the global `c.sig.return_type` proxy. ⛔ **ADR-0068 Box/recursive is BARRED.** → [[campaign_aggregate_nullable]] [[campaign_expected_type_propagation]] [[campaign_heap_nullable]] [[campaign_typecheck_exhaustiveness]]
- ★ 2026-06-22(b) — POSITIONING: TERNARY-FIRST, "AI-first" removed. `8ab55b8`. Coherence per VISION §8 = one Ł3 algebra across null/logic/capability. [[doc_highlights_and_ternary_seeds]] [[project_vision_os_capable]]
- ★ 2026-06-17(c) — DECISION: KEEP the `return` keyword (G wanted it beheaded; O pushed back citing `~->`/ADR-0020). [[lang_return_keyword_survives]]
- ★ 2026-06-09→12 — Tier D fat-pointer ABI · MirType/borrowck MIR NLL · Outcome `T~E` 2-register + sret · ADR-0059 `&0` heap · ADR-0060 nested `a.b.c`. Details: [handoff 06-12](handoff_2026_06_12_adr0060_nested_aggregate.md) · [06-11 `&0`](handoff_2026_06_11_muiC_adr0059.md) · [06-11 CFG/Outcome](handoff_2026_06_11_adr0055_tail_expr.md) · [06-10 heap Outcome](handoff_2026_06_10_op2_dong.md) · [06-10 debt repayment](handoff_2026_06_10_op1_dong.md) · [06-09 Tier D](handoff_2026_06_09_bac_d_closed.md)
- [★ THE REALITY OF THE REWRITE — READ SECOND](project_rewrite_reality_2026_06_04.md) — the v0.2-v0.10 backend was DELETED, 13 crates, starting over.
- [docs/HIGHLIGHTS.md + the three-way backlog](doc_highlights_and_ternary_seeds.md) — I✅/II🎯/III🌱. #2 rounding and Outcome-discriminant-as-Trit deserve an ADR.
- [★ Outcome Monadic Pipeline (`->>`), MapError (`-->`) & Explicit Error Unions `(E1 | E2 | E3)`](idea_outcome_flatmap_and_error_union.md) — Pre-Self-Hosting pipeline algebra (Giang & Mentor G, 2026-08-24).
- [★ Post-Self-Hosting (Triết 1.0) Vault: Odin Gems (Context Allocator, Swizzling, BitSet) & Comptime Metaprogramming](idea_post_self_host_odin_gems_and_comptime.md) — Strictly post-self-hosting roadmap (Giang & Mentor G, 2026-08-25).
- [Future — a Comparable trait + the ?-family](future_comparable_trait_and_monad_gap.md) · [sized ternary ints](future_sized_ternary_ints.md) — deferred.
- [Future — ternary placement syntax +T/T/-T](idea_ternary_placement_syntax.md) — O found 4 holes (non-polar placement dilutes coherence §8). PARKED until ADR-0068 reopens.
- [★ Post-Rust Architectural Foundations: Trilean, Placement (+T/T/-T), Directional Borrows (&0/&+/&-), 90/10 Pit of Success, Precompiled Libs, Two-Tier Unsafe](idea_post_rust_architecture_and_ternary_foundations.md) — Comprehensive pre-ADR foundations (Giang & Mentor G, 2026-08-23). Details: `docs/proposals/post_rust_architecture_foundations.md`.
- [Triết — the OS-capable vision](project_vision_os_capable.md) · [project overview](project_triet_overview.md) · [parser → schema AST](project_parser_schema_migration.md)

## Personas (TWO personas, different roles — do not confuse them)
- **[★ Mentor O — DEFAULT FOR EVERY SESSION, no need to be called by name (CLAUDE.md §AI Persona, 2026-08-02)](mentor_o_persona.md)** — Opus, **gatekeeper / review owner**. Verify-don't-trust (runs the gate personally), teeth-must-go-red-when-the-guard-is-removed, refuse over guess, never fixes things for others, admits false alarms, freezes the tree while grading. **Since 2026-08-02 O is the ONLY role that spawns D** (`subagent_type: "colleague-d"`) and must relay D's raw gate to G and Giang.
- **[★ Colleague D — Strict Colleague](colleague_d_persona.md)** — **the implement side, THE SINGLE SOURCE OF TRUTH**; **a SUBAGENT of O since 2026-08-02** (agent file `.claude/agents/colleague-d.md`; Giang no longer relays WOs, G has no channel to D). 6 rules + Rule #7 refuse over guess + **G's IRON LAWS** (① gate on the first line, matching the submitted tree · ①b pre-existing claims need a stash diff · ② fmt + clippy + tests before reporting · ③ never delete a negative test · ④ stuck → ask O). Patterns: reporting prettier than reality, and blaming the infrastructure.

## User
- [Webapp dev, vision-driven, defers technical decisions](user_role_webapp_dev_visionary.md)
- [Vietnamese + non-engineer tone](user_communication_vietnamese.md)

## Feedback
- **[⚔ Verify the PRODUCER before the CONSUMER](feedback_verify_producer_before_consumer.md)** — flipping a field type while the producer still emits the old representation = NOT migrated. REJECT.
- **[★ The 7-step loop + 4 roles](feedback_collaboration_loop.md)** — O is the verification checkpoint and does NOT code; told to "proceed" → define the teeth, then wait for D's submission.
- **[⚔ Poison must go red](feedback_poison_must_be_red.md)** — a structural fix whose poisoned logic leaves the test green = REJECT. Never trust a test by its name.
- **[★ The 5-section report protocol for G](feedback_g_report_protocol.md)** — O assembles the 5-section package and reconciles it against G's reply before recording it.
- **[⚠ TEETH never use git checkout](feedback_teeth_never_git_checkout.md)** — snapshot to /tmp with `cp` FIRST, restore with cp/Edit, never git checkout/restore/stash.
- **[⚔ Failure modes must be PRECISE](feedback_failure_mode_precision.md)** — D claimed SIGSEGV when the reality was a LEAK. 4 distinct signals (134 double free / 139 bad deref / FREE==0 leak / FREE==2). G despises Hollywood. An aggregate field with no slot → SIGSEGV; a heap scalar or `T?` with no slot → a silent leak.
- [Verify semantics before asserting](feedback_verify_semantics_before_asserting.md) — probe before encoding semantics into a test.
- [Stability over speed](feedback_stability_over_speed.md) — architectural decisions get an ADR.
- [Syntax — verbose + ~~dot paths~~](feedback_syntax_verbose_dot_paths.md) — `module`, not `mod`. ⚠️ dot paths SUPERSEDED by ADR-0071 (`::` for paths).
- [Option A — defer cleanly](feedback_cham_ma_chac_pattern.md) — at a design cliff: ship the diagnostic plus an ADR backlog entry, NOT a skeleton.
- [Implementer's choice](feedback_implementer_choice.md) — the author delegates implementation internals; the AI picks.
- [Quality over speed](feedback_quality_over_speed_v0_10.md) — the AI is the technical-quality owner, with a pre-commit self-audit.
- [Tiered Opus/DeepSeek workflow](feedback_tiered_opus_deepseek_workflow.md) — unsafe/ABI/IR work is Opus-only.

## Reference
- [Source-of-truth docs](reference_spec.md) — paths to SPEC/VISION/ROADMAP/TODO/ADR/CLAUDE.md.

## Triết language gotcha
- [Trit literals — the suffix form](triet_trit_literals.md) — `1_trit/0_trit/-1_trit`, not `0t+` (that is a balanced-ternary Integer). SPEC §1.5.1.
