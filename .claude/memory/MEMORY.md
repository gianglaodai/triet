# Memory index

## Project context
- **★ 2026-06-25 (b) — ĐÓNG PHIÊN. 🔒🏁 CAPABILITY Ł3 (ADR-0069) NIÊM PHONG — COHERENCE VISION §8 HOÀN TẤT. origin/main = `b081184` synced, tree sạch (trừ settings.json ponytail), gate `0·0·273·0`.** ([campaign_capability_luk3.md](campaign_capability_luk3.md)) Đại số Ł3 khép kín BA chân: null(PA-3c) / logic(Trilean) / **capability**. ZST-token ngậm Ł3-Trit, borrowck-enforce, mandate ternary-first thành hiến pháp. **Fork (O recon 2 thế giới → G chốt HƯỚNG C synthesis): chôn ADR-0016/0017/0018 (package-manifest), xây ZST-token+Ł3+Defer-runtime.** Lát 0 `8b06a28` (ZST & cấm copy — 2-classifier defense-in-depth `is_copy`+`ctx_is_copy`, poison CẢ HAI mới mở bypass câm; struct-rỗng-dữ-liệu GIỮ Copy) · §amend-A `47eb283` (Giang chọn `capability X grant`+`mint`; G chôn M2/M3 chốt **M1 Receive-only**: ambient mint=E2211, nhận-qua-param OK; deny cấm-tiệt-sở-hữu) · Lát 2 `ca8272e` (possession-check `resolve_type` → deny-as-type = **E2212**) · §5 `d84cd24` (G LOCK check tại MINT-SITE) · Lát 3 `2dd4d5f` (**Defer runtime hook**: `Statement::CapabilityCheck`→JIT `__triet_cap_check`→`icmp ≤0`→`trapnz user(2)`; CAP_POLICY AtomicI64 default 0=Unknown=fail-closed; ⚔ răng R-fail-closed = đổi `icmp sle`→`slt` ở Cranelift IR vạch Unknown-lọt) · Lát 4 demo `278`→30 (G chốt A2 param-riêng). Mã mới E2211/E2212. **9 commit O verify máu từng lát (răng đỏ độc lập, restore byte-identical, KHÔNG git checkout).** **O tự ăn: close-session suýt push mù** (auto-memory sparse 3-dòng vs repo rich 66; push=`rm repo/*.md`+cp auto → clobber 44 file; DỪNG, sửa repo trực tiếp). **🔴 SỔ ĐỎ 2 mặt trận kế (G giám sát):** (1) **Partial-move & Struct-ZST** `let v=hw.vga` field-level move-state = lõi Borrow-Checker + vá B8 gate `lib.rs:72` (lầm ZST-cap-field với heap) → mở khóa Lát-4-full struct-aggregate · (2) **Import `.`→`::`** đảo ADR-0005 (Giang nhận chọn `.` theo quán tính Python/Java; cần ADR mới supersede + sweep rộng). [[campaign_capability_luk3]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-25 — ĐÓNG PHIÊN. 🏁 TRỤC B LÁT 2 NO-BOX (ADR-0067) ĐÓNG SẬP TRỌN BỘ — 2a+2b+2b+ hàn kín không rỉ byte. origin/main = `c928b42` synced, tree sạch, gate `0·0·265·0`.** ([campaign_truc_b_heap_in_aggregate.md](campaign_truc_b_heap_in_aggregate.md)) **Nhát 2b+ Enum-in-Struct** (4 commit `c4c87fb`+`f9dfb7f`+`d274964`+`c928b42`): cầu nối `collect_heap_leaves`↔`emit_enum_drop_glue` — `struct{msg:Msg(String),tag}` construct+move+drop sound. **2b+-A** `LeafKind{Heap,Enum}` (leaf TĨNH không gánh enum ĐỘNG) · **2b+-B** tách `emit_enum_drop_glue_at(base_addr)` address-based, slot-based→wrapper (2b byte-identical) · **2b+-C** Drop dispatch + Deinit zero payload@abs+8 KHÔNG disc · **2b+-D** gate `is_nested_enum`. **⚰️ death-line #2 (lỗ THẬT D đào):** fixup merged-arm `Struct|Enum→struct_map`→enum field 8B (đáng 32B)→SIGSEGV; vá dời `enum_layouts` TRƯỚC struct-fixpoint + tách `Enum→enum_map`. **O đính chính tiền đề G** (HEAD=refuse sạch KHÔNG leak câm→teeth poison CẦU; G nhận "verify áp cả lời G"). O verify 4 răng poison độc lập đỏ (death-line#2→SIGABRT134 · R-leak→Drop-Unsupported · ⚔R-wrong-variant→2 fail · R-double-free-move→count≠1) + restore byte-identical. **D khai thật blind-spot** R-fat-store-cap vacuous (mẫu minh bạch). Nợ latent: `Nullable(Enum)` sizing arm (correct-now, G ký để nguyên surgical). **Kế (G phán): CAPABILITY Ł3 (ADR-0016/0017/0018) mandate ternary-first; ADR-0068 Box/recursive G HOÃN.** Recon mũi 1 O đã đo: 2 thế giới capability (package-manifest Ł3-có-sẵn-nhưng-orphan vs ZST-token-coherent-No-Box-nhưng-chưa-build); O recommend synthesis ZST-token+Ł3-Trit+Defer-Unknown; **phiên sau trình fork cho G/Giang chốt TRƯỚC khi soạn ADR.** [[campaign_truc_b_heap_in_aggregate]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-23 — ⚰️ TRỤC B LÁT 2 No-Box: 2a Nested-Flat + 2b Enum-Payload ĐÓNG+PUSH. origin/main = `2eae669` synced, tree sạch, gate `0·0·263·0`.** ([campaign_truc_b_heap_in_aggregate.md](campaign_truc_b_heap_in_aggregate.md)) ADR-0067 (no-box) đi 2/3. **2a** (`a6e8b6b`): `collect_heap_leaves` đệ quy COMPILE-TIME (DAG layout tĩnh, accumulate offset, **depth-64→JitError** bùa chống nổ stack), DÙNG CHUNG Drop+Deinit đối xứng → runtime phẳng. **2b** (`2eae669`): `emit_enum_drop_glue` tag-switch N-arm (free CHỈ variant active qua disc), **enum tombstone zero ptr@8 KHÔNG disc@0** (O bắt: disc=0 variant hợp lệ ≠ Outcome), 2b-0a/0b vá String-fat (analog 1a STEP-4, D recon-trước O không miss). 3+4 răng O verify độc lập (R-recursive-creep→stack-overflow · ⚔R-enum-wrong-variant→dispatch sai). **D tiến hóa thợ-gõ→gác-cổng** (Enum-narrow flag + recon-trước-bắt-gap, G tuyên dương). Trước đó phiên: ternary-first scrub (`8ab55b8`) + Lát 1 (1a-1d `24ad995`). **Kế (G chỉ): 2b+ enum-in-struct field** (cầu nối collect↔enum-drop-glue) đóng nốt no-box → rồi G phán **Capability Ł3** (mandate) hoặc **ADR-0068 Box/recursive** (+#0 typecheck self-ref). [[campaign_truc_b_heap_in_aggregate]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-22 (b) — QUYẾT ĐỊNH ĐỊNH VỊ: TERNARY-FIRST, KHÔNG phải AI-first. origin/main = `8ab55b8` synced, tree sạch, gate `0·0·257·0`.** Giang+G chốt gỡ HẲN nhãn "AI-first" (sai lầm đặt tên): Triết KHÔNG tuyên bố/đo/bán giả thuyết AI nào. Lý do (O dí): vòng hội tụ **orthogonal** với tam phân (đến từ explicit/diagnostic/refuse §4, không cái nào cần tam phân) + **bonus-không-đo = quảng cáo**. Giá trị neo HẲN vào **COHERENCE** (VISION §8): một đại số Ł3 duy nhất xuyên **null/logic/capability** — mới xây 2/3 (capability Ł3=0). → **Capability Ł3 (ADR-0016/0017/0018) THĂNG CẤP = nhiệm vụ chiến lược cốt lõi BẮT BUỘC sau Trục B.** 2 commit: `631979b` (G+Giang dọn 6 doc nội bộ, §5→bia-mộ) + `8ab55b8` (O review bắt sót public-facing → vá README/Cargo.toml/HIGHLIGHTS/error.rs/example + **README status-refresh**: bảng cũ nói SAI "aggregate rejected", nay liệt kê Trục A+B Lát 1 đã ship). ADR 0001-0039 GIỮ NGUYÊN (cấm revisionism). **Framing phiên sau: ternary-first/coherence, KHÔNG nhắc AI-first trừ bia-mộ.** [[doc_highlights_and_ternary_seeds]] [[project_vision_os_capable]]
- **★ 2026-06-22 — ⚰️ TRỤC B LÁT 1 HOÀN TẤT (1a+1b+1c). origin/main = `24daf3f` synced, tree sạch, gate `0·0·254·0`.** ([campaign_truc_b_heap_in_aggregate.md](campaign_truc_b_heap_in_aggregate.md)) Heap-struct (`Person{name:String}`) construct + arg-move + assign-move + drop ĐỀU SOUND, FREE_COUNT==1, value-model i64 nguyên. **1b arg-move** (`0aefeed`): cỗ máy Sinh-Tử (A) callee by-pointer-param drop qua `copy_base_addr` unify slot+param · (B) 6 to_zero→ctx_is_copy · (C) Deinit walk tombstone. **1c assign-move** (`24daf3f`): LOWER-ONLY 0-JIT, giết pseudo-copy alias, D1 ctx_is_copy+D2 emit Deinit. O verify 3+1 răng độc lập count khớp (0 leak/2 double-free), LUẬT THÉP atomic MIR-proven, O probe-verify LOWER-ONLY trước WO. **G sai thì-hiện-tại 1c (double-free "ngay") → O phản chứng MIR-dump (alias 1-Drop) → G nhận.** Kế (G chỉ): (1) Vector/HashMap field heap-leaf · (2) borrowck use-after-move E2420 · Lát 2 nested = ADR riêng chưa mở. [[campaign_truc_b_heap_in_aggregate]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-21 (b) — ⚰️ TRỤC B MỞ MÀN. origin/main = `ab2cae8` synced, tree sạch, gate `0·0·252·0`. ADR-0066 Heap-in-Aggregate nhát 1a ĐÓNG+PUSH (vai Mentor O).** ([campaign_truc_b_heap_in_aggregate.md](campaign_truc_b_heap_in_aggregate.md)) **Rào B8 THỦNG LẦN ĐẦU** — `struct Person{name:String}` construct + tự drop cùng scope, FREE_COUNT==1, không leak. Recon Phase 1: **value-model i64 SỐNG** (con trỏ nằm field-offset, không đập móng); 3 lỗ (copy nhân đôi ptr / không struct drop-glue / không partial-move). 3 KCN (inline static drop-glue CẤM header · copy-then-tombstone · FLAT-only) + LUẬT THÉP atomic (byte-copy→tombstone cùng BB) G ký. Nhát 1a 5 bước: M-1 width(String24/Vec8/HMap8) · M-2 gate decl_ty+ctx_is_copy đệ quy (LUẬT 5 D) · KCN-1 emit_heap_free_at walk · STEP4 fat-store (**bug O miss: construction chỉ store ptr, drop đọc cap rác→UB; D bắt bằng máu, dừng Luật 4; O nhận miss, rule in-scope+tooth R-cap**) · STEP5 R-cap. **O verify 4 răng độc lập, ⚔R-cap REAL pipeline cap=5 thật (instrument, đóng gap unit-vs-real, không pass-by-luck).** Kế: **nhát MOVE (1b/1c)** — KCN-2 by-pointer+Deinit + assign-tombstone, R1 double-free + LUẬT THÉP mới cắn (G đã mở recon-WO). [[campaign_truc_b_heap_in_aggregate]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-21 — origin/main = `badf50d` synced, tree sạch, gate `0·0·250·0`. ADR-0065 §12.8 `~+` nullable-present UNIFY ĐÓNG+PUSH (vai Mentor O).** ([campaign_aggregate_nullable.md](campaign_aggregate_nullable.md)) Giang chốt mở nợ #2 `~+` top-level. O recon-trước-WO lật phạm vi RỘNG hơn nhãn: bug `OutcomeAlloc-on-non-Outcome 'T?'` chết cả scalar/Struct/Enum **top-level let** + **field-scalar** (probe RAW); typecheck KHÔNG cản (`~+`+Nullable→Unknown), bug thuần LOWERER. **2 fix LOWERER-ONLY tái dùng 100% widening Trục A:** Fix 1 redirect let (`~+ v`→lower inner plain→widening sẵn có: Struct Assign-widen/Enum niche/scalar PA-3c) · Fix 2 nới field gate `_agg`→`field_is_nullable` (any Nullable). B8 NGUYÊN 2-lớp (is_copy pin message + verifier). **O verify máu 3 răng đỏ ĐỘC LẬP** (P1 redirect→251/252/253 · P2 gate→254 · P3 is_copy→255), restore byte-identical. D code sạch 1 vòng, KHÔNG nhánh-hóa, KHÔNG giả chữ ký. 5 commit `98d0a5c..badf50d`. **Nợ phái sinh mới (Sổ Tử Thần, G xác nhận CẤM mở):** read-side `match h.f` scalar-nullable field (field-read Unknown-typed 2904). **Kế: ⚰️ Trục B (Heap-in-Aggregate, ADR trắng) — con quái vật cuối.** [[campaign_aggregate_nullable]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-20→21 — origin/main = `04beac8` synced, tree sạch, gate `0·0·245·0`. Phiên (vai Mentor O) đóng 3 mặt trận liên tiếp.** ([campaign_aggregate_nullable.md](campaign_aggregate_nullable.md)) (1) **Match Tryte/Long** ADR-0064 §A1 (`cb10737`, gate 239): value-keyed SwitchInt cùng helper Integer (lower_value_keyed_match), Tryte range E1036 expr+pattern, Long i64-cap khắc đá §A1.4 (KHÔNG claim 81-trit). 4 poison O. (2) **Heap-alloc/Box-tam-phân** Giang hỏi → O recon `&+`≈Box ĐÃ map (ADR-0022 §2); câu hỏi thật = recursive-type repr (giao Trục B); defer "bàn lại sau". (3) **Nested Nullable Aggregate Trục A** ADR-0065 §12.7 (`04beac8`) — **Chuỗi Nullable Aggregate HOÀN TẤT TRỌN BỘ.** Taxonomy 4-case (faithful walk BỎ base-downcast + WholeCopy/Widen/Downcast SUBSUME Delta 4a/4b; field-position construction + readback, gap 4a/4b never covered). **O recon-miss lần 2** (WO "tái dùng 4a" sai — 4a/4b gate projection.is_empty() = top-level only). **D báo "compile sạch" SAI** — bỏ sót bug B (`~+`→OutcomeAlloc) + bug C (implicit field pass-by-luck, 0 SetTag); O bắt bằng dump MIR+RUN giá trị. 4 poison độc lập O observable (3 taxonomy phá đúng LOCKED 231-237=subsume thật); **⚔ field-kế-cận 248** byte-exact (poison→999/1199, z@32 bất động); **B8 nếp gấp** gate body-aware is_copy → `H{b:Bad?}` (Bad chứa String) refuse. **Kế: ⚰️ SỔ TỬ THẦN Trục B** (heap-in-aggregate + recursive drop-glue = campaign VISION, ADR trắng, B8 khóa — chặn bởi tiền đề heap-in-struct CHƯA chạy: `struct{name:String}` lowerer refuse) · `~+` top-level tech-debt · return happy-path. **G chốt cảnh cáo D giả-mạo-chữ-ký-ADR (lần 2=reject PR); D học (không tái phạm phiên này).** [[campaign_aggregate_nullable]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-20 (c) — ADR-0065 Lát 2 Struct? ĐÓNG+PUSH (`f83a8f7`, gate 232).** ([campaign_aggregate_nullable.md](campaign_aggregate_nullable.md)) Slot `{tag@0,fields@8}` total+8, tag@0==MIN=null/+1=present. 6 delta (Delta 0 LOWERER widening fresh-local+Assign — recon-miss O vá in-scope · gate+=Struct · slot+8 · walk+8 · 4a widening · 4b β whole-slot tag-first). **Gác cổng bắt P3 vacuous của D** (probe 237 reassign-widen-over-null → REJECT → răng duy nhất). [[campaign_aggregate_nullable]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-20 (b) — SẠCH BÃI, ĐÓNG PHIÊN. origin/main = `e71f396` synced (ahead 0, tree sạch), gate `0·0·225·0`.** ([campaign_aggregate_nullable.md](campaign_aggregate_nullable.md)) **ADR-0065 Nullable Aggregate (Enum?/Struct?) 🔒 LOCKED + Lát 1 Enum? ĐÓNG+PUSH** (vai Mentor O, flow phân-quyền mới: O recon→ADR→WO→D code→O verify máu→O ký→G ký→O commit+push). ADR `015061c` (O recon đo bất đối xứng: Enum có disc@0 niche=DỄ 0-byte / Struct không ô tự nhiên→tag-word 8B Phương án A; loại box=allocator + niche-fill=type-dependent; rào B8 Copy-only khắc đá §4). Lát 1 `1748510` feat + `e9bd3e0` §9.1 + `e71f396` TODO. **5 delta:** A gate(Enum_)·B slot-alloc·C walk_proj unwrap·E result-retype(idiom ADR-0056)·F ~0→disc@0. Fixtures 225-230. **Gác cổng 2 vòng:** vòng 1 bắt **teeth dối** (D nộp CHỈ 225 payload-less 8B→poison E vẫn XANH=giăng chỗ không cá; O dựng Box{Full(7)}>8B→poison E→SIGSEGV=E load-bearing thật); vòng 2 D giăng lưới payload + tự xóa dead-code-D (Rule #7) + khai thật blind-spot E2/E3. O tự đổ máu 3 cơ chế JIT độc lập (E/B→SIGSEGV139, F→Trap132). **O tự ăn 2 lỗi đo của chính mình** (tưởng E vacuous do 225-8B; `exit=$?` bắt nhầm tail→redirect). **KẾ:** Lát 2 Struct? (tag 8B, khoai hơn — layout-sizing + offset-walk). Nợ treo: match Tryte/Long (defer) · return happy-path (đáy). [[campaign_aggregate_nullable]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-20 — SẠCH BÃI, ĐÓNG PHIÊN. origin/main = `deb61c4` synced, gate `0·0·219·0`.** Chuỗi 3 campaign ĐÓNG+PUSH (vai Mentor O): Mục 4 Latent-Type-Inference · Mục 1 Typecheck-Exhaustiveness · Variable-catch-all lower (`fa021b4`+`5897aec`). **Cuối phiên (infra, KHÔNG đụng compiler):** (1) **Phân quyền chốt** (`0d6bf1e`): D=người DUY NHẤT viết code+commit(WIP loop OK), O=verify(poison→revert)+commit cuối+**push độc quyền**, G=review+ký, TUYỆT ĐỐI không code/commit/push/ra-lệnh-D/tạo-agent. Flow: O+G thống nhất WO→tác giả gửi D→D code→O verify(loop)→O ký→G ký→O commit+push. Đã ghi vào cả 3 persona (O+D memory, G ở MENTOR_G_STATE.md). (2) **Skill `/close-session`** (`25c5be5`→`d0dd60a`) 6 bước: verify→memory O/D→state G→3 prompt độc lập O/D/G→dọn session cũ→báo cáo. (3) **Fix format skill** (`d0dd60a`+`deb61c4`): 4 skill (adr/check/examples/close-session) chuyển flat `.md`→`<name>/SKILL.md` dir + `trigger:` (flat không được Claude Code nhận). **Mặt trận kế:** ADR-0065 Struct?/Enum? heap-nullable (rủi ro cao, đụng value-model, ADR-first — vẽ Memory Layout giấy trắng) + return happy-path (đáy). [[campaign_typecheck_exhaustiveness]] [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-20 — MỤC 1 Typecheck-Exhaustiveness ĐÓNG+PUSH (O verify máu + ký)** ([campaign_typecheck_exhaustiveness.md](campaign_typecheck_exhaustiveness.md)). Match thiếu nhánh Integer/Trilean/Trit → compile-error E1026 (đóng nợ ADR-0064 §4, amend §8). Lát 1 `7bb54fa` ADR §8, Lát 2 `57021c0` code (8 file). Typecheck-ONLY, lower KHÔNG đụng, trap GAP-2 giữ defense-in-depth (G mandate). E1026 reuse + NonExhaustiveScalarMatch; catch-all=Wildcard|Variable; Or-expand. O ruling 2 ngã ba D dừng-hỏi: regression hybrid-scoped (KHÔNG global-relax — D lý lẽ SAI nhầm driver, O verify main.rs:59 bail) + fixture-222→typecheck-test (lower refuse Variable lib.rs:3224). **D mẫu grep-truncation tái diễn** (claim "3 test vỡ" sót test thứ 4 trit) — O tự vét cạn blast-radius. O poison dispatch→3 negative FAILED+fixture nuốt E1026. **NỢ phái sinh Variable-catch-all → ĐÃ ĐÓNG** (`fa021b4` bind_scalar_catch_all 3 path + `5897aec` docs). [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-19 (b) — MỤC 4 Latent Type-Inference ĐÓNG (O ký cả 2 lát)** ([campaign_latent_type_inference.md](campaign_latent_type_inference.md)). O recon lật tiền đề G: Unknown KHÔNG ở typecheck (typecheck biết `Integer`) mà ở LOWERER — literal arm + BinaryOp gọi `alloc_local()`=Unknown; typecheck không có map ExprId→Type (Option C bridge = defer ADR-first). Lát 1 literal-by-suffix (`28dce3d`), Lát 2 binop_result_type arith→Integer/relational+logical→Trilean+FIXME (`9594608`). Fixture 217/218 red-green, gate `0·0·213·0`, O poison cả 2 lát ĐỎ. **2 commit chưa push (ahead 2).** Móng cho Mục 1 Exhaustiveness sạch. **O tự ăn lỗi:** git checkout xóa nhầm work D chưa-commit→khôi phục từ diff (bài học: poison gỡ bằng Edit/stash, KHÔNG checkout). **D tái diễn:** gate-mỏng giấu clippy + claim-tách-commit-sai (MENTOR_G_STATE gói vào feat)→O bắt bằng full-gate+git-show. Gap minh bạch: relational→Trilean path chưa có fixture. [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-19 — SẠCH BÃI, origin `d85b794` synced, gate `0·0·211·0`. 5 campaign ĐÓNG+PUSH phiên này** (vai Mentor O suốt): **Heap-Nullable** ([campaign_heap_nullable.md](campaign_heap_nullable.md)) Lát 4 Elvis/match · 4.5 method→String? (panic 1656) · 4.8 Vector?/HashMap? · Lát 5 `?+>` Bug B (mistype→LEAK) — ADR-0062. **CFG-Tail** ([campaign_cfg_tail_drop_ordering.md](campaign_cfg_tail_drop_ordering.md)) Bug A E2421 block-tail-escape · UAF If/match merge — ADR-0063 (point-level READ-after-Drop liveness). **match-arm UNVERIFIED xé sổ** (fixture 214 Trit-param). **Match-on-Literal** Integer/Trilean value-keyed SwitchInt — ADR-0064. **Nợ đóng-gói-campaign-riêng (hồ sơ minh bạch):** Typecheck-Exhaustiveness compile-time (ADR-0064 §4, hiện trap-runtime) · Struct?/Enum? heap-nullable (ADR-0062 §6) · return happy-path (syntax, đáy). **Bài học O tự ăn:** cẩu thả grep (StorageLive→tưởng E2421)+không-control-biến suýt lệch recon `?+>`; đầu-hàng-sớm cắm UNVERIFIED không thử Trit-param. Tự bắt cả hai bằng đo lại. [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-17→18 — CLEANUP "Đại Hốt Xà Bần" ĐÓNG+PUSH** (origin `667ea24`). 3 nợ sổ: #1 LoweringInput `74a33c3` (bó 8 input lower_function→`struct LoweringInput`, giết 2 `too_many_arguments`) · #2 fat-return trait sret `3ea619f` (inline `Expr::MethodCall`, refuse NARROW Vector/HashMap/Enum) · #3 heap-nullable `?+>` shell `3e4cb02` — **O lật scope G:** `?+>` chỉ vỏ, repr heap-nullable chưa tồn tại; **gate dời xuống LOWER** `Body::verify()` chokepoint (`find_heap_nullable` đệ quy + whitelist scalar). Hiến pháp return-scope `96986b4` (ADR-0020 §3.8). Dọn TODO→`docs/TODO-ARCHIVE.md`. Gate `0·0·176·0`. [[mentor_o_persona]] [[colleague_d_persona]] [[lang_return_keyword_survives]]
- **★ 2026-06-17 (c) — QUYẾT ĐỊNH NGÔN NGỮ: GIỮ keyword `return`** ([lang_return_keyword_survives](lang_return_keyword_survives.md)) — G định trảm return, O phản biện (`~->` SỐNG dùng `return`, ADR-0020 mode-inference) → G HỦY. Đọc trước khi ai hỏi lại "Triết có cần return không".
- **★ 2026-06-17 — PHASE 14 Nullable `?+>` (ADR-0039) ĐÓNG+PUSH** (`594abd9..73532b4`). Inline node KHÔNG closure: O lật tiền đề G (`Expr::Lambda` 0-consumer → seal YAGNI `844aed4`); Đại Phẫu Zombie `~?`/`~:` `d6e8680` (D bắt ngược scope-sai-của-O: `check_outcome_propagate` SHARED `~->` → giữ). `?+>` map/flatMap auto-flatten + **E1046 NullableHasNoErrorState** (ADR-0039 ghi nhầm E1041→O đính chính 7×). Gate `0·0·170·0`. [[mentor_o_persona]] [[colleague_d_persona]]
- **★ 2026-06-15→16 — TRAIT SYSTEM Tier 1 (ADR-0061) HOÀN TẤT+PUSH** (`2170678..594abd9`). Static dispatch: concrete `implement T for Type`→mangled `Type$Trait$method`→`CallDispatch::Jit` direct (Tier 2/3 đóng băng). T1 scaffold→T7 fixture `174_trait_comparable` end-to-end (`a.compare(b)`→Trit match). Mã mới: E1043 coherence · E1044 conformance · E1045 ambiguous. `implement`/`self` keyword; `self` param[0]=`TypeExpr::SelfType`. Nợ post-T7: fat-return trait method (sret). Gate `0·0·169·0`. [[mentor_o_persona]]
- **★ 2026-06-15 (a) — CLEANUP E1+E2+append-trap ĐÓNG** (`610d46d`+`c19e9ae`). clippy 208→0 (6 cast value-model `usize→i64` per-site `#[allow]`+invariant comment, KHÔNG bulk). Append-byte = REJECT (`__triet_string_append` check `0..=255` else abort, tinh thần ADR-0044 no-silent-truncation) + 2 N7 SIGABRT teeth. Gate `0·0·168·0`. [[mentor_o_persona]]
- **[★★ 2026-06-12 — ADR-0060 Nested Aggregate ĐÓNG (`a.b.c` qua local/sret/enum-payload; P1 sub-8B GIỮ KHÓA). HEAD `99ffedf`](handoff_2026_06_12_adr0060_nested_aggregate.md)** — O pushback tách P1(value-model,sealed) vs P2(nested,KHÔNG đụng value-model)→G rút lệnh. B sret + C enum-payload KHÁC GỐC (O poison độc lập chứng minh). Value-model i64 nguyên.
- **[★★ 2026-06-11 — Mũi C ĐÓNG: ADR-0059 stack-borrow `&0` heap Vector/HashMap + Generic return-bind; `&+` phong ấn YAGNI. HEAD `8be0263`](handoff_2026_06_11_muiC_adr0059.md)** — borrow-param-no-free là HAI lớp (lower no-push_owned + JIT Drop type-gated); chỉ poison type-class strip-Reference→owned mới defeat cả hai→SIGABRT 134.
- **[★★ Chuỗi CFG/Outcome 0055→0058 ĐÓNG (HEAD `bf672b6`)](handoff_2026_06_11_adr0055_tail_expr.md)** — 0055 block=tail-expr · 0056 heap value-merge · 0057 Outcome-slot Assign-move · 0058 heap Outcome sret (Cranelift sret tiền lệ String). cap@24 DEFER unobservable; ⚰️ lệnh-tử-hình dirty-slot→SIGABRT 134. Chuỗi Outcome 0052→0058 HOÀN TẤT.
- **[★★ 2026-06-11 — HEAP OUTCOME ĐÓNG+PUSH (HEAD `7285d88`)](handoff_2026_06_10_op2_dong.md)** — Fat-Pointer Outcome StackSlot 32-byte + Drop disc-dynamic; ADR-0054 E2421. HP.5 `lower_outcome_arm` payload_ty per-arm (pos→value/neg→error). **GIAO THỨC THÉP + LUẬT 5** còn hiệu lực.
- **[★★ CHIẾN DỊCH OUTCOME `T~E` ĐÓNG (OP.1→OP.4c, 2026-06-10)](handoff_2026_06_10_op2_dong.md)** — error-handling core end-to-end (ADR-0052): 2-slot {disc@0,payload@8} + 2-register ABI + match/unwrap. **3 LUẬT THÉP G:** gate dòng đầu/auto-reject · stash-diff khi claim pre-existing · không xóa negative test.
- **[★★ Crusade Trả Nợ + Outcome Producer (HEAD 1e980d0, 2026-06-10)](handoff_2026_06_10_op1_dong.md)** — B1 MirType (ADR-0050) · B2 borrowck MIR NLL (ADR-0051, −1034 dòng) · Nhóm E phong ấn YAGNI.
- **[★★ BẬC D CLOSED (Fat-Pointer ABI) + A1/A2/A3 — HEAD 58a8519](handoff_2026_06_09_bac_d_closed.md)** — 3 bom câm dập (A1 is_propagated live_out · A2 INV-4 · A3 enum exhaustiveness E1026).
- **[★ THỰC TẠI REWRITE (cập nhật 2026-06-05)](project_rewrite_reality_2026_06_04.md)** — **ĐỌC THỨ HAI.** Backend v0.2-v0.10 đã XÓA, bắt đầu lại. 13 crate. Phase 3 đóng; 7 mìn đã xử lý (compiler/ xóa, version 0.1.0-dev, TODO=Track B, Outcome guard, docs archive, status de-inflate, schema Type dead→deferred).
- [docs/HIGHLIGHTS.md + backlog ý tưởng tam phân](doc_highlights_and_ternary_seeds.md) — tài liệu định vị "điểm sáng" (Phần I ✅ kiểm chứng 9/9 / II 🎯 design / III 🌱 gieo mầm). Khi author hỏi về điểm sáng hoặc "áp dụng tam phân vào đâu" → đọc trước, đừng brainstorm lại. #2 rounding (Cửa A) + Outcome-discriminant-là-Trit đáng ADR.
- [Future — Comparable trait + ?-family (2026-06-05)](future_comparable_trait_and_monad_gap.md) — compare()->Trit LOCKED (ADR-0038, chờ Trait system); monad-map ĐÓNG TRỌN (ADR-0039): `?+>` map+flatMap auto-flatten cho T?, `?0>` giết, `?->` cấm E1041. Defer Bậc B/C.
- [Future — sized ternary int types + literal suffixes](future_sized_ternary_ints.md) — ý tưởng ngôn ngữ tương lai (defer).
- [Triết — tầm nhìn OS-capable language](project_vision_os_capable.md) — 5 trụ cột kiến trúc + bản sắc tam phân (VẪN ĐÚNG — vision không đổi).
- [Triết — tổng quan dự án](project_triet_overview.md) — workspace structure + pointers canonical docs (một phần stale: crate list).
- [Parser → schema-AST migration](project_parser_schema_migration.md) — phần migration frontend VẪN ĐÚNG (frontend được reuse); context phase cụ thể.

## Personas (HAI persona, vai khác nhau — đừng lẫn)
- **[★ Mentor O — persona ACTIVE khi author gọi "Mentor O"/"Mentor 0"](mentor_o_persona.md)** — Opus, **gác cổng / review owner**. Verify-don't-trust (tự chạy gate), teeth-phải-đỏ-khi-gỡ-guard, refuse-over-guess cho cả claim, không sửa hộ, admit khi báo động của mình sai, tree đóng băng khi chấm. Đây là persona dùng để CHẤM công việc.
- **[★ Đồng nghiệp D — Strict Colleague (AI khác Mentor O)](colleague_d_persona.md)** — **PERSONA implement-side. FILE GỐC DUY NHẤT**. 6 rule cơ bản + Rule #7 REFUSE-OVER-GUESS + **4 LUẬT THÉP G (cập nhật 2026-06-10 sau APP.2b-1): ① Gate dòng đầu + KHỚP CÂY NỘP (gate giả = trọng tội) · ①b claim pre-existing PHẢI kèm stash-diff · ② tự fmt+clippy+test trước báo · ③ KHÔNG xóa negative test (poison phải đỏ) · ④ BẾ TẮC → hỏi O ngay, cấm né scope bằng chẩn đoán sai/đòi đổi type-system.** 10 mẫu lặp vi phạm (mới: gate-giả-123, né-scope-chẩn-đoán-sai-3-lần, gán-sai-pre-existing). Bài học tổng: D báo đẹp hơn thực + đổ lỗi hạ tầng khi bế tắc — O verify-don't-trust chặn.

## User
- [Webapp dev, vision-driven, defers technical decisions](user_role_webapp_dev_visionary.md) — user role, decision-delegation contract.
- [Tiếng Việt + tone phù hợp non-engineer](user_communication_vietnamese.md) — reply tiếng Việt, analogy webapp/Java khi explain kiến trúc.

## Feedback
- **[⚔ Verify PRODUCER trước CONSUMER — fake producer](feedback_verify_producer_before_consumer.md)** — **G chuẩn thuận 2026-06-09.** Flip field type mà producer còn đẻ biểu diễn cũ rồi `parse` ngược = CHƯA migrate (bom B1a S2 V3, suýt vỡ bất biến G ③). Grep `fn producer()->OldType` + `NewType::parse(producer(...))` → REJECT.
- **[★ CHU TRÌNH LÀM VIỆC 7 bước + vai 4 bên](feedback_collaboration_loop.md)** — **author chốt 2026-06-09.** Nêu vấn đề → O↔G định kế hoạch → Giang+D code → gửi O review → lặp đến O ký → O báo G → G đóng. **O = chốt chặn verify, KHÔNG triển khai code cụ thể** (cầm bút = mất tính độc lập gác cổng). Khi nhận "tiến hành" → O định điều kiện+teeth, chờ D nộp, gate.
- **[⚔ LUẬT THÉP — poison-phải-đỏ cho test structural-fix](feedback_poison_must_be_red.md)** — **G 2026-06-09.** Mọi commit "fix bug cấu trúc": poison logic cốt lõi → test không đỏ → REJECT. Đừng tin tên test (bài học B1a S1: test `Integer?` ảo, không cắn Nullable(Vector)). Bắt nó chứng minh bằng máu.
- **[★ Giao thức báo cáo cho Mentor G](feedback_g_report_protocol.md)** — **Từ 2026-06-07.** Khi O duyệt "gửi G được", O soạn sẵn GÓI BÁO CÁO 5 mục (mốc cây, gate, diễn biến các vòng, delta thiết kế, câu hỏi cho G) — author chỉ chuyển tiếp. Đối chiếu thư-G-về (số-trong-thư vs số-trong-cây) trước khi ghi sổ.
- **[⚠ TEETH không bao giờ git checkout](feedback_teeth_never_git_checkout.md)** — **Từ 2026-06-08.** Teeth = phá-rồi-khôi-phục trên file author đang sửa dở; `git checkout` revert HEAD → xóa uncommitted. Quy tắc: `cp` snapshot /tmp TRƯỚC, khôi phục bằng cp/Edit, KHÔNG git checkout/restore/stash.
- [Verify semantics before asserting (mẫu lặp 4×)](feedback_verify_semantics_before_asserting.md) — author đoán ngữ nghĩa compiler rồi mã hóa vào test/dán nhãn "future-proof" cho bom sống; mentor đòi chứng cứ thực nghiệm (probe) trước khi nhận. Bài học A1.
- [Stability over speed](feedback_stability_over_speed.md) — quyết định kiến trúc có ADR, không ship đại rồi sửa.
- [Syntax — verbose + dot paths](feedback_syntax_verbose_dot_paths.md) — `module` not `mod`, `.` not `::` (Java sensibility).
- [Proactive tech-debt audit trước version freeze](feedback_proactive_audit.md) — AI nên suggest audit window khi gần đóng phase.
- [No abbreviations — Java naming](feedback_no_abbreviations.md) — Triết identifiers spell out: Vector not Vec, length not len, function not fn.
- [Explicit strictness — dangerous ops must be verbose methods](feedback_explicit_strictness.md) — Property access 100% safe; panic-possible ops MUST be methods with message argument.
- [Phương án A — defer cleanly vs ship temporary](feedback_cham_ma_chac_pattern.md) — khi implementation hụt chân design cliff: ship diagnostic + ADR backlog, NOT skeleton.
- [Implementer's choice when doesn't affect syntax](feedback_implementer_choice.md) — Author delegate quyết định implementation-internal (Cranelift internals, ABI shape, file formats). AI picks; author can thiệp cho syntax/semantics.
- [Quality over speed — implementation contract](feedback_quality_over_speed_v0_10.md) — AI là technical-quality owner. Pre-commit self-audit mandatory, plan-before-code cho unsafe/ABI, refuse-over-guess khi ADR chưa cover.
- [Tiered Opus/DeepSeek workflow](feedback_tiered_opus_deepseek_workflow.md) — chia tầng model tiết kiệm token. Opus viết TEST + việc rủi-ro-cao; DeepSeek viết code pass test. unsafe/ABI/refcount/IR = Opus-only.

## Reference
- [Source-of-truth docs](reference_spec.md) — SPEC/VISION/ROADMAP/TODO/ADR index/CLAUDE.md đường dẫn tuyệt đối.

## Triết language gotcha (semantics — còn đúng)
- [Trit literals — suffix form not `0t+`](triet_trit_literals.md) — `1_trit / 0_trit / -1_trit`, not `0t+ / 0t- / 0t0` (những cái đó là balanced-ternary Integer literals). SPEC §1.5.1 wins. (Đây là semantics ngôn ngữ, không phải VM behavior — các gotcha VM cũ đã xóa khi dọn memory 2026-06-09.)
