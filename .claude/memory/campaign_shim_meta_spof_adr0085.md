---
name: campaign_shim_meta_spof_adr0085
description: "✅ FULLY CLOSED 2026-07-25 — ADR-0085 plugs the SPOF builtin_shim_meta.arg_consumes. Beat 1 (P-exist): full 8-entry table + Body::verify() discriminator gate. Beat 2a: self-loan-exclusion M3 mutate-precheck. Beat 2 THREAT-1 canary (WO-SPOF-3, c22a751): shim_arg_consumes_spof_canary.rs pins the table against C-shim free behavior, measurable 2-directional teeth (push [t,t]→[t,f] FREE 1→2 double-free; pop [f]→[t] FREE 2→1 leak). THREAT-2 (WO-SPOF-2 disable_m3 isolation) BURIED permanently = architecturally impossible (Stmt::Let unconditional Drop → M3 load-bearing named-local tombstone, cannot isolate !consumed via FREE-count with M3-OFF). O slipped the Direction-2 shape THREE times (verifying consumer while skipping producer-gate), D+O caught themselves via MIR-dump. gate 0·clean·0·455·0."
metadata:
  node_type: memory
  type: project
  originSessionId: 82e5c422
  modified: 2026-07-25T02:53:16.321Z
---

## ✅ CLOSED — Beats 1 + 2a, signed by O+G, PUSHED (origin/main `f6b569f`, gate `0·0·452·0`)

```
f6b569f docs(todo): close Beat 2a self-loan-exclusion (ADR-0085 AMEND-2)
9a9cb72 fix: WO-N2a-Amend — replace vacuous T2 with a plain-local mutate-precheck tooth
7c8cbf2 feat: WO-N2a — self-loan-exclusion M3 mutate-precheck + restore mutates_arg:Some(0)
15b5b9a docs(adr): ADR-0085 shim-meta totality + verify-gate (Beat 1 close)
63887e4 feat: WO-N1 — full 8-entry table + existence gate in Body::verify()
```

## Background — SPOF `builtin_shim_meta` (discovered in [[campaign_nullable_position_and_temp_ownership]], debt recorded in [[campaign_forgot_nullable_sweep]])
Static table `triet-mir/src/lib.rs:1076`, read by **5 read-sites / 3 crates** (borrowck ×3, JIT M3 ×1, lowerer `emit_shim_call` ×1), all `if let Some`. A missing/wrong entry is silently swallowed: missing-consuming→JIT doesn't zero + lowerer schedules Drop = **silent double-free**; a false-flag borrow-labeled-as-consume→double-free, consume-labeled-as-borrow→leak. NOT defense-in-depth (one source, five places all wrong in the same direction).

## Beat 1 (P-exist) — `63887e4`+`15b5b9a`
**FULL 8-entry table** (added 7→**8**: `cap_check`/`hashmap_contains`/`pow`/`string_append`/`string_clear`/`string_contains`/`string_hash` + **`vector_contains`**) + **existence gate `Body::verify()`** discriminator `__triet_`: `CallDispatch` calling `__triet_*` when `builtin_shim_meta`→`None` = `MirError::UnknownShim` at **P3.5 (BEFORE borrowck/JIT)**, machine-fixable per ADR-0027, NOT a silent miscompile. Option β (single-gate verify) > α (5 copies of the predicate) > γ (registry refactor, too much blast radius) > δ (existence canary = a 4th copy of the oracle, circular). §append/clear initially got `mutates_arg:Some(0)` "to also fix E2440 while we're at it" — **AMEND-2 walked this back to `None`** (see Beat 2a).
🩸 **O's poison:** deleted `__triet_vector_push`→fixture 166 explodes `UnknownShim` at P3.5, restore md5 `14a6a39`. T2 (`some_user_fn`→Ok)+452 fixtures green proves the discriminator does NOT kill user-fns.
⚖ **D refuted O 2/2:** ① **table 7→8** — O's recon `comm` covered ONLY JIT-dispatch-names, missed `__triet_vector_contains` emitted from the **lowerer** `:2607` (live, running in fixture 86). ② **`mutates_arg:Some(0)` scope-creep** — wiring it in fired back 5 E2440 fixtures.

## Beat 2a (self-loan-exclusion) — `7c8cbf2`+`9a9cb72`+`f6b569f`
**MIR-grounded (refutes both D's and O's assumptions):** `clear(&0 mutable m)` lowers to `_1 = &0 mutable _0; Call clear(_1)` — args[0]=`_1`=**loan.dest** (a reference), NOT `_0`=m. The precheck `checker.rs:1294` compares `places_conflict(loan.source=_0, arg=_1, conservative)` → `_1` points at `_0` → alias → E2440 against **its own loan**. **Fix = mirror the U2 precedent `:1260`:** trace `arg`→`real_place` (loan.dest→loan.source) + `.filter(|l| l.dest != *arg)` excludes the self-loan. + restore `mutates_arg:Some(0)` for append/clear → genuine E2440-string-mutate works again. Blast radius: pop/remove (a plain container Local, not a borrow-dest) UNCHANGED.
⚖ **O caught T2-clear VACUOUS BEFORE signing off:** the original T2 used `clear` + 2 borrows `r`(&0)+`arg`(&0 mut) → E2440 came from an **ordinary borrow-conflict** at the creation of `arg` (ExclusiveMutable vs live ReadOnly), exploding BEFORE the precheck. Poisoning `.filter(|_l| false)` (blinding the whole precheck) → T2 **still passes** = a paper shield. With a reference-arg, a genuine non-vacuous concurrent case is IMPOSSIBLE (the borrow-conflict always explodes first). → G's Option A: delete it, replace with a **T2 plain-local** `pop(v)` that sidesteps the borrow-conflict, hitting the precheck directly.
🩸 **O poisoned the new T2 two ways (non-vacuous):** (a) `.filter(false)`→T2 FAILED · (b) over-exclude `source!=real_place`→T2 FAILED (**the teeth that pay off: catches the false-negative**). (T3) removing the line `l.dest!=*arg`→fixture 93 explodes E2440 (false-positive teeth, orthogonal to T2). Two teeth / two claims / two poisons. restore md5 `4357908d`.

## 🦷 RULE 18 CAUGHT O **THREE TIMES** IN ONE SESSION (a bad record) — same root cause: "designing by imagination, not looking at the memory model"
1. **Table 7→8:** half-hearted recon (`comm` covered one JIT branch, missed the lowerer-emit). Violates the very rule 19 O wrote into the WO ("grep the ENTIRE family").
2. **`mutates_arg:Some(0)` scope-creep:** bolted on a mechanism to catch E2440 that didn't match the `&0 mutable` calling convention (self-loan). "One arrow, two targets" → shot itself in the foot with 5 fixtures.
3. **T2 vacuous:** designed a spec-test using a reference-arg → E2440 came from a different layer, test was green regardless = a lie.
🔑 **All THREE were caught by the poison/D-measurement process BEFORE signing.** G's verdict: *"Humans are full of habitual ruts — only a paranoid process, poison testing, and hardware constraints save the project."* **New G decree:** on every future front, every mechanism must come with an **attached MIR trace map** — no more designing by poetry.

## Role notes
**D (Sonnet 5):** refuted O 2/2 correctly (actually wired+measured in the field, didn't blindly copy O's table), used poison CORRECTLY without falling into the trap (O's WO warned about the two-poison trap). Blemish: **reporting discipline — summarized logs 3 times** (WO-N1 ×2, WO-N2a ×1) → G issued a **STANDING INFRASTRUCTURE DECREE: every WO must close in foreground+timeout600000+raw, reject-outright if summarized** (enforced one final time, D coughed up the raw). Ran gate in background + did not commit WIP (round 1) → O caught it, forced a commit first.

## ⚰ BEAT 2b RETIRED (2026-07-24(b), signed by G) — gatekeeper audit surfaced the true nature
O recon'd exhaustively before issuing a WO (Giang decided "check coverage-gap first"): **the "per consuming shim" canary was STALE** — all 3 heap-consume flags (push elem, insert key, insert value) ALREADY had freed-once teeth + poisons pre-written in the `*_counting.rs` corpus (typed_vector #1, typed_hashmap #1/#4, vector_userstruct T-DOUBLE/T-LEAK). Building a new canary would fall straight into trap **δ**, already refuted by G. **THE ONE REAL HOLE** (a prior-O comment in `triet-lower/src/lib.rs:1490-1509` admits it): every tooth test uses the **UNIFIED pipeline** (lowerer `push_owned` + JIT M3 both on); because **M3 alone is already sufficient** (zero slot → Drop no-op), the `push_owned` layer is **redundant defense that is MASKED** — completely wrong yet the test stays green. The real tooth would have to **fire with M3 TURNED OFF** → requires disable-M3 JIT infrastructure that doesn't exist yet, to check a layer that today is not load-bearing. G's ruling: **a waste of war resources, Simplicity First** → demote Beat 2b from "urgent campaign" to a **push_owned-vs-M3-isolation defense-in-depth debt** in the debt ledger. 🩸 O nearly chased the wrong target (canary-fixture) — saved by producer+consumer evidence + the prior-O comment; matches G's decree "DUMP the MIR / read the source, no poetry."

## ✅ BEAT 2 THREAT-1 CANARY CLOSED + THREAT-2 BURIED PERMANENTLY (2026-07-25, signed by O+G, PUSHED `c22a751`)
G separated **TWO threat models** that O had mistakenly conflated:
- **Threat 1 (table lying about vs the C-shim):** teeth-able. Closed via `crates/triet-driver/tests/shim_arg_consumes_spof_canary.rs` — a canary **M3-ON** (`with_shims`, like production), counting-shim `__sacs_*` counts String-frees, 3 shapes (A push→FREE1, B insert→FREE1, C push×2+pop→FREE2). Teeth in 2 directions **planted and measured for real by O himself** (cp-snapshot mir `ff3d1fd1`, NOT git checkout): **Direction 1** consume→borrow `__triet_vector_push [true,true]→[true,false]` → A **FREE 1→2** (clean double-free count, NOT SIGABRT since the counting-shim doesn't actually free); **Direction 2** borrow→consume `__triet_vector_pop [false]→[true]` → C **FREE 2→1** (leaking "a"). Closes the "no test canaries arg_consumes itself" debt (note `triet-lower/src/lib.rs:1661`).
- **Threat 2 (table vs Lowerer `!consumed` — the old debt #1 "push_owned-vs-M3 isolation"): BURIED = ARCHITECTURALLY IMPOSSIBLE.** WO-SPOF-2 (adding `JitOptions.disable_m3` + an M3-OFF canary asserting FREE==1) **CANCELED — wrong premise**: `Stmt::Let` registers a Drop for a named local UNCONDITIONALLY → the `!consumed` branch only saves an **anonymous temp** from a redundant Drop; the named-local tombstone rests **ENTIRELY on M3**. M3 is NOT "redundant cover" — it's load-bearing for something ELSE. Turning off M3 → the baseline **double-frees on its own** (comment `triet-lower/src/lib.rs:1641-1647` had ALREADY correctly recorded this exact crash-mode `free(): double free ... tcache 2` SIGABRT before) → there is no clean baseline to isolate `!consumed`. D ran the M3-OFF canary → reproduced that exact SIGABRT → O verified independently + re-read note 1641-1647 (the evidence was IN O's own recon) → admitted the mistake, reverted clean (`git checkout` the diff D hadn't committed + removed the canary).

🩸 **O's BLEMISH — slipped the Direction-2 shape THREE TIMES, same root cause "verify the consumer path, skip the producer-gate":** ① `length(Vector<String>)` — no overload (Integer-only), typecheck rejects it (D caught it). ② `length(s)` owned String — a **fast-path** at `triet-lower/src/lib.rs:2701-2723` reads the `len` field directly, bypassing `emit_shim_call` (D caught it, via MIR-dump). ③ `length(&0 s)` — reference = **Copy** (`mir 629/736`), M3 `!is_copy@4809` + borrowck `@1333` skip it → poison is **vacuous** (O caught it himself before re-tasking). The ONLY verify-reachable escape: `pop<T>(Vector<String>)` generic → non-Copy handle by-value through `emit_shim_call:3095`. **Lesson G nailed down (for the Nth time): "don't design tests by imagination — everything must be checked against the real architecture."** All 3 times D/O caught themselves BEFORE writing code (MIR-dump + reading-the-real-thing + the is_copy layer).
⚖ **D=Sonnet 5 (subagent):** STOPPED per RULE-4 correctly **twice** (owned-String bypass, Vector<String> no-overload), reported the real MIR-dump, did NOT quietly widen scope/loosen assertions. Submitted raw gate on the first try, clean canary (A=1,B=1,C=2). Minor blemish: the `length(&0 s)` fix proposal still missed the is_copy layer (O caught it).

## 🔴 OPEN DEBT
1. Carry-over: `mir_lower.rs` panics instead of Err (~4 panics + 4 unreachable + 5 unwrap + 38 expect, Track B #1 — needs triage reachable-vs-internal; **GROUP A already closed** → [[campaign_front_b_panic_audit_group_a]]) · N1 widening hole · method-call return over-refuse · Deep-Clone · drain · `&0 Enum` consumption · borrow-params `&+ T` (Tier C slice 2). **ALREADY CLOSED:** Threat-2 push_owned-vs-M3 (architecturally impossible, section above) · container-element `Nullable(Struct-heap)` refuse §15.6 → [[campaign_spof_container_nullable]] (WO-SPOF-1) · `LowerError` error-code system → [[campaign_front_a_lower_error_codes]].

[[campaign_forgot_nullable_sweep]] [[campaign_nullable_position_and_temp_ownership]] [[mentor_o_persona]] [[colleague_d_persona]] [[feedback_poison_must_be_red]] [[feedback_g_report_protocol]]
