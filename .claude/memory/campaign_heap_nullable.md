---
name: campaign_heap_nullable
description: ★ IN PROGRESS 2026-06-18 — Heap-Nullable Campaign (ADR-0062 ptr-sentinel repr). Slice 1 String? DONE + pushed + O re-verified. NEXT = Slice 4 Elvis/match (suspended-court §8). READ before opening a slice.
metadata:
  node_type: memory
  type: project
  originSessionId: heap-nullable-campaign
---

**Heap-Nullable Campaign** — `T?` for T = heap (String/Vector/HashMap). ADR-0062 (`docs/decisions/0062-heap-nullable-ptr-sentinel-repr.md`) LOCKED, signed by O+G (`7fd3937`). Repr (a) **ptr-sentinel**: `T?` uses the SAME slot as `T`; null = a cell with `ptr == NULL_SENTINEL` (`i64::MIN`), `ptr==0` FORBIDDEN; widening = no-op; 0 byte overhead. Scope locked to String/Vector/HashMap; **defer Struct?/Enum?** (multi-word aggregate, no natural ptr cell — needs its own ADR).

## Status (2026-06-18, origin `f73ae25` synced)
- **★ IN PROGRESS — Slice 4.5 (debt: 3839 method→String?) — G APPROVED WO, D typing.** 3839 is NO LONGER latent: probing a trait method `function get(self)->String?` → **JIT panic `mir_lower.rs:1656 index out of bounds len 0 index 0`** (callee_ret=Nullable(String) falls into `scalar_ret` 3850 matching Nullable(_) → treated as i64 → reads args[0] before sret is inserted → explodes). Violates Track B rule #1 (compiler panics on valid user code). **Fix (narrow scope, only Expr::MethodCall lib.rs:3839):** `is_fat_ret += callee_ret.is_string_repr()` (mirroring 2239); narrow `scalar_ret` to exclude `Nullable(_)` → use `is_scalar_nullable_payload` (triet-mir, FORBIDDEN to invent a new predicate); sret layout-name "String". Does NOT touch Vector/HashMap/`?+>`. Teeth: remove is_string_repr → panic 1656 returns · §8 sentinel → leak -9...808 · M1 → N7 count 2. O verifies with blood on 3 poisons before signing.
- **Slice 4 `String?` Elvis/match CLOSED + PUSHED** (`bee649b`+`f73ae25`, origin synced): **0 production code** — the mechanism was already sufficient on HEAD (type-driven null-check `is_nullable()`; `load_place` reduces String?-local to slot@0=ptr so `obj==NULL_SENTINEL` already compares ptr@0 NOT the whole slot; present-arm move + M1 tombstone already existed). 5 fixtures 192-196 + N7 `string_nullable_match_move_counting`. ★ §8 gate + double-free §8 — both poisons explicitly went red (O planted them personally, G signed). **Recon OVERTURNED the Work Order's premise** ("comparing the whole slot is wrong, projection is needed" = moot); O acknowledged a wrong-premise guess (rule #4). **O personally overturned the defer-double-free ruling** (probe had the wrong shape: `match f()` temp vs `let x=f()` named scrutinee) → double-free reachable in Slice 4, locked down with N7 counting.
- **Slice 1 `String?` DONE + PUSHED** (`3179427`): `is_string_repr()` SSOT (triet-mir:614 = String | Nullable(String)); gate `find_refused_nullable(allow)` position-aware (return+local for String? pass through; struct-field/enum-payload STILL refused — fixture 180); lower `is_fat_return`/`is_fat_ret` + sret layout-name "String"; JIT slot-alloc/param-load/Drop keyed off is_string_repr; `~0` Const writes NULL_SENTINEL into slot@0 (mir_lower.rs:1189). Fixtures 189/190/191 + N7 `string_nullable_drop_counting`.
- **Runtime foundation §4 ALREADY IN PLACE:** every free-shim is a no-op on `ptr==NULL_SENTINEL` (string 4039, vector 2485, hashmap 2708; get-OOB/key-miss RETURN SENTINEL = producer null). → Slice 3 conditional-Drop is mostly confirmation+teeth.

## O re-verifies Slice 1 (independent poison, verified with blood) — 2/3 teeth real
- ✅ **Drop reads ptr@0**: poison `stack_load @0→@8` (mir_lower.rs:1420) → `null` test FAILED (len-garbage@8 of null gets counted by free). Load-bearing.
- ✅ **Borrowck move**: fixture 191 use-after-move → E2420 refused.
- ❌ **`~0` writing SENTINEL into ptr@0 = VACUOUS**: poison store `@0→@8` (1189, leaving ptr@0=0 fresh) → BOTH tests stayed GREEN. `free` is a no-op on both `0` and `SENTINEL` so null-count==0 CANNOT distinguish "SENTINEL@0" from "0@0". This is exactly the risk flagged in **ADR-0062 §8 sentinel-vs-zero** — Slice 1 has NOT yet paid this off.
  - Classification (lesson from ADR-0058 cap@24): **(a) unobservable at Slice 1** (no consumer branch yet on ptr@0==SENTINEL; Drop is a no-op either way). Production code is CORRECT (writes to @0). NO rework needed for Slice 1.

## ✅ SUSPENDED §8 PAID OFF (Slice 4 acceptance gate — PASSED)
Fixture 193 `match (~0:String?){~+ s=>len; ~0=>7}` → 7; poison sentinel-store 1189 `@0→@8` → 193 returns -9...808 (195 elvis-null too), non-null 192/194/196 unaffected → **RED**. O verified with blood, matched pre-verify. Gate closed.

## ⚔ DOUBLE-FREE §8 — REACHABLE + LOCKED AT SLICE 4 (NOT deferred to Slice 5 — O personally overturned the ruling)
**Ruling history (traceable correction):** O initially ruled "double-free vacuous, defer to Slice 5" based on a probe using `match f()` (temp scrutinee → MIR only Drops(arm_local) → 1 free). O PERSONALLY OVERTURNED this after examining real fixtures using `let x=f(); match x` (named scrutinee): drop-elaboration emits `Drop` for BOTH the arm-local `s` AND the scrutinee `x` at the merge → **two Drops on the same ptr, a REAL double-free at Slice 4**. The M1 zeroing-on-move tombstone (mir_lower.rs:1322, writes 0 into slot@0 after `s=move x`) saves it → Drop(x) becomes a no-op → 1 free. **D got called out by O for a "half-hearted rebuttal"**: D held MIR bb2 Drop(_0)+bb4 Drop(_5), saw the real SIGABRT, and still conceded "not String?-specific mechanism → defer." **Lesson: do NOT concede a tooth with blood on it just because the mentor waves it off; argue from the MIR.** Tooth lock = N7 counting `present_arm_move_out_freed_once` (count==1; poison M1 @0→@8 → count==2 → RED, not relying on lucky crashes). ADR §8.1 amendment records the tombstone-0 coupling ⟂ borrowck. Slice 5 (`?+>`) = a DIFFERENT double-free path (payload escapes into a map-fn).

## ✅ Latent 3839 CLOSED — Slice 4.5 (commit `f19f2a0`, SIGNED by O, awaiting G finalize+push)
**O verified with blood on 5 fronts (committed tree):** baseline 197→2/198→9 + 2 N7 green · §8 poison 1189→198 -9...808 · M1 poison 1322→both N7 count2 · **D honestly flagged "panic needs BOTH bugs" → O VERIFIED CORRECT:** poisoning is_fat_ret alone → clean refuse (no panic); poisoning is_fat_ret + restoring the blanket Nullable(_) → panic 1656 comes back. scalar_ret-narrow is genuine defense-in-depth. Tree reverted clean, build/clippy 0.

A trait method returning `String?` used to **panic in JIT `mir_lower.rs:1656 index OOB len 0`** (Track B rule #1 violation): Expr::MethodCall lowering (lib.rs:3839) for `Nullable(String)` falls into `scalar_ret` (blanket `Nullable(_)`) + missing is_fat_ret → callee sret is void, caller reads an empty inst_results[0] → explodes. **Fix (mirroring Expr::Call, scoped to String?):** is_fat_ret += `is_string_repr()` · scalar_ret drops `Nullable(_)` → `Nullable(inner) if is_scalar_nullable_payload(inner)` (expose the predicate from triet-mir, do NOT create a new one) · sret_layout_name "String". Fixtures 197/198 + N7 `method_return_present_arm_freed_once`. **3 poisons went RED:** is_fat_ret → refuse · §8 1189 → -9...808 · M1 1322 → count2. **Honest finding:** poisoning is_fat_ret ALONE → refuse (NO panic) because scalar_ret had already narrowed; the original panic 1656 needs BOTH bugs. Vector?/HashMap?/Struct?/Enum? return STILL refused (out of scope). **Forgot fmt before commit → pre-commit hook blocked it (Rule 2); fixed with cargo fmt + re-gated.** Gate 0·0·193·0.

## ✅ Slice 5 (?+> Bug B retype) — SIGNED by O, awaiting G finalize+push — commit `436b708`
**O verified with blood:** baseline 211→5/212→5/213→7, 3 N7 (String/Vector/HashMap scalar-in heap-out) freed-once; poison retype (2749 `let _=result_ty`) → all 3 N7 `left:0 right:1` = **LEAK RED** (Bug B is REALLY a leak in scalar-in heap-out, not pure latent — heap output mistyped as scalar → no-Drop → leak). **Blind spot O dug up personally (N7 only covered scalar-input):** heap-in heap-out (`String? ?+> dup`, dup returns the same alloc, input+output alias = double-free risk) → temp counting probe → **freed exactly 1** (the M1 tombstone already locked in covers it, no double-free no leak). 175 pass even under poison (guards the naive-retype). Struct?/Enum? still refused. build/clippy 0, tree reverted clean. G ruling: residual Drop = conservative MIR, N7 count-based verdict → correct.

## (history) Slice 5 (?+> Bug B retype) — commit `436b708`
NullableMap `?+>` allocated the result according to inner_ty (T?) regardless of what the body produces → when T≠U, mistype (heap-in scalar-out: String? holding Integer → Drop=string_free on garbage; latent UB survived by luck because slot@0=0). **1-point fix (Expr::NullableMap lib.rs:2734):** after body_val, retype `result_ty = if body_ty.is_nullable() { body_ty } else { Nullable(Box(body_ty)) }` (flatMap keeps as-is, map wraps). A naive-retype-without-wrap broke 175/Elvis. 3 fixtures 211/212/213 (heap→scalar 5, scalar→heap 5, null-path 7) + N7 `nullable_map_heap_output_counting` (String/Vector/HashMap freed-once). **Poison reverting the retype → all 3 N7 count 0 (leak) RED; 175 still passes under poison (guards the naive-retype).** Gate 0·0·208·0.

## ✅ CFG-tail Slices 1/2 CLOSED (pushed): Bug A block-tail escape `159fd68` (E2421, mirrors If idiom + is_reference guard) · UAF-through-merge ADR-0063 `51e401b` (borrowck Drop-check + dest_used_after point-level read-after-Drop; 1 construct-agnostic point covering If/match; teeth 210 + 84/101 no-false-pos).

## ✅ Slice 4.8 (widen Vector?/HashMap?) — SIGNED by O, awaiting G finalize+push — commit `e54a712`
**O verified with blood (committed tree, BOTH handles VERIFIED SEPARATELY):** baseline 199-206 = 2/99/3/E2420 · 1/99/2/E2420; 2 N7 files 3/3 green. §4 guard poison (vector_free 2491 + hashmap_free 2714 stripping SENTINEL) → null 200/204 SIGSEGV 139 for both. M1 var-zero else-branch poison (1324 store_place→`let _=zero`) → present-move N7 count left:2 right:1 **run SEPARATELY per file**: vector RED + hashmap RED (per G's rule of not-trusting-merged-output if even one of a pair is missing). E2420 202/206 confirmed. Struct?/Enum? STILL refused (scope intact). build/clippy 0, tree reverted clean.

## (history) Slice 4.8 CLOSED — commit `e54a712`
**0-code mechanism (like String?):** match/Elvis/null-check/move/borrowck are all type-driven via is_nullable() → already embrace Nullable(Vector/HashMap). **ONLY 2 production deltas:** (1) gate `is_lowerable_nullable_payload` `matches!(String)`→`t.is_any_heap()`; (2) JIT Drop dispatch unwrap `eff=nullable_payload().unwrap_or(ty)` then is_vec/is_hashmap (do NOT create a new predicate). 8 fixtures 199-206 (Vec+HM × match present/null + Elvis null + E2420) + 2 N7 `vector_nullable_drop_counting`/`hashmap_nullable_drop_counting` (null→0, nonnull→1, present-move→1). **Teeth RED on both handles:** Poison A M1 var-zero else-branch (mir_lower ~1323) → present count 2 (vector+hashmap left:2 right:1); Poison B §4 sentinel-guard (vector_free 2491/hashmap_free 2714) → null fixtures 200/204 free(SENTINEL)→SIGSEGV 139. §8 single-handle has NO offset poison → locked down via drop-count (matches the WO). Struct?/Enum? STILL refused (defer). Gate 0·0·201·0.

## (history) Slice 4.8 WO recon
**O's recon (HEAD `f19f2a0`):** Vector/HashMap = **single-i64 handle**, NOT slot-backed like String → dodges the entire fat/sret/StructAlloc/layout machinery; null-check `handle==NULL_SENTINEL` compares directly on the i64 var; `~0`=iconst into var; free-shim §4 already no-ops on SENTINEL. Annotation syntax: `Vector<Integer>?`/`HashMap<K,V>?` (bare HashMap=Unknown). **ONLY 2-point delta:** (1) gate `triet-mir:1398 is_lowerable_nullable_payload` opened `|| t.is_any_heap()` (struct-field/enum-payload 1499/1512 KEEP scalar-only); (2) JIT Drop dispatch `mir_lower.rs:1405-1415` unwraps Nullable: `let eff=ty.nullable_payload().unwrap_or(ty)` then match is_vec/is_hashmap (G's call: do NOT create is_vec_repr/is_hashmap_repr — unwrap-at-site). Scope locked to String/Vector/HashMap return+local; defer Struct?/Enum?; NOT `?+>`. **Teeth (6, G approved — §8 is a DIFFERENT SHAPE: single-handle has no offset@0/@8 to poison → locked down via drop-counting):** compile+run Vec?/HM? non-null+null · **N7 drop-counting for BOTH Vector AND HashMap** (null→0, non-null→1; per G: missing one of the two throws out the whole batch) · double-free named-scrutinee (M1 else-branch var-zero 1323-1330, DIFFERENT from String's slot@0) · E2420 use-after-move · match `~+/~0`+Elvis (D examined: is the 0-code mechanism inherited from String? infra or broken → patch in-scope) · blind spot for both handles.

## 🔬 DEEP RECON `?+>` heap (2026-06-19, G authorized) — O PERSONALLY CORRECTED black-box recon
**Initial recon was WRONG (O caught it personally):** O reported "Vector? map→E2421, String? runs, asymmetry=infra uneven" → **WRONG on 2 levels**: (1) grep pattern `storage` matched `StorageLive` in the MIR dump → made it look like E2421 was everywhere (actually running correctly); (2) confounded block-init with direct-init. **Truth (full-output, clean Write-tool file, not printf-fish-glob):** `?+>` map/flatMap heap **RUNS** for String?(→2)/Vector?(→1)/HashMap?(→1) alike, via function/direct binding.
**Bug A — block-init E2421 (ORTHOGONAL, pre-existing):** `let v: Vector<Integer> = { let mutable t=...; t }` → premature `Drop(_2)` then uses `_2==SENTINEL` → E2421. **Breaks even plain non-nullable Vector** (A2 proves it) → NOT related to heap-nullable, NOT related to `?+>` — belongs to the CFG Tail-Expression/block-tail drop-ordering family. Out of scope for Slice 5.
**Bug B — NullableMap result mistyped (genuine `?+>`-specific, LATENT):** lower:2660 `result = alloc_local_ty(inner_ty)` = type T? REGARDLESS of body. typecheck correctly computes U? (E1003 when annotated wrong). Heap-input + scalar-body: result local is heap-typed (String?) holding an Integer. MIR: `_3:String? = move _5(len Integer)` → `Drop(_3)`=string_free reads slot@0. **DOES NOT explode because value→var/register, Drop reads a fresh slot=0→free(0) is a no-op** (value/slot divergence saves it). Fragile, latent, not yet exploited. **Slice 5 fix = type result per body (U?) + double-free teeth for heap-output (ADR §8) + N7 counting.**
**Conclusion:** Slice 5 is SMALLER than thought (mostly works); the real work = fixing the result-type + teeth. Block-init E2421 = a SEPARATE campaign (CFG Tail family).

## ★ Slice 5 RECON (O trial-implemented ×3, clean tree `51e401b`+CFG-tail pushed) — 2026-06-19
**Bug B = NullableMap result mistype, CONFIRMED but LATENT (O honest: could NOT construct an exploit crash).**
- Root: lower:2690 `result = alloc_local_ty(inner_ty)` = T? regardless of what body produces. Mistype shows up when T≠U (heap-in scalar-out: result String? holds an Integer; MIR emits `Drop(_3)`=string_free but returns the correct value, free doesn't explode — works-by-accident because slot@0=0/value-slot divergence, O does NOT fully explain this via MIR grep, doesn't pretend to).
- **Naive fix (retype body_val directly) was WRONG** → broke fixture 175 + Elvis: body `len(s)`=`Integer` (non-nullable), map auto-wraps into Integer?; retype=Integer loses the wrap → Elvis on non-nullable breaks.
- **CORRECT fix (nullable-aware wrap) VERIFIED:** after `body_val=lower_expr(body)`, `result_ty = if body_ty.is_nullable() {body_ty} else {Nullable(body_ty)}`; `c.local_decls[result.0].ty = result_ty` (map wraps U→U?, flatMap keeps U? as-is). **Measured: heap→scalar 5, scalar→heap 5, null-path heap-output→7 (sentinel materialize slot OK), 205/205 + workspace 0 FAILED.** Reverted (recon).
- **Residual: heap→scalar still has `Drop(_3)` after retype** — O has NOT fully explained this (Integer? Copy should have no-Drop); needs N7 counting confirmation of benign vs hazard. **= exactly the double-free heap-output teeth G wants.**
**Slice 5 WO (2 parts) — G LOCKED direction (a), WO handed to D:** (1) nullable-aware retype at lower:2734; (2) N7 counting heap-output map freed-EXACTLY-once (answers the residual + ADR §8 double-free teeth). G ruling: residual Drop(_3) = MIR emits it conservatively, later JIT/pass is a no-op; N7 count-based verdict is the ultimate weapon, only the number tells the truth. Bug B is latent (O honest: no exploit crash) but the type is a monstrosity (String? holding an Integer) — fix it to remove the fragility before future codegen turns it into a UAF. **Poison must be mandatory: break Drop/routing → N7 jumps to 0 (leak) or 2 (double-free).**

## Remaining roadmap (ADR §7)
Slice 4 ✅ pushed · Slice 4.5 ✅ (`f19f2a0` pushed) · **Slice 4.8 widen Vec?/HM? ⟵ IN PROGRESS** · Slice 5 `?+>` map/flatMap heap + Deinit/tombstone (defer, large) · remove gate `HeapNullableNotLowered` + clean up `find_refused_nullable`/`is_scalar_nullable_payload`.

[[mentor_o_persona]] [[colleague_d_persona]] [[campaign_cfg_tail_expression_kickoff]]
