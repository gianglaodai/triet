# TODO

Sub-task tracking — short-term work in progress.

- Long-term phasing: [`ROADMAP.md`](ROADMAP.md)
- Architectural decisions: [`docs/decisions/`](docs/decisions/)
- Language semantics: [`SPEC.md`](SPEC.md), [`VISION.md`](VISION.md)

This file tracks the **current phase** only. When a phase finishes, its summary archives to `ROADMAP.md` and detailed checkboxes are deleted from here.

---

## v0.2 — v0.6 archived

All shipped phases now live in [`ROADMAP.md`](ROADMAP.md):

| Phase | ADRs | Final test count |
|---|---|---|
| v0.2.x Module system | 0005, 0006 | 700+ |
| v0.3 Bytecode VM + Stable IR | 0007, 0008 | 835 |
| v0.3.x.cleanup | 0009 | 835 |
| v0.3.x.ternary | 0010 | 838 |
| v0.4 Crate-Pack + Stable ABI | 0011, 0012, 0013 | 867 |
| v0.5 CAS Packaging | 0014, 0015 | 918 |
| v0.5.x.review | 0015 Addendum | 924 |
| v0.6 Capability System | 0016, 0017, 0018 | 1079 |
| v0.6.x.review | 0018 Addendum | 1085 |

---

## v0.7 — Self-hosting Compiler 🔄 in progress

**Quyết định kiến trúc:** [ADR-0019](docs/decisions/0019-self-hosting-compiler-bootstrap.md) (bootstrap + Rust-shim stdlib), [ADR-0020](docs/decisions/0020-outcome-error-handling.md) (Outcome error handling). See ROADMAP.md §v0.7 for deliverables + gates (recalibrated per ADR-0019 §7 — perf parity defers to v0.9).

### Shipped (9 commits, 1085 → 1129 tests)

| Sub-task | Description | Commit |
|---|---|---|
| v0.7.1 | ADR-0019 framework + ROADMAP §v0.7 recalibrate + ADR index update | `277ee7f` |
| v0.7.2 | Canonical emission invariants audit + `triet-bootstrap` skeleton + 3 determinism tests | `cf7eaf4` |
| v0.7.3.1 | `TypeTag::Vector`/`HashMap` + `.triv` wire v3 → v4 + ADR-0019 Addendum §A1-A4 | `5da6234` |
| v0.7.3.2 | Vector builtins (4 ops, wire IDs 8-11) — VM dispatch + 5 tests | `472cc65` |
| v0.7.3.3 | HashMap builtins (5 ops, wire IDs 12-16) + error-model 3-tier lock + 7 tests | `77e5acf` |
| v0.7.3.4 | IO/path/string builtins (10 ops, wire IDs 17-26) — closes v0.7.3 umbrella; 12 tests | `f304e87` |
| v0.7.4.1 | Generic function syntax — parser + AST + typecheck + lowerer (type-erased per Q3-A deviation §A7.1) | `96c92ef` |
| v0.7.4.2 | Stdlib `.tri` stubs (5 new files, Java-aesthetic) + `path_to_builtin` (19 entries) + 5 VM tests | `f6d722f` |
| v0.7.4.3-error (docs) | ADR-0020 Outcome error handling design (10 §s including null/~0 unification) + ADR-0001/0010 Addendums + SPEC §1.5.3/§2.5 updates | `9f8dca6` |
| v0.7.x.audit.1 | CRITICAL fixes (CLAUDE state + ADR-0007/0008 cross-refs) | `46dd59a` |
| v0.7.x.audit.2 | MAJOR fixes (README + TODO + anchors) | `0b2d336` |
| v0.7.x.audit.3 | MINOR fixes (SPEC null→~0 + CLAUDE outcome syntax + audit memory) | `06eff56` |
| v0.7.4.3-error.1 | Lexer + AST + Parser for Outcome syntax (compound tokens, `\|capture\|`, productions) | `c0fe111` |
| v0.7.4.3-error.2 | Typecheck Outcome support — `Type::Outcome` + 9 errors (E1024-E1032) + W2001 NullDeprecated | `d8e5b07` |
| v0.7.4.3-error.3a | IR data plane — `TypeTag::Outcome` + 6 opcodes (0xC1-0xC6) + `.triv` v5 + VM dispatch + E2210 | `f9d1f91` |
| v0.7.4.3-error.3b | Lowerer — AST Outcome → IR opcodes + pattern matching + 10 e2e VM tests | `d03aa66` |
| v0.7.4.3-error.3c | ADR-0021 `Trilean!` refinement + SPEC §7.1.1 fix + ADR-0010 Addendum §C | `f4fa78e` |
| v0.7.4.3-error.3d | `Trilean!` refinement type + E1033 `PossiblyUnknownCondition` + E1034 `TrileanReturnNotRefined` + 15 typecheck tests | `c3eb126` |
| v0.7.4.3-error.3e | Migrate corpus `if (trilean == lit)` → `match` (alu/memory/utils/print) | `6e4db80` |
| v0.7.4.3-error.4a | `dao fmt --migrate-null` tool (lexer-based, idempotent, dry-run-by-default + `--write`) | `e49d389` |
| v0.7.4.3-error.4b | Apply migration to 6 stdlib stubs (`examples/nullable.tri` deferred pending outcome-null runtime unification) | `be7532d` |
| v0.7.4.3-error.6a | Outcome-null runtime unification — ADR-0010 Addendum §D + lowerer + 4 cross-tolerant VM opcodes + 6 tests | `ffcf6de` |
| v0.7.4.3-error.6b | Interpreter parity for `~0` + migrate `examples/nullable.tri` (closes `.4b` deferred) | `a48c275` |
| v0.7.4.3-fix (struct-fields) | Wire `StructDef` field order into lowerer — kills `field_name_to_idx` placeholder | `0d4577e` |
| v0.7.4.3-error.5a | Capstone demo (`demos/05-error-handling/` — 4 `.tri` files + README) | `c139a89` |
| v0.7.4.3-error.5b | Capstone integration tests (4 tests in `error_handling_demo.rs`) | `b5b2abc` |

### Closing summary (`v0.7.4.3-error` umbrella)

`v0.7.4.3-error` introduced Outcome error handling (ADR-0020), Trilean! refinement (ADR-0021), and the outcome-null runtime unification (ADR-0010 Addendum §D). The `.5` capstone demo proves all locked features end-to-end through the VM tier. 1221 workspace tests pass.

---

## v0.7.4.3-debt — Lowerer + typecheck cleanup before lexer port (in progress)

Pre-port audit surfaced **7 workaround sites** in the draft `compiler/lexer.tri`. Author opted (2026-05-19) for the no-tech-debt path: fix the underlying compiler bugs FIRST, then commit `lexer.tri` without workarounds. Rationale per [`feedback_stability_over_speed.md`][stab] + the explicit "tránh tăng thêm nợ kỹ thuật" directive.

[stab]: ../home/.claude/projects/-mnt-M2-STORAGE-Work-workspace-gh-rust-triet/memory/feedback_stability_over_speed.md

The draft `compiler/lexer.tri` + `crates/triet-bootstrap/tests/lexer_self_smoke.rs` stay in the working tree (uncommitted) as a regression gate — each debt fix must keep the smoke test passing.

### Debt sub-tasks

- [x] **v0.7.4.3-debt.1** — Trilean! parser support (WA-3 + WA-4) — `123ffa7`
  - Parse `Trilean!` in type-annotation position as `Type::Trilean { refined: true }`
  - Auto-fix WA-4 (refinement preservation through `&&` once helpers can declare `-> Trilean!`)
- [x] **v0.7.4.3-debt.2** — Field access alphabetical bug (WA-2) — (this commit)
  - Lowerer now tracks `value_outcome_value_struct` / `func_return_outcome_value_struct` parallel to the existing struct-typing maps. Propagation lands in `lower_outcome_propagate` (after `OutcomeUnwrapValue` + Phi), `lower_outcome_default` (after success-arm `OutcomeUnwrapValue` + same-struct Phi), and `bind_pattern_vars` for `Pattern::OutcomeArm(Positive)`. 2 integration tests in `triet-bootstrap`.
- [x] **v0.7.4.3-debt.3** — E1025 false positive (WA-5) — (this commit)
  - `Checker::expected_type_stack: Vec<Type>` consulted before `current_return_type` in `check_outcome_constructor_context`. `check_initializer` pushes the let-binding annotation while inferring the value; `with_expected` RAII helper handles push/pop. Local site wins over surrounding return type, so `let x: T? = ~0` inside a `T~E` function is accepted while a bare `return ~0` from the same function still raises E1025. 5 integration tests cover positive + negative paths.
- [x] **v0.7.4.3-debt.4** — Generic chain inference (WA-7) — (this commit)
  - `extract_type_params` switched from `or_insert_with` to a prefer-concrete rule: when the existing `sub_map[T]` is itself a `TypeParam` (poisoned by an upstream un-inferrable generic like `new<T>()`), a subsequent concrete arg replaces it. First-concrete-wins still holds for `f<T>(a: T, b: T)` mismatched calls. 5 integration tests cover the lexer-port `push(new(), x)` chain, nested chains, mismatch detection, and the bare `new()` alone case.
- [x] **v0.7.4.3-debt.5** — Match-arm mutation phi + bare-variant pattern dispatch (WA-1) — (this commit)
  - Two interacting bugs found and fixed together. (a) `lower_match_expr` now collects mutated vars across all arms, pre-snapshots, post-snapshots per arm, and emits one phi per mutable at the merge block — mirroring `lower_if_expr`. (b) `lower_pattern_test` + `bind_pattern_vars` rewrite `Pattern::Variable(name)` to an `EnumVariant` tag check when `name` resolves to a known unit variant. Pre-fix the parser bug was latently masked by the static-last-write semantics of bug (a); fixing (a) exposed it.
  - 5 integration tests in `match_arm_mutation_phi.rs`: while+match+push, bare-unit-variant dispatch, mutation-observable-after-match, wildcard-with-mutations, and a `lex_decimal_integer` reproducer.
- [x] **v0.7.4.3-debt.6** — Rewrite `compiler/lexer.tri` + commit lexer port (this commit)
  - Removed all 7 workarounds: helpers now return `Trilean!` (with plain `if` at call sites), `NumericSuffix?` replaces the explicit `NoSuffix` sentinel, `OneToken` declared in natural order, mode-dispatch restored to canonical `match top { NormalMode => …, FStringMode => …, InterpolationMode(state) => … }`. Generic `push(new(), x)` chain reads cleanly. ~1090 LOC Triết.
  - Also fixed two additional gaps surfaced during rewrite: struct-literal field positions need the expected-type push (mirrors `.debt.3`'s let-binding logic); and `OutcomeDiscriminant`/`OutcomeUnwrapValue` now cross-tolerate bare `T` values flowing through a `T?` slot (closes WA-6 — the previously-deferred lowerer cross-tolerance for match-arm dispatch beyond the 4 opcodes proven in `ffcf6de`).
  - Bootstrap regression gate (`lexer_self_smoke.rs`) green; 1247 workspace tests pass.

- [x] **v0.7.4.3-debt.7** — EnumTag Integer variant index (parser.tri unblocker) — `730fddc`
  - `EnumTag` opcode: output changed from `Trit(Positive | Negative)` to `Integer(variant_index)`. Pattern::EnumVariant + Variable-as-variant now compare `Eq(tag, Integer(idx))` instead of `Eq(tag, Trit(idx==0?Positive:Negative))`. Pre-fix any enum with 3+ variants collapsed variant 1,2,3,... into indistinguishable Negative; post-fix all variants dispatch correctly. 1247 tests pass; 4-variant enum reproducer `E { A, B, C, D }` now produces `A→1 B→2 C→3 D→4`.

### Deferred (not in debt umbrella)

_None — all 7 workarounds resolved. WA-6 deferral moved to .debt.6 since it surfaced together with struct-field expected-type extension and was cheap to fix in the same commit._

### After v0.7.4.3-debt: remaining v0.7 sub-tasks

- [x] **v0.7.4.4** — `lexer_differential` NDJSON byte-diff test + verify gate (closes v0.7.4 umbrella) — `e1535fd`
  - New `crates/triet-bootstrap/tests/lexer_differential.rs` (20 tests). Adds `dump_ndjson(source: String) -> String` to `compiler/lexer.tri` (NDJSON bridge format per [ADR-0019 §A2]: `{"t":<Kind>,"s":[start,end][,"v":...][,"u":...]}` per token; `{"e":...}` on error). Rust-side mirror converts byte spans → char spans via `byte_to_char_index` so real `examples/*.tri` files with UTF-8 comments (box-drawing) participate in the corpus. Corpus covers keywords, operators (single + compound + outcome), nullable/force-unwrap, ternary + decimal + suffixed integer literals, string/f-string/escape handling, line comments, question-modified keywords (`if?`/`while?`), realistic function signatures, and three example files (factorial / maybe / nullable). 1267 workspace tests pass.
  - **Lowerer fix surfaced by this gate**: `lower_while_loop` now uses `rebind_var` (instead of `bind_var`) when binding the loop-header phi-dest into the live scope. Without this, an `Expr::Block` scope wrapping a match-arm body (the parser wraps `~+ x => { … }` arm bodies as `Expr::Block`) would `pop_scope` and drop the phi-dest mapping before `lower_match_expr`'s post-arm `resolve_var` snapshot, so any variable mutated through a `while` inside a match arm reverted to its pre-match value after the match. The body-scope shadow at line ~1846 still keeps in-body reads/writes pointing at the phi-dest.
  - **VM fix surfaced by this gate**: `NullCheck` no longer classifies `RuntimeValue::Enum { payload: None, .. }` as the null state. The legacy "any payload-less Enum is null" arm collided with bare unit-variant enums (e.g. `LetKw`) flowing through a `T?` slot via the ADR-0010 Addendum §D cross-tolerance — `keyword_for(slice) ?: Identifier(…)` then mis-classified every keyword as null and produced `Identifier` for `let`/`while`/etc. The two canonical null carriers remain `RuntimeValue::Null` and `Outcome { discriminator: Trit::Zero, payload: None }`.
  - **Lexer-port gap surfaced by this gate**: `finish_ident` now peeks past the identifier slice and absorbs a trailing `?` for the `if?` / `while?` compound keywords, mirroring the Rust impl's `#[token("if?")]` / `#[token("while?")]` longest-match.

### v0.7.4 umbrella closed

The 4-sub-commit umbrella from [ADR-0019 §A7.4] is now done end-to-end: v0.7.4.1 generic syntax → v0.7.4.2 stdlib stubs → v0.7.4.3 lexer port + `-debt.{1..7}` cleanup → v0.7.4.4 differential gate. Triết-side and Rust-side lexers agree byte-for-byte on the corpus.

[ADR-0019 §A2]: docs/decisions/0019-self-hosting-compiler-bootstrap.md
[ADR-0019 §A7.4]: docs/decisions/0019-self-hosting-compiler-bootstrap.md

---

## v0.7.5 — `compiler/parser.tri` port (in progress)

Per ADR-0019 §A7.5: port crates/triet-parser/ (~6027 LOC across 9 files) to a Triết-native `compiler/parser.tri`, mirroring crates/triet-syntax/'s arena pattern. Author opted (2026-05-21) for the split-umbrella approach: each sub-task ships an incremental AST + parser slice with its own integration test, rather than a single 7000-LOC commit. Rationale per `feedback_stability_over_speed.md` — debug surface co lại, per-sub-task verify gate matches v0.3 cadence.

### Sub-tasks

- [x] **v0.7.5.1** — AST + Arena scaffolding (this commit)
  - New `compiler/parser.tri` (~340 LOC). Mirrors Rust `Arena` with four `Vector<Spanned*>` sub-arenas keyed by Integer index. Ships minimal `Expr` / `Pattern` / `TypeExpr` / `Stmt` surfaces (just the variants needed to prove the recursive lookup pattern); subsequent .5.N sub-tasks expand each enum as parser features land. `AllocResult` wraps `(arena, id)` because IR tuple returns are deferred post-v1.0. Smoke `main()` constructs the AST for `1 + 2 * 3`, asserts arena counts + recursive `format_expr` traversal, and exercises all four sub-arenas.
  - **Two pre-existing lowerer/VM bugs surfaced + fixed under this sub-task:**
    - `lowerer.rs`: `bind_pattern_vars` for `Pattern::EnumVariant` never propagated payload-struct identity onto the SSA value bound by the match, so `match e { Variant(p) => p.field }` always read slot 0. Pass 1a.2 now populates `variant_payload_struct` (variant_name → struct_name) which the bind site consults — parallel to the OutcomeArm path covered by debt.2.
    - `vm.rs`: `NullUnwrap` retained a legacy `Enum { variant: 0, payload: Some(p) } → unwrap` arm that was the inverse of the now-unused `NullWrap` emit. Under ADR-0010 Addendum §D unified encoding, `T?` flows as the bare value (or `Null`); the legacy arm only ever hit user enums whose variant-0 carried a payload, so `Vector<Node>.get(...)!!` returned `Integer(10)` instead of `Leaf(10)`. Symmetric to the v0.7.4.4 NullCheck cleanup.
  - 4 integration tests across `parser_arena_smoke.rs` (1) + `struct_field_through_enum_variant.rs` (3) cover the smoke + both bug fixes. 1271 workspace tests pass.

- [x] **v0.7.5.2** — Pratt expressions + atoms (this commit)
  - `compiler/parser.tri` (+1245 LOC, ~1585 total) — `module lexer;` + `from crate.lexer import …` wires the Token stream from `compiler/lexer.tri`. Adds `ParserState` (tokens + cursor + arena) threaded functionally through every parse helper; `ParseStep~ParseError` is the outcome wrapper (no tuple returns yet per SPEC §95). Expr enum grows from 8 to 17 variants — adds `ForceUnwrapExpr`, `CallExpr`, `FieldAccessExpr`, `MethodCallExpr`, `OutcomeConstructorExpr` (+ `OutcomeArm` enum), `OutcomePropagateExpr`, `OutcomeDefaultExpr`, `ElvisOpExpr`, `RangeExpr`. Pratt covers: 20-variant `BinaryOperator` ladder (precedence + assoc per SPEC §12.1), unary `Negate` (3 surface forms: `-x` / `!x` / `not x`), parenthesised grouping, postfix `!!` / `(args)` / `.field` / `.method(args)` / `~?` / `~:`, plus Elvis `?:` and range `..` / `..=`. Smoke `main()` asserts 17 source snippets parse to their expected s-expr shape. Block / If / Match / Lambda / Tuple / FString / StructLiteral / SafeAccess defer to v0.7.5.3+.
  - **Two pre-existing bugs surfaced + fixed under this gate:**
    - `crates/triet-typecheck/src/check_resolved.rs`: Pass 1 collected each module's declared types in isolation, so any field whose annotation referenced a user-defined type from *another* module fell through to `Type::Unknown`. Pass 2 imports then carried Unknown field types, breaking `match spanned.token { Variant(payload) => ... }` (the `bind_pattern` `UserEnum` guard fails on Unknown → payload binding never enters scope → E1002 on every reference). Pass 1 now iterates to a fixed point with a cross-module name table, so user-type references resolve into their full `UserStruct` / `UserEnum` shapes before Pass 2 runs. Two regression tests in `check_resolved.rs` (cross-module struct-field-match + nested struct field access).
    - `crates/triet-ir/src/lowerer.rs`: With (1) fixed, parser.tri parsed cleanly through typecheck but blew up at the VM on chained field access like `step.state.arena` — the intermediate `step.state` SSA value had no `value_struct_types` entry, so the next `.arena` slot resolution fell back to slot 0 and triggered E2201 "expected Unit, got non-struct". Pass 1a now records `struct_field_types: (struct, field) → struct-name-if-field-is-a-named-struct`, and `Expr::FieldAccess` propagates that identity onto the `FieldGet` dest so chained accesses keep tracking through every link. Parallel to the [v0.7.4.3-debt.2] `value_outcome_value_struct` propagation chain.
  - 6 integration tests across `parser_expr_smoke.rs` (1, lex+parse end-to-end via the VM smoke) + `cross_module_field_resolution.rs` (3, covers single-module, nested chained access, and outcome-unwrapped chained access) + 2 typecheck unit tests pinning the cross-module resolution. 1277 workspace tests pass; `cargo clippy --workspace --all-targets` clean.
- [x] **v0.7.5.3** — Statements + bindings (this commit)
  - `compiler/parser.tri` (+1001 LOC, ~2465 total) — ports `crates/triet-parser/src/stmt.rs` (~540 Rust LOC). Adds the `Block` struct (`block_statements: Vector<Integer>` + optional `block_final: Integer?`), grows `Stmt` from one variant (`ExpressionStmt`) to ten — `LetStmt` / `ConstantStmt` / `ReturnStmt` / `BreakStmt` / `ContinueStmt` / `ForStmt` / `WhileStmt` / `LoopStmt` / `AssignStmt` / `ExpressionStmt` — each multi-field variant carrying its own `*Payload` struct because Triết enum variants take a single payload. `Pattern` grows from `WildcardPattern` only to `WildcardPattern | IdentifierPattern(StringPayload)` — the minimum needed for `let name = …` and `for i in …` / `for _ in …`. New step wrappers (`ParseStmtStep`, `ParsePatternStep`, `ParseBlockStep`, `BlockElement` + `BlockElementStep`, `EatFlagStep`) extend the v0.7.5.2 `ParseStep` pattern; the `BlockElement` two-arm enum tells `parse_block` whether the next element is a finished statement or the block's trailing final-expression. Token plumbing: `is_semi` / `is_rbrace` / `is_assign_token` / `is_comma` / `is_mutable_kw` / `is_while_q_kw` / `is_value_terminator` predicates, `eat_semi` / `eat_mutable`, and `expect_assign` / `expect_lbrace` / `expect_rbrace` / `expect_in_kw` / `expect_colon` consume-or-error helpers. `parse_optional_type_annotation` + `parse_type_minimal` ship a single-identifier `NamedType` parser to fill the `let`/`constant` annotation slot — full type grammar lands in v0.7.5.5. The `parse_block` driver is a `mutable` while-loop over `parse_statement_or_final_expr`; the statement-vs-final-expression dispatch follows the Rust impl (assignment when `Expr::Identifier =`, expr-stmt when `;`, final-expression when `}` / EOF, expr-stmt fallback otherwise). Smoke `main()` asserts 20 new block snippets covering every Stmt variant + the empty / final-only / trailing-final paths.
  - One Triết-side gotcha surfaced + recorded: **`mutable` is a reserved keyword**, so the `LetStmtPayload.mutable` field had to rename to `is_mutable`. Rust uses `mutable: bool` freely because Rust has no `mutable` keyword; Triết's `let mutable` syntax (per ADR-0005, verbose keywords) forces the renaming.
  - 1 new integration test (`parser_stmt_smoke.rs`) loads `compiler/parser.tri` end-to-end (load → typecheck → lower → write `.triv` → round-trip read → run `main()` on VM) — same shape as `parser_arena_smoke.rs` (v0.7.5.1) and `parser_expr_smoke.rs` (v0.7.5.2). 1278 workspace tests pass; `cargo clippy --workspace --all-targets` clean.
- [x] **v0.7.5.4a** — Items: top-level driver + functions + const + typedef + visibility (this commit)
  - `compiler/parser.tri` (+876 LOC, ~3341 total) — ports the upper half of `crates/triet-parser/src/item.rs`. Adds the full `Item` enum (all 8 variants scaffolded so the AST + Arena shape stays stable across the `.4a` → `.4b` split): `FunctionItem(FunctionDef)` / `ConstantItem(ConstantItemPayload)` / `TypeAliasItem(TypeAliasPayload)` / `StructItem(StructDef)` / `EnumItem(EnumDef)` / `ImportItem(ImportPath)` / `ImportFromItem(ImportFromPayload)` / `ModuleItem(ModuleDecl)`. New supporting types: `Visibility` (PrivateVis / PublicVis / PackageVis per ADR-0005), `ParameterPassing` (BorrowedParam / MutableParam / OwnedParam per SPEC §10.3), `FunctionParam`, `FunctionBody` (BlockBody / ExpressionBody), `FunctionDef`, `ConstantItemPayload`, `TypeAliasPayload`, plus stub `StructField` / `StructDef` / `EnumVariant` / `EnumDef` / `ImportPath` / `ImportName` / `ImportFromPayload` / `ModuleContent` / `ModuleDecl` for `.4b`. Arena grows from 4 to 5 sub-arenas with `items: Vector<SpannedItem>` keyed by Integer; `Program { arena, item_ids }` is the new root container.
  - Parser surface: `parse_program` (loops over items till EOF, collects IDs in source order), `parse_item` (visibility prefix + keyword dispatch — struct/enum/module/import stubs surface as `UnexpectedTokenErr` until `.4b`), `parse_visibility` (handles `public` / `public(package)` per ADR-0005), `parse_function` (+ `parse_parameter_list` / `parse_parameter` / `parse_generic_params` / `parse_function_body`), `parse_constant_item`, `parse_type_alias`. Helpers: `parse_ident`, `parse_item_name` (+ ADR-0005 reserved-name check for `std` / `sys` / `dev` / `usr` / `core` via new `ReservedItemNameErr`), 11 new token predicates (`is_function_kw` / `is_constant_kw` / `is_type_kw` / `is_struct_kw` / `is_enum_kw` / `is_module_kw` / `is_import_kw` / `is_from_kw` / `is_public_kw` / `is_lparen` / `is_rparen` / `is_lt` / `is_gt` / `is_thin_arrow` / `is_owned_kw`). Step wrappers: `ParseItemStep`, `ParseProgramStep`, `ParseVisibilityStep`, `ParseIdentStep`, `ParseParamsStep`, `ParseParamStep`, `ParseGenericsStep`, `ParseFunctionBodyStep`. `parse_function_body` consumes either `{ … }` (via v0.7.5.3 `parse_block`) or `= expr` (via v0.7.5.2 `parse_expression`). Smoke `main()` asserts 15 new program snippets covering every supported item shape + every visibility / passing combination.
  - **Six pre-existing lowerer struct-tracking gaps surfaced + fixed under this gate** (all in `crates/triet-ir/src/lowerer.rs`):
    1. **While-loop phi.** `let mutable state: T = …; while … { state = step.state }` — the phi at the loop header didn't inherit pre-loop struct identity, so post-loop `state.field` fell back to `field_idx=0`. Fix: pre-propagate the pre-loop value's struct identity onto `phi_dest` BEFORE the body lowers (the user-declared `let mutable name: T` contract guarantees rebinds preserve T), plus the post-body symmetric check.
    2. **Match-arm mutated-var phi.** `match … { … => state = step.state, _ => {} }` — the per-arm mutated-var phi at the merge didn't propagate struct identity when every arm agreed.
    3. **Match-expression merge phi.** `match … { ~+ … => ~+ X, _ => ~+ X }` — the expression `merge_dest` didn't carry `value_struct_types` or `value_outcome_value_struct` when every arm agreed.
    4. **If-expression merge phi.** `if cond { ~+ X } else { ~+ X }` — same two-incoming merge as match, same gap.
    5. **Outcome constructor literal-side propagation.** `~+ StructValue { … }` didn't seed `value_outcome_value_struct[dest]` from the payload's struct identity, so a subsequent `~?` unwrap dropped the identity. Literal-side analogue of the call-site `func_return_outcome_value_struct` seeding.
    6. **Let-with-type-annotation seeding.** `let p: T = get(v, i)!!` — the `Vector<T>` element extraction is opaque to the lowerer (T-generic), so `p` ended up untracked even though the user wrote the type. Fix: seed `p` from the let's `type_annotation` when the value isn't already tracked.
  - Refactored into a single `shared_struct_identity(tracker, value_ids) -> Option<String>` helper to deduplicate the four shared-identity propagation call sites (while-loop phi, match-arm mutated-var phi, match merge_dest, if merge_dest).
  - **One Triết-side bug surfaced + fixed**: an earlier draft of `parse_function`'s optional-return-type block mixed `ParseLetAnnotation` and `~- err` in two arms of the same `match` — invalid since arms must agree on result type. Restructured to thread `ParseLetAnnotation~ParseError` through a `~?` boundary at the end, mirroring `parse_optional_type_annotation` in v0.7.5.3.
  - 6 new integration tests: `parser_item_smoke.rs` (1, full end-to-end on the new `compiler/parser.tri`) + `phi_struct_tracking.rs` (5, one per lowerer fix — pin each independently of the bootstrap source so future regressions land at the right blame line). 1284 workspace tests pass (was 1278, +6); `cargo clippy --workspace --all-targets` clean.
- [x] **v0.7.5.4b** — Items: struct + enum + module + imports (this commit)
  - `compiler/parser.tri` (+669 LOC, ~4223 total) — ports the lower half of `crates/triet-parser/src/item.rs`. Wires the remaining 5 Item variants .4a scaffolded: `StructItem` / `EnumItem` / `ImportItem` / `ImportFromItem` / `ModuleItem`. Closes the item layer of the umbrella.
  - New parsers: `parse_struct` (+ `parse_struct_field` / `parse_struct_fields`); `parse_enum` (+ `parse_enum_variant` / `parse_enum_variants` — optional tuple-style payload `Some(T)`); `parse_module` — file-bound `module foo` + inline `module foo { items… }` (recursing into `parse_item` via `parse_inline_module_body`, which lets `module data { struct Cell { … } }` work in a single declaration); + ADR-0005 §"File resolution" rejection of `module foo.bar` dotted declarations. `parse_import_item` (`import a.b.c`); `parse_from_import` (`from a.b import x, y as z` per ADR-0005 §"Imports — Python style"); `parse_import_name_list` / `parse_import_name` (+ ADR-0005 glob `*` rejection + optional `as alias`); and the `parse_dot_path` family (root accepts `crate` / `self` / `super` per ADR-0005 §"Path syntax"; subsequent segments must be regular identifiers). Visibility on `import` / `from` is explicitly rejected via `reject_visibility_on_import` (re-exports of imported names are a post-v0.2.x feature).
  - 8 new step wrappers (`ParseStructFieldStep` / `ParseStructFieldsStep` / `ParseEnumVariantStep` / `ParseEnumVariantsStep` / `ParseDotPathStep` / `ParseImportNameStep` / `ParseImportNamesStep` / `ParseModuleContentStep`) + 6 new token predicates (`is_dot` / `is_star` / `is_as_kw` / `is_crate_kw` / `is_self_kw` / `is_super_kw`).
  - Pretty-printer extended for all 5 new Item variants + helper printers `format_struct_fields` / `format_enum_variants` / `format_dot_path` / `format_import_names`. Smoke `main()` adds 15 new assertions covering every shape (incl. generic struct `Box<T>`, generic enum `Option<T>`, dotted-path imports, `as` alias, inline + external modules, `public(package)` on a module).
  - **One Triết-side gotcha recorded**: match arms with a single-statement body need braces — `~0 => should_continue = false,` parses the `=` as a pattern continuation. Wrap as `~0 => { should_continue = false }` instead. Mirrors the v0.7.5.3 `parse_block` pattern.
  - 1284 workspace tests still pass (no new bootstrap test added — the existing `parser_item_smoke.rs` from .4a runs against the same `compiler/parser.tri` and now covers the .4b assertions automatically). `cargo clippy --workspace --all-targets` clean.
- [x] **v0.7.5.5a** — Full type grammar (this commit)
  - `compiler/parser.tri` (+483 LOC, ~4706 total) — expands the v0.7.5.3 placeholder `parse_type_minimal` (single-Identifier `NamedType` only) into the full ADR-0020 + ADR-0021 grammar **in place**. Every existing call site (let / constant / parameter annotation / return type / struct field / enum variant payload / type alias body) inherits the full grammar without touching its signature. New `TypeExpr` variants: `GenericType(GenericTypePayload { name, arguments })`, `TupleType(Vector<Integer>)`, `NullableType(Integer)`, `FunctionType(FunctionTypePayload { parameters, return_type })`, `OutcomeType(OutcomeTypePayload { value_type, error_type, allow_null_state })`, `RefinedTrileanType`.
  - Parser surface restructured as atom + postfix split: `parse_type_atom` handles NamedType (+ optional generic args via `parse_generic_args`) or paren-form (via `parse_paren_type` — disambiguates grouping `(T)`, singleton tuple `(T,)`, multi tuple `(T1, T2 …)`, and function type `(T1, T2) -> R` after-the-fact); `apply_type_postfix` runs ADR-0020 §1.3 precedence — `Trilean!` (only after a bare `Trilean` atom per ADR-0021 §2.7) → `T?~E` (compound `?~`) → `T~E` (bare `~`) → `T?…?` chained nullable via `apply_chained_nullable`. New helper `parse_type_list_until_rparen` returns both the type list AND a trailing-comma flag, letting `parse_paren_type` distinguish `(T)` from `(T,)`.
  - 4 new token predicates: `is_question` / `is_question_tilde` / `is_tilde` / `is_bang`. 1 new step wrapper: `ParseTypeListStep`.
  - `format_type` pretty-printer extended for all 6 new variants + helper formatting for generics, tuples, function-type params, outcome operators. Smoke `main()` adds 16 new type-level assertions covering NamedType, `Trilean!`, single/chained nullable, single/multi/nested generics, tuple (incl. `(T,)` vs `(T)`), function types, and outcome (binary + ternary, with generic value type).
  - 1 new integration test (`parser_type_smoke.rs`) — same end-to-end shape as the v0.7.5.{1,2,3,4} smoke tests. 1285 workspace tests pass (was 1284, +1); `cargo clippy --workspace --all-targets` clean.
- [x] **v0.7.5.5b** — Full pattern grammar (this commit)
  - `compiler/parser.tri` (+~700 LOC) — expands the v0.7.5.3 minimal `Pattern` (Wildcard / Identifier) into the full grammar per `crates/triet-parser/src/pattern.rs`. New AST: `LiteralPattern` sub-enum (Integer / Ternary / String / Trilean) + 4 new payload structs (`LiteralIntegerPattern` / `RangePatternPayload` / `EnumVariantPatternPayload` / `OutcomeArmPatternPayload`). New `Pattern` variants: `LiteralPat`, `TuplePat`, `OrPat`, `RangePat`, `NullPat`, `EnumVariantPat`, `OutcomeArmPat`.
  - Parser surface: `parse_pattern` (top level — handles or-patterns) + `parse_pattern_no_or` (sub-pattern, keeps `|` from binding across structural boundaries — used by tuple elements, enum variant payloads, outcome arm payloads). Atom helpers: `parse_identifier_or_enum_variant_pattern` (Identifier-vs-EnumVariant disambiguated by `(` lookahead), `parse_tuple_pattern` (singleton `(p,)` vs grouping `(p)` distinguished by trailing-comma flag, mirroring the v0.7.5.5a tuple type), `parse_trilean_pattern_atom`, `parse_literal_or_range_pattern`, `parse_negative_literal_pattern` (`-5` / `-9_tryte` shape), `parse_literal_pattern_payload` (shared by start + range-end), `parse_outcome_arm_pattern_with_payload` (the `~+ binding` / `~- binding` cases — `~0` ships inline at parse_pattern_no_or per ADR-0020 §5.2).
  - 6 new token predicates: `is_pipe` / `is_dotdot` / `is_dotdoteq` / `is_underscore` / `is_null_kw` / `is_minus`. 1 new step wrapper: `ParseLiteralPatternStep`.
  - `format_pattern` pretty-printer extended for all 7 new variants + helper `format_literal_pattern` for the literal sub-enum. Smoke `main()` adds 24 new pattern-level assertions covering every variant (incl. negative range bounds, singleton-tuple-vs-grouping distinction, multi-level or-patterns, multi-element variant payload via explicit tuple wrap `Cell((c, d))`).
  - **One Triết grammar reminder recorded**: enum variants take exactly one payload (per syntax SPEC). For tuple-shaped payloads use explicit wrap `Cell((c, d))` — the Rust impl has the same constraint.
  - 1 new integration test (`parser_pattern_smoke.rs`) — same end-to-end shape as v0.7.5.{1..5a}. 1286 workspace tests pass (was 1285, +1); `cargo clippy --workspace --all-targets` clean. Closes v0.7.5.5 — next is `.6` (error recovery + `parser_differential` NDJSON gate, closes the v0.7.5 umbrella).
- [x] **v0.7.5.6a** — Error recovery (this commit)
  - `compiler/parser.tri` (+~130 LOC, ~5963 total) — extends `ParseProgramStep` with `errors: Vector<ParseError>`, changes `parse_program` signature from `~ParseError` outcome to plain `ParseProgramStep` (program is always producible; failed items contribute nothing to `item_ids`). New `synchronize(s: ParserState) -> ParserState` skips tokens until a sync-stop keyword (Function / Let / Constant / Type / If / IfQ / Match / While / WhileQ / For / Loop / Return / Break / Continue / Import / From / Module / Public / Struct / Enum / RBrace) or after `;`. `parse_program` records the error + synchronizes + force-advances when synchronize lands on a sync-stop without consuming anything. Mirrors `triet-parser::parser::synchronize` + `parse_tokens` driver semantics.
  - 3 new smoke assertions cover: broken middle item between two good functions, leading broken item, error at EOF. Each verifies both the error count AND the surviving items' pretty-printed shape — confirming recovery preserves AST integrity for the good items.
  - 1 new integration test (`parser_recovery_smoke.rs`) loads `compiler/parser.tri` end-to-end (same shape as v0.7.5.{1..5b}). 1287 workspace tests pass (was 1286, +1); `cargo clippy --workspace --all-targets` clean.
- [x] **v0.7.5.6b** — `parser_differential` NDJSON byte-diff gate (this commit, closes v0.7.5 umbrella per ADR-0019 §A7.5)
  - `compiler/parser.tri` (+830 LOC, ~6660 total) — adds `dump_program_ndjson(source: String) -> String` recursive AST walker that emits one JSON line per Item / nested node in pre-order. New `crates/triet-bootstrap/tests/parser_differential.rs` (~1100 LOC) is the Rust-side mirror over `triet-syntax`: walks the same pre-order, formats identical JSON, asserts byte-equality. 28-test corpus covers every supported Item / Stmt / Expr / Pattern / TypeExpr variant. Mirrors v0.7.4.4 `lexer_differential` shape; transient bridge per ADR-0019 §A2 (dropped at v0.7.9 when Triết-side data flows in-memory).
  - **Three pre-existing lowerer struct-tracking bugs surfaced + fixed under this gate** (all in `crates/triet-ir/src/lowerer.rs`):
    1. **`Nullable<NamedStruct>` return type not tracked.** `peek(s) -> SpannedToken?` didn't seed `func_return_struct` because `type_expr_to_struct_name` only handles `TypeExpr::Named`, not `Nullable`. Triết represents `T?` as bare `T` at runtime (no wrapper), so the outer value's struct identity IS the inner T. Fix: new `nullable_inner_struct_name` helper + try-then-fall-back in `declare_function`.
    2. **`let p: T? = …` annotation not seeded.** Same shape as v0.7.5.4a fix #6 but for nullable annotations. Fix: extend the Stmt::Let seeding to also try `nullable_inner_struct_name`.
    3. **`~+ pat =>` pattern destructure on `T?` lost struct identity.** `bind_pattern_vars` for `OutcomeArm::Positive` only checked `value_outcome_value_struct` (the `T~E` / `T?~E` case). For `T?` the scrutinee carries struct identity in `value_struct_types` directly (since T? bare-stores T). Fix: fall through to `value_struct_types[scrutinee]` after the outcome check.
    4. **`!!` (NullUnwrap) didn't propagate struct identity.** `Expr::ForceUnwrap` emitted the opcode without carrying tracking onto the dest. Fix: propagate `value_struct_types[inner] → value_struct_types[dest]`.
  - These fixes are why parser smoke tests passed before the differential: the pretty-printer doesn't read `.span_start` / `.span_end` field values, only walks structural children. The differential is the first gate that materializes span data into output — without the fixes, every `next.span_start` access in `parse_prefix` (and similar peek-pattern code) silently returned the Token enum value (field_idx fell back to 0) instead of the Integer span.
  - **One AST-coverage gap recorded**: the Triết-side parser at v0.7.5.6 doesn't yet produce `Expr::Block` / `Expr::Tuple` / `Expr::StructLiteral` / `Expr::EnumLiteral` / `Expr::If` / `Expr::Match` / `Expr::Lambda` / `Expr::SafeFieldAccess` / `Expr::TupleIndex` / `Expr::FStringLiteral`. The Rust parser does. The corpus is curated to avoid these (e.g. `function consume() { … }` not `function consume() = { … }` — the latter folds the block into `Expr::Block` on the Rust side). The Rust mirror's `dump_expr` panics on those variants, with a message pointing at the gap. Closing the gap is a v0.7.7+ concern.
  - **Error recovery is NOT in the differential corpus** — two implementation-level divergences make a byte-diff infeasible without flattening one side's behavior: (a) Rust's mutable-cursor synchronizer advances through partial consumption while Triết's functional `~-` propagation discards intermediate state, producing more errors per source-level mistake; (b) Rust formats the offending token via `Debug` (variant names like "Assign") while Triết uses the user-facing label ("="). Both divergences are concerns about error reporting, not AST shape. `parser_recovery_smoke.rs` pins Triết-side recovery end-to-end.
  - 1315 workspace tests pass (was 1287, +28 = parser_differential corpus); `cargo clippy --workspace --all-targets` clean. v0.7.5 umbrella closed; next is v0.7.6 (`compiler/modules.tri` + `modules_differential` test) per ADR-0019 §A7.6.

### Remaining v0.7 sub-tasks after parser

## v0.7.6 — `compiler/modules.tri` port (in progress)

Mirrors `crates/triet-modules/` (2487 Rust LOC across 7 files). Per
[ADR-0019 §A7.6], ships in 5 sub-tasks: types + LoaderError → loader
(filesystem) → cycle detection → resolver (name resolution) →
`modules_differential` NDJSON gate.

- [x] **v0.7.6.1** — Types + LoaderError scaffolding (this commit)
  - `compiler/modules.tri` (~600 LOC, new) — ports `crates/triet-modules/src/{path.rs, module.rs, error.rs}`. New data types: `ModulePath` (segments + crate_root / child / parent / display / is_reserved_root / is_local_crate per ADR-0005), `AbsolutePath` (module_path + name + display), `ModuleId` / `ArenaId` (Integer newtype wrappers — keeps the loader from cross-feeding indices), `Module` (path + source_path + arena_id + item_ids + bindings + parent + children — `item_ids: Vector<Integer>` mirrors the v0.7.5.4a `Program { arena, item_ids }` shape), `ResolvedProgram` (arenas + modules + root). `LoaderError` enum with 8 variants covering E2100 CyclicImport → E2107 AliasedVariantImport; each multi-field variant wraps its fields in a per-variant `*Payload` struct (Triết enum variants take exactly one payload — mirrors v0.7.5.3+ Stmt pattern). Helpers: `loader_error_code` → stable `triet::modules::EXXXX` strings, `loader_error_span` → `Vector<Integer>` 2-element start/end pair (IR tuple returns defer post-v1.0 per SPEC §95), `loader_error_message` → user-facing `#[error]` template strings for v0.7.6.5 byte-diff.
  - **Design Q1-A (coupling)**: Hybrid — modules.tri declares its own type surface (mirror Rust crate boundary) but `Module.item_ids` references parser's Arena by integer ID. Long-term: `ResolvedProgram.arenas: Vector<crate.parser.Arena>`. Short-term placeholder `Vector<Integer>` because Triết's nested-search-dir loader resolution would require restructuring `compiler/parser.tri` + `compiler/lexer.tri` into a `compiler/parser/` nested layout to let modules.tri declare `module parser;` cleanly. That restructure lands in v0.7.6.2 alongside the actual loader logic (the real consumer of the Arena import).
  - **Design Q2-A (error encoding)**: per-variant payload structs. 8 mini-payload structs (`CyclicImportPayload` / `FileNotFoundPayload` / `ReservedNamespacePayload` / `VisibilityViolationPayload` / `UnresolvedImportPayload` / `ChildParseErrorPayload` / `IoErrorPayload` / `AliasedVariantImportPayload`) + 8 enum variants. Matches the v0.7.5.3 / v0.7.5.5b pattern; verbose but type-safe.
  - **One Triết-side gotcha recorded**: `module` is a reserved keyword (ADR-0005), so field names `module:` and parameter names `module:` parse as keyword-misuse. Renamed `AbsolutePath.module` → `AbsolutePath.module_path`, `ChildParseErrorPayload.module` → `ChildParseErrorPayload.module_path_text`. Dropped trivial `Module`-typed accessor functions (`module_path_of` / `module_arena_id` / `module_item_count`) — direct field access at call sites avoids needing to coin a non-keyword parameter name.
  - 1 new integration test (`modules_types_smoke.rs`) loads `compiler/modules.tri` end-to-end (load → typecheck → lower → write `.triv` → round-trip read → run `main()` on VM) — same shape as `parser_*_smoke.rs`. Triết-side `main()` exercises every constructor + display / code / span / message helper across all variants. 1316 workspace tests pass (was 1315, +1); `cargo clippy --workspace --all-targets` clean.

- [x] **v0.7.6.2** — Loader (filesystem + in-memory) + file layout restructure (this commit)
  - File layout restructure: `compiler/lexer.tri` → `compiler/parser/lexer.tri`, `compiler/parser.tri` → `compiler/parser/parser.tri`. The loader's nested-search-dir resolution (`<parent>/<name>/`) requires parser.tri's `module lexer;` to resolve under `compiler/parser/` when parser.tri is loaded as a child of modules.tri. Standalone loads (v0.7.4/v0.7.5 smoke tests) still work because their root_path becomes `compiler/parser/parser.tri` and that file's sibling lookup finds `compiler/parser/lexer.tri`. Updated 10 test files (lexer_self_smoke, lexer_differential's `compiler_lexer_path`, 7 parser_*_smoke, parser_differential's `compiler_parser_path`) to use the new paths.
  - parser.tri's `from crate.lexer import …` → `from self.lexer import …` per ADR-0005 relative-path support. Now works both standalone (self = crate = parser.tri) and as child of modules.tri (self = crate.parser).
  - `compiler/modules.tri` (~570 LOC added, ~1170 LOC total) — loader logic. `LoaderContext` (functional state — arenas/modules/errors/root_id, threaded through every helper since Triết has no mutable references). `LoadProgramResult { program, errors }` mirrors Rust's `Result<ResolvedProgram, Vec<LoaderError>>` while staying within Triết's value-type model (caller fail-closes on `length(errors) > 0`). New surface: `load_program_from_source(source: String) -> LoadProgramResult` (in-memory), `load_program(root_path: String) -> LoadProgramResult` (filesystem). Internal: `load_from_source` (parse + arena alloc + recurse), `load_inline` (shares parent's arena), `process_items` (separates ModuleItem from other items), `process_module_decl` (routes to `load_inline` or `resolve_external`), `resolve_external` (sibling `<dir>/<name>.tri` then nested `<dir>/<name>/<name>.tri` per ADR-0005), `allocate_module` / `update_module_items` (functional rebuild — Triết's no-mutation model needs full Vector rebuild to patch a single slot), `parse_error_to_loader_error` / `push_parse_errors` (ChildParseError E2105 wrapping for parser errors).
  - `ResolvedProgram.arenas` promotes `Vector<Integer>` placeholder (v0.7.6.1) → `Vector<Arena>` (real parser type). The Hybrid coupling decision (Q1-A) now fully realized.
  - **Stdlib pre-loading deferred to v0.7.6.4 per Q2-A constraint**: Triết has no env-var-read builtin (Rust's `env!("CARGO_MANIFEST_DIR")` is compile-time). Stdlib resolution naturally fits alongside the resolver (which is the actual consumer of `from std.X import Y`) rather than as a loader-side hack.
  - **Triết-side gotchas recorded**: (1) `~+ value` is the Outcome positive-arm CONSTRUCTOR — not a Nullable wrapper. To pass a non-null T into a T?-typed parameter or field, pass the bare value (Triết bare-stores T at runtime — verified via parser.tri precedent). (2) Block comments `/* … */` not supported by Triết lexer — only line comments (`//`). Em-dash inside a block comment lexes the `/` and `*` as bare tokens, then trips on the next character.
  - 1 new integration test (`modules_loader_smoke.rs`) — runs Triết-side `loader_smoke_main()` which exercises 7 corpus cases: empty source, single function, single inline submodule, nested inline submodules, external-in-memory (FileNotFound expected), sibling inline modules, mixed items + modules. 1317 workspace tests pass (was 1316, +1); `cargo clippy --workspace --all-targets` clean.

- [x] **v0.7.6.3** — Cycle detection (DFS over import graph) (this commit)
  - `compiler/modules.tri` (+~340 LOC, ~1740 LOC total) — ports `crates/triet-modules/src/cycle.rs` (~270 Rust LOC). New surface: `detect_cycles(program: ResolvedProgram) -> Vector<LoaderError>` — returns one `CyclicImportErr` (E2100) per cycle found, with a `foo -> bar -> foo` trace per ADR-0005 §"Cyclic imports". Internal: `ImportEdge` struct (target_text + target_segments + span), `DfsState` (colors + cycle_stack + errors, threaded functionally through recursion since Triết has no mutable references), `build_import_graph` (walks each module's retained items for ImportItem/ImportFromItem), `build_path_index` (HashMap<String, ModuleId-raw> via module_path_display), `resolve_edge_target` (direct lookup + parent-drop fallback for `import crate.foo.bar` where `crate.foo` is the module + self-loop guard for `from crate.X import Y` in root), `dfs_visit` (recursive white/gray/black DFS), `build_cycle_trace` (walks cycle_stack from cycle_target's position to top, appends cycle_target to close), `vec_set_at` / `vec_drop_last` (functional vector mutation helpers). Smoke `cycle_smoke_main()` exercises 8 corpus cases: 2-cycle, 3-cycle, diamond (no cycle), stdlib (no cycle), no-imports (no cycle), multiple independent cycles, self-import via parent-drop (no false positive), acyclic chain.
  - Refined `module_path_is_reserved_root` + `module_path_is_local_crate` return types `Trilean` → `Trilean!` per ADR-0021 — bodies are deterministic comparisons that already produce refined Trileans; the previous `Trilean` annotation just lost the refinement at the boundary. Required for `if is_reserved_root(target) { … }` in `build_import_graph` to compile without E1033.
  - **One Triết-side runtime bug surfaced and worked around**: top-level `constant NAME: T = expr` declarations lower silently to `Unit` at runtime. Searching the workspace confirms NO existing `.tri` file uses top-level constants (parser.tri, lexer.tri, examples/, std/ all use local `let` bindings + literals); cycle detection was the first user. Workaround: inline the color sentinels (0/1/2) as Integer literals at every use site, no `constant` decl. Lowerer is missing an `Item::Const` arm — the AST + parser + modules-resolver all carry the variant, but `crates/triet-ir/src/lowerer.rs` skips it. Logged as **v0.7.x.runtime-fix.const-items** below for a future audit pass; not blocking v0.7.6.x.
  - 1 new integration test (`modules_cycle_smoke.rs`) — runs Triết-side `cycle_smoke_main()` against the 8-case corpus. 1318 workspace tests pass (was 1317, +1); `cargo clippy --workspace --all-targets` clean. Verified under `ulimit -v 8388608` (8 GB RAM cap) — finishes in <1 s, confirming the v0.7.6.3 RAM-blowup debugging round (which earlier saw 20 GB+ as the constant-items bug caused infinite recursion) is fully resolved.

- [x] **v0.7.6.4** — Resolver (name resolution + visibility check) (this commit)
  - `compiler/modules.tri` (+~700 LOC, ~2440 LOC total) — ports `crates/triet-modules/src/resolver.rs` (~727 Rust LOC). New public surface: `resolve_names(program) -> ResolveResult { program, errors }`. Two-phase algorithm: (1) bind own definitions (function / struct / enum / constant / type-alias) + child modules into each module's `bindings: HashMap<String, AbsolutePath>`; (2) resolve `import X.Y.Z` and `from X import a, b as c` declarations against the loaded module graph, with `crate.` / `self.` / `super.` path keyword expansion via `resolve_path_keywords`.
  - Visibility check (`is_visible`): `PublicVis` + `PackageVis` visible everywhere (cross-crate pack visibility lands v0.4+); `PrivateVis` only inside the defining module. Wrong-visibility import raises E2103 `VisibilityViolation`.
  - Reserved namespace check (`is_reserved_unusable`): `sys` / `dev` / `usr` import roots raise E2102 — reserved for v0.6 capability namespaces per ADR-0005. `std` / `core` are usable.
  - Enum variant lookup (`find_enum_owning_variant`): `from std.result import Ok` resolves `Ok` to its parent enum `Result` so the variant constructor is in scope. Aliasing a variant import raises E2107 `AliasedVariantImport` per ADR-0005.
  - Triết-side caveats: HashMaps + structs are immutable, so updating `Module.bindings` rebuilds the Module, then `Vector<Module>`, then `ResolvedProgram` (`module_with_bindings` + `program_with_module` helpers). Same functional-rebuild pattern as v0.7.6.2 loader + v0.7.6.3 cycle detector.
  - **Stdlib pre-load remains deferred to v0.7.6.5** — Triết still lacks an env-var-read builtin; the workspace `std/` path can't be discovered from inside the VM. v0.7.6.4 smoke uses a non-stdlib corpus.
  - **Triết-side gotcha recorded**: `_ignored: Integer = …` doesn't parse — Triết's identifier grammar rejects names starting with `_` followed by characters (the bare `_` is wildcard pattern only). Worked around by dropping the unused local binding entirely (the value wasn't needed).
  - 1 new integration test (`modules_resolver_smoke.rs`) — runs `resolver_smoke_main()` against 8 corpus cases: own function bound, child module bound in parent, from-import absolute path, alias binding, visibility violation (private import), unresolved import (nonexistent module), reserved namespace `sys.io`, `self.X` keyword expansion inside submodule. 1319 workspace tests pass (was 1318, +1); `cargo clippy --workspace --all-targets` clean. Verified under `ulimit -v 8388608` — finishes in <1 s.

- [x] **v0.7.6.5** — `modules_differential` NDJSON byte-diff gate (this commit, closes v0.7.6 umbrella per ADR-0019 §A7.6)
  - `compiler/modules.tri` (+~280 LOC, ~2720 LOC total) — adds `dump_resolved_program_ndjson(source: String) -> String` plus helpers: `json_escape`, `quote_string`, `json_int`, `json_parent_path`, `is_stdlib_module`, `dump_module_line`, `dump_error_line`, `count_user_modules`, `sort_string_vector` (O(N²) insertion-sort — Triết Vector exposes no built-in sort at v0.7.3.2; the differential corpus runs on tens of modules so quadratic is fine), `string_less_than` + `ascii_char_lt` (Triết has no String ordering primitive at v0.7.3.3, so we do a printable-ASCII lookup-table compare suited to dot-paths). Pipeline mirrors Rust `LoaderState::finish`: aggregate loader errors → cycle errors → (skip resolver if pre-resolver errors exist) → resolver errors. Drops the partial program when any error exists to match Rust's `Result::Err` arm.
  - Schema is **path-based**, not idx-based: `Module` lines carry `path` + `parent` (as path text or `null`) + `children` (sorted list of child paths) + `items` count + sorted `bindings` array. `idx`/`arena`/`ModuleId.raw` are deliberately omitted because Rust pre-loads stdlib (occupying idx 0..10) and Triết doesn't — the raw indices would differ for byte-identical user programs. Path identity is implementation-neutral.
- [x] **v0.7.6.1** — Types + LoaderError scaffolding
- [x] **v0.7.6.2** — Loader (filesystem + in-memory) + file layout restructure
- [x] **v0.7.6.3** — Cycle detection (DFS over import graph)
- [x] **v0.7.6.4** — Resolver (name resolution + visibility check)
- [x] **v0.7.6.5** — `modules_differential` NDJSON byte-diff gate (closes v0.7.6 umbrella)

## v0.7.7 — `compiler/typecheck.tri` port (in progress)

- [x] **v0.7.7.1** — Types + TypeError + Env scaffolding (this commit)
  - `compiler/typecheck.tri` (~1000 LOC, new) — ports `crates/triet-typecheck/src/{types.rs, error.rs, env.rs}` (~1150 Rust LOC combined). New types: `TypeKind` (16 variants — Trit / Tryte / Integer / Long / Trilean+refined / String / Unit / Nullable / Tuple / Function / Range / UserStruct / UserEnum / TypeParam / Outcome / Unknown), `TypeArena` (Vector<TypeNode> keyed by Integer ID — Triết enum variants can't recursively contain themselves so `Box<Type>` becomes arena ID, mirrors parser.tri's Arena pattern), `TypeError` (27 variants covering E1001 UnknownType → E1034 TrileanReturnNotRefined + W2001 NullDeprecated, each with per-variant payload struct), `TypeEnvironment` (Vector<Frame> scope stack, `Binding { type_id, is_mutable }`). Helpers: `alloc_*` constructors for every TypeKind variant, `type_display` (recursive type printer matching Rust's `impl Display for Type` — `Integer?`, `(Integer, String)`, `function(…) -> R`, `Range<T>`, `Integer~String`, `Integer?~String`), `type_is_numeric` / `type_is_trilean` / `type_is_refined_trilean` predicates, `type_error_code` / `type_error_span` for the v0.7.7.5 byte-diff gate, `type_env_new` / `push_frame` / `pop_frame` / `declare` / `declare_with_mut` / `lookup_binding` / `lookup_type`.
  - **Triết-side gotcha recorded**: `mutable` is a keyword (used in `let mutable x`), so `mutable: Trilean!` can't be a field name and `binding.mutable` can't be a field access. Renamed `Binding.mutable` → `Binding.is_mutable` consistently — matches v0.7.5.3 precedent (`LetStmtPayload.is_mutable`).
  - **Note on `with_prelude`**: Rust's `env.rs` ships a `with_prelude()` that pre-binds `print` / `println` / `to_string` / etc. v0.7.7.1 omits this — the prelude depends on resolver stdlib pre-load (deferred to v0.7.10) and on Function/Tuple type construction sites which mature in v0.7.7.{2,3}. Lands alongside the checker driver in v0.7.7.2.
  - 1 new integration test (`typecheck_types_smoke.rs`) loads `compiler/typecheck.tri` end-to-end (load → typecheck → lower → write `.triv` → round-trip read → run `main()` on VM). Triết-side `main()` exercises every constructor + display / code / span helper across all variants + Env push/pop/declare/lookup. 1333 workspace tests pass (was 1332, +1); `cargo clippy --workspace --all-targets` clean.

### Remaining v0.7.7 sub-tasks

- [x] **v0.7.7.2** — Literal + binding + control-flow checking (Stmt::Let / Const / Return / Break / Continue / Assign / If / While + Expr literals + binary/unary ops). Lands `check_resolved` driver + prelude. (commit af57187)
- [x] **v0.7.7.3** — Function + call + generic Rust-style inference (FunctionDef typecheck + CallExpr resolution + TypeParam substitution per ADR-0019 §A7.1). (commit af57187)
- [x] **v0.7.7.4a** — Structs + enums + tuple/generic types + For-loop pattern binding. Lands `StructItem` / `EnumItem` declaration (pass 1), full `TupleType` resolution, `Vector<T>` / `HashMap<K,V>` built-in shells (per ADR-0019 Addendum §A7), generic user-type arity check + `TypeParam` substitution, and `bind_pattern` for For-loop variables (`Wildcard` / `Identifier` / `Tuple` / `Or` / `EnumVariant` / `OutcomeArm` / `Range` / `Literal` / `Null`). `MatchExpr`-driven pattern binding + Match exhaustiveness + `StructLiteralExpr` / `EnumLiteralExpr` / `OutcomeConstructorExpr` body inference defer to v0.7.7.4b — Triết-side parser doesn't yet surface `Expr::Match` / `Expr::StructLiteral` / `Expr::EnumLiteral` (gap noted under v0.7.5.6). (commit 9524dfa)
- [x] **v0.7.7.4b** — `OutcomeConstructorExpr` (return-context driven) / `OutcomeDefaultExpr` (`~:`) / `ElvisOpExpr` (`?:`) / `RangeExpr` (`..` / `..=`) / `FieldAccessExpr` body-side inference. Wires `infer_outcome_propagate` mirroring Rust impl, but its smoke assertion defers to v0.7.7.5 — the capture-binding path hits a Triết-side VM regression (`declare_with_mut` reads `HashMap` field as Unit at runtime after deep frame stack). `MatchExpr`-driven pattern binding + `StructLiteralExpr` / `EnumLiteralExpr` defer until parser surfaces those variants (gap under v0.7.5.6). (commit 98dc8e5)
- [x] **v0.7.7.5** — `typecheck_differential` NDJSON byte-diff gate (closes v0.7.7 umbrella per ADR-0019 §A7.7). Adds `dump_typecheck_errors_ndjson(source) -> String` to `compiler/typecheck.tri` + helpers (`tc_json_escape`, `tc_string_less_than` / `tc_ascii_char_lt` mirroring `modules.tri`'s ASCII-lookup approach, `tc_sort_errors` O(N²) insertion sort by `(span_start, span_end, code)`). 20-case corpus covering clean programs + every major error class (`E1001`/`E1002`/`E1003`/`E1006`/`E1007`/`E1012`/`E1015`/`E1016`/`E1025`/`E1033`/`E1034`) lands byte-identical. ~? propagate corpus + match-arm cases defer until the v0.7.5.6 parser gap closes and the v0.7.7.4b `declare_with_mut`/HashMap-Unit runtime regression is root-caused. (commit 8a77c7f)

### Remaining v0.7 sub-tasks after typecheck

- [ ] **v0.7.8** — `compiler/ir_lowerer.tri` + lowerer_differential test
  - [x] **v0.7.8.1** — IR types + Instruction enum scaffolding. Ports `crates/triet-ir/src/{types.rs, constant.rs, instr.rs, module.rs}` foundations: 4 ID wrappers (`ValueId` / `BlockId` / `FuncId` / `ConstId` with `*_display`), recursive `TypeTag` via `TypeTagArena` (12 variants — primitives + Nullable / Vector / HashMap / Outcome with recursive Display), `Constant` enum (8 variants) + `ConstantPool` (Vector-backed, linear-scan intern stub), `Operand` (ValueOp / ConstOp), the full 53-variant `Instruction` enum (Const / 7 arithmetic / 5 Ł3 logic / 3 K3 logic / 6 comparisons / 5 conversions / 3 struct / 3 enum / 3 nullable / 4 call / 2 closure / 5 control-flow / Phi / 6 Outcome) with per-variant payload structs, `BuiltinName` (27 variants), `PhiIncoming`, `BasicBlock` + `Function` + `IrModule` + `IrProgram` containers, `instruction_is_terminator` / `instruction_destination` / `instruction_opcode_name` helpers, `function_is_well_formed`. 1 new bootstrap test (`ir_lowerer_types_smoke.rs`). Triết-side gotcha: field name `constant:` conflicts with the `constant` keyword — renamed to `const_id:`. (commit 40fba9f)
  - [x] **v0.7.8.2** — Literal + binding + arithmetic lowering. Adds the `LowerCtx` state thread + helper API (`fresh_value` / `fresh_block` / `start_block` / `emit` / `push_scope` / `pop_scope` / `bind_var` / `resolve_var` / `intern_constant` / `resolve_type_expr_to_tag`). Lowers literals (Integer / Ternary / Trilean / String / Null), Identifier lookup (parameter + let-bound), all 20 binary ops (arithmetic + comparison + Ł3 + K3 logic), unary `Negate`, force unwrap (`!!` → `NullUnwrap`), statements (`Let` / `Constant` / `Return` / `ExpressionStmt`), block lowering with scope push/pop, function lowering with entry block + parameter ValueId allocation + auto-`Ret`, and the top-level `lower_module` + convenience `lower_source(source) -> IrProgram` driver. 1 new bootstrap test (`ir_lowerer_lower_source_smoke.rs`) covering empty / integer body / block-body let+arithmetic / multi-param function. Triết-side gotchas: `module` is reserved → renamed local to `ir_mod`; the lowerer's `lower_function` returns `LowerFunctionResult { ctx, fn }` so the threaded `type_arena` + `constants` survive across function boundaries. Control-flow (While / For / Loop / Break / Continue / Assign) needs phi-node insertion + `LoopContext` — deferred to v0.7.8.3 alongside function-call lowering. (commit 1eea2cf)
  - [x] **v0.7.8.3** — Function call + assign + simple control flow. Adds `func_table: HashMap<String, FuncId>` + `loop_stack: Vector<LoopContext>` to `LowerCtx`, threaded through every setter. `resolve_builtin_name` mirrors Rust's 27-entry stdlib name→`BuiltinName` table. `lower_call` dispatches: bare-identifier callee → builtin match first, then per-module `func_table` for `CallLocal`. `lower_assign_stmt` rebinds the target name in the innermost scope. `lower_loop_stmt` emits the canonical `Br entry → loop_body → Br loop_exit` shape, pushes a `LoopContext` for break/continue. `lower_break_stmt` / `lower_continue_stmt` resolve the innermost loop's target via the stack. Module-level pass 1.5 builds `func_table` upfront so forward calls resolve. 1 new bootstrap test (`ir_lowerer_call_loop_smoke.rs`) covering builtin call / local call / assign / loop+break / loop+continue. Phi-node insertion for While/For + break-with-value + CallCrossModule defer to v0.7.8.3b/.4. (commit b64418e)
  - [x] **v0.7.8.3b** — While + phi-node insertion. Adds `patch_phi_incoming(blocks, block_idx, phi_dest, new_incoming)` — rebuilds the `Vector<BasicBlock>` with the matching `Phi` instruction's `incoming` list replaced (Triết Vectors are immutable so every layer rebuilds — O(N²) but corpus is tens of blocks). Adds `collect_assigned_vars` (walks a `Block`'s statements, dedups AssignStmt target names), `emit_phi_placeholder` (one-incoming-edge stub, patched after body lowers), and `block_id_at` accessor. `lower_while_loop` implements the canonical 10-step pipeline: snapshot pre-loop bindings → spawn header / body / exit blocks → Br from pre-block to header → emit Phi placeholders at header → lower condition reading the phi'd vars → spawn `while_unknown_panic` block holding `Unreachable` (only for plain `while`; `while?` skips that and routes Unknown to exit per ADR-0010) → BrTrilean from header → lower body in fresh scope → patch Phi.incoming with body-end edges → implicit Br back to header → start exit block as new current. Triết-side gotcha: `Vector<ValueId?>` typechecks `get(v, i)` as `Nullable<Nullable<ValueId>>` — used a `ValueId { raw: -1 }` sentinel pattern instead to keep the storage as `Vector<ValueId>`. 1 new bootstrap test (`ir_lowerer_while_phi_smoke.rs`) verifying the 4-block / 5-block layouts + Phi at header.position[0] + 2-edge incoming after patch + BrTrilean terminator. (commit 178a481)
  - [x] **v0.7.8.4** — Struct + enum + outcome + For-loop lowering. Extends `LowerCtx` with `struct_fields: HashMap<String, Vector<String>>` + `variant_index: HashMap<String, VariantInfo>` + `enum_variants: HashMap<String, Vector<String>>` (Python helper script threaded all 3 fields through the 7 setters). `lower_module` pass 1a populates the tables from `StructItem` / `EnumItem` walks. `lower_identifier` promotes bare unit-variant names (`None`) to `EnumNew { variant_idx, payload: ~0 }`. `lower_call` promotes tuple-variant calls (`Some(42)`) to `EnumNew { ..., payload: arg }` before the builtin/local-call dispatch. `lower_outcome_constructor` emits `OutcomeNewPositive` / `OutcomeNewNegative` / `OutcomeNewNull` per arm. `lower_range_expr` lowers endpoints (full `Range` aggregate materialization defers to v0.7.8.4+). `lower_for_loop` reuses the v0.7.8.3b phi-insertion primitive: counter Phi at the for_header + Lt + BrTrilean dispatch + per-body-var phis, all 4 blocks (entry / `for_header` / `for_body` / `for_exit`). `FieldGet`'s `field_idx` falls back to 0 — full ADR-0023 ValueKind tracking defers. **Triết-side gotcha pinned**: `st.field_name` access on a re-bound local (e.g. after `st = result.ctx`) loses the LowerCtx kind tag and field_idx falls to 0 — added 3 accessor helpers (`ctx_current_block_idx` / `ctx_blocks` / `ctx_loop_stack`) that take a typed `LowerCtx` parameter to restore kind context. 1 new bootstrap test (`ir_lowerer_struct_enum_outcome_smoke.rs`) covering struct decl / enum decl / unit-variant promotion / tuple-variant promotion / Outcome ~+/~-/~0 / For loop over Range. (commit 606c550)
  - [x] **v0.7.8.5** — IR NDJSON dump (transitional bridge per ADR-0019 §A2). Ships `dump_ir_program_ndjson(source: String) -> String` + the supporting helper family (`ir_json_escape` / `ir_quote` / `dump_operands_array` / `dump_value_ids_array` / `dump_phi_incoming_array` / `dump_path_segments_array` + 22 per-opcode `dump_inst_*` helpers covering all 53 Instruction variants + `dump_block_lines` / `dump_function_lines` / `dump_module_lines`). Stable schema: `{"k":"Program","modules":N}` → `{"k":"Module","functions":M}` → `{"k":"Function","id":"@fN","name":"…","params":N,"return":"…","blocks":B}` → `{"k":"Block","id":"bN","name":"…","insts":I}` → `{"k":"Inst","op":"…","dest":"%N",…}`. Display-string IDs (`%N` / `bN` / `@fN` / `cN`) keep the format implementation-neutral. NDJSON is a transient bridge per ADR-0019 §A2 — v0.7.9 drops it when Triết-side data flows in-memory via the linker. Full `.triv` binary serde (3249 LOC Rust serde.rs port) defers to v0.7.9 alongside the cross-package linking infrastructure. 1 new bootstrap test (`ir_lowerer_dump_smoke.rs`) covering empty program / integer return (exact byte expected) / determinism / 2-param arithmetic (exact byte expected) / outcome constructor non-empty. Triết-side gotcha: `+` is not String concat — used `concat(…)` chains for building expected NDJSON. (commit 164e489)
  - [x] **v0.7.8.6** — `lowerer_differential` corpus gate (closes v0.7.8 umbrella per ADR-0019 §A7.8). Adds `crates/triet-bootstrap/tests/lowerer_differential.rs` with 21 corpus cases verifying Triết-side opcode-emission ordering matches the expected sequence for each source program: empty, Const→Ret, all 5 arithmetic ops + 6 comparisons + 5 Ł3 + 3 K3 logic, unary Negate, block-body let+arith, force-unwrap `!!`, builtin call (`println`), local function call, AssignStmt rebind, Loop+Break (3-block shape), While+Phi (5-block + canonical Phi/Lt/BrTrilean header), For+Range (4-block + counter Phi), enum unit + tuple variants, Outcome `~+`/`~-`/`~0`, comparison + Trilean logic ops smoke. **Why opcode-sequence, not full byte-diff**: Rust pre-loads stdlib (9 modules / 27 functions) during `load_program_from_source` whereas Triết doesn't (per the v0.7.6.5 stdlib-preload deferral to v0.7.10); the constant-pool offsets also diverge because Rust's pool is pre-filled with stdlib literals. Full byte-identical NDJSON is unreachable until v0.7.9 drops the bridges per ADR-0019 §A2. The opcode-sequence gate catches lowerer behavioral regressions (which instructions get emitted, in what order) without false positives from impl-numeric ID divergence. Triết-side dump tweaks: `dump_const_value` looks up the constant by ID + emits its display string (`Integer(42)` / `String("hi")` / etc.) instead of the per-impl `cN` ID; `dump_function_lines` drops the `id` field for the same reason. 1 new differential test file. v0.7.8 umbrella closed. (commit 83f35cb)
- [ ] **v0.7.9** — `compiler/pack_writer.tri` + `compiler/main.tri` + drop bridges
  - [x] **v0.7.9.1** — `.triv` LEB128 + TypeTag arena + ConstantPool writer/reader scaffolding. Ships `compiler/pack_writer.tri` (~750 LOC) with the 9 fixed-width / varint primitives (`write_u8` / `write_u32_le` / `write_i16_le` / `write_i64_le` / `write_string` / `write_varint` / `write_leb128` + read mirrors), `write_type_table` / `read_type_table` covering all 11 `TypeTag` discriminators (primitives + Nullable / Vector / HashMap / Outcome), `write_constants` / `read_constants` covering all 8 `Constant` variants. Bookended by `pack_writer_smoke_main(): Trilean!~TrivError` which builds a synthetic arena + 8-variant ConstantPool, runs encode → decode → re-encode → byte-equality at every hop, and returns `~+ true` on success. Rust-side bridge: 3 new VM builtins `into_bytes` / `from_bytes` / `blake3_hash` (per design Q4 — added now, unlocks the `iface_hash` / `impl_hash` CAS write path for v0.7.9.3); `blake3` workspace dep added. Triết-side gotchas pinned: (a) `Vector` is functional (Q1-A) — `push(buf, x)` returns new vector, so every writer threads buf via return value (Rust's `&mut Vec<u8>` doesn't translate); (b) `loop {}` form skips phi-stitching for mutable rebinds, hangs the VM in subsequent iterations — refactored to `while cond` form throughout (tracked as `v0.7.x.runtime-fix.loop-phi` below); (c) Triết `Integer` is 27-trit (~3.8e12 max) so `read_i64_le` only round-trips values within ±2^31 at v0.7.9.1 (Long-range support folds into v0.7.9.2 alongside the instruction encoder). Closes the 5 design questions from the plan doc: 1:1 port (Q1), `compiler/factorial.tri` for v0.7.9.5 gate (Q2), defer `vm_execute` to v0.7.11 (Q3), add `blake3_hash` now (Q4), `propagate-capture` already fixed by `f804cab` (Q5). 1 new bootstrap test (`triv_serde_smoke.rs`). (this commit)
  - [x] **v0.7.9.2** — Instruction encoder/decoder (53 opcodes) + Function table + Code section + IrProgram writer/reader. Adds ~1400 LOC to `compiler/pack_writer.tri`: 54 OPCODE_* + 2 OPERAND_* + 5 SEC_* constants (unblocked by v0.7.x.runtime-fix.const-items from previous commit); `write_instruction` / `read_instruction` over all 53 opcodes + dispatch tables (`construct_binary_inst` / `construct_unary_inst`); operand/option-operand/option-value/PhiIncoming/operand-list/value-id-list encoders; `absolute_path_to_string` + `string_to_absolute_path` for cross-module call paths; `collect_type_table` walks the IrProgram's modules + constants and builds a deduplicated global `TypeTagArena` with composite inner-indices remapped from source-module arenas; `write_function_table` + `write_code` + top-level `write_program` (header `triv` magic + version 5 + 4 sections). The byte-identical gate in `triv_round_trip.rs` (19 cases) uses Triết-emit → Rust-decode → Rust-re-emit byte equality (not direct vs `Rust write_program` since stdlib pre-load divergence — see v0.7.6.5 deferral and v0.7.10 entry below). 19/19 cases pass: const+ret, arithmetic, unary negate, block+let, force-unwrap, builtin call, local call, assign, loop+break, while+phi, for+range, enum unit + tuple variants, outcome ~+/~-/~0, comparison + Trilean logic. Triết-side gotchas pinned + memory'd: (a) **`==` on enum/struct values returns false in VM** (`runtime_eq` only covers primitives) — hand-rolled `type_tag_eq` + `constant_eq` with discriminator + per-variant accessor helpers; also rewrote `constant_pool_intern` in `compiler/ir_lowerer.tri` to dedup via the new `constant_eq` because Rust's intern dedups while Triết's didn't, which broke round-trip byte symmetry (Rust reader collapses duplicates). (b) **`get(vec, i)!!.field` inline chain loses struct identity** — bind to `let m = get(...)!!` first then access `m.field`; surfaced when `write_function_table` saw `length(program.modules)=1` but `length(get(program.modules, i)!!.functions)=0`. (c) **`Trilean!` refinement breaks on long if-else-if chains** — the v0.7.5.6a `find_double_colon` used `result == ~0` (Nullable compare returns generic `Trilean`) inside a while-loop condition, triggering E1033; refactored to use a separate `found: Trilean!` sentinel. (d) **Module path display matches Rust's `AbsolutePath::to_string`** — empty segments + empty name = `.` (one byte), not `""`. Snapshot in `crates/triet-cli/tests/snapshots/demo_programs__module_system_demo.snap` updated to reflect the 3 new std.text functions added in v0.7.9.1 (`parse_integer` / `into_bytes` / `from_bytes`). New bootstrap test count: 19 (`triv_round_trip.rs`). (this commit)
  - [x] **v0.7.9.3** — `.khi` ABI metadata + sections + BLAKE3 hash chain (minimal scope). Appends ~430 LOC to `compiler/pack_writer.tri` (rather than a separate `tripack_writer.tri` — Triết loader's nested-search-dir rule would force `compiler/pack_writer/parser.tri` lookup when loading the sibling transitively, see "Triết-side gotchas" below). Ships `SemVer`/`AbiMetadata`/`KhiRead` structs, 6 domain separator helpers (16-byte ASCII strings per ADR-0014 §6 — `triet/term-i  \0\0` etc., byte-for-byte mirror of `crates/triet-pack/src/hash.rs`), `blake3_with_domain` helper composing domain ‖ payload through the v0.7.9.1 `blake3_hash` builtin, `compute_iface_hash_empty` + `compute_impl_hash` for the package hash chain (per ADR-0014 §4), `write_khi` (magic `trip` + pack version 1 + 2 sections: ABI metadata + IR code), `read_khi` (mirror with magic check + section dispatch), and `serialize_source_to_khi` source→bytes entry. v0.7.9.3 scope is the MINIMAL case: empty types/exports/deps/caps tables — these serialize as `varint(0)` so the wire format stays valid for Rust's `read_khi`, just without populated metadata. v0.7.9.4 populates real types/exports when `compiler/main.tri` walks the lowered IR. New `std/crypto.tri` stub registers `blake3_hash` for path-to-builtin resolution; `crates/triet-modules/src/loader.rs` adds `module crypto` to the stdlib synthetic-root source (stdlib count 12→13). Snapshot `demo_programs__module_system_demo.snap` updated for the new std.crypto module. 2 new bootstrap tests in `crates/triet-bootstrap/tests/khi_serde_smoke.rs`: (1) Triết-side `khi_smoke_main()` returns `Outcome::Positive(Trilean::True)` after writing+reading its own minimal pack; (2) Rust `triet_pack::read_khi` cleanly decodes the Triết-emitted bytes, asserts abi_version=2 + pkg_name + non-zero iface/impl hashes + embedded `triv` code section. **Triết-side gotcha pinned**: loader's nested-search-dir (`<parent>/<name>/<name>.tri`) means a new sibling .tri file that does `module pack_writer;` triggers `compiler/pack_writer/parser.tri` lookup — which doesn't exist. Workaround: co-locate related code in one file. Fix scope: loader could detect that `<parent>/<name>.tri` is the canonical location and not push a new search dir; tracked in deferred bugs as `v0.7.x.runtime-fix.loader-nested-search`. 1411 workspace tests pass (was 1409). (this commit)
  - [x] **v0.7.9.4** — `compiler/main.tri` self-hosted CLI driver (`check` + `build` subcommands; `dump` + `run` deferred per Q3-B). New file `compiler/main.tri` (~260 LOC) declares 5 top-level `module X` siblings (parser / modules / typecheck / ir_lowerer / pack_writer) so the absolute-path imports in those existing files (`from crate.parser import …`, `from crate.ir_lowerer import …`) resolve when main.tri is the root — wasteful but correct; a future cleanup converts the inner `module X` declarations to `self.X` form to dedupe. `parse_args(argv: Vector<String>) -> Args` walks positional + `-o` / `--pkg` flags; `classify_subcommand` maps to a 4-variant `Subcommand` enum (CheckCmd/BuildCmd/UnknownCmd/MissingCmd). `check_command` runs `dump_typecheck_errors_ndjson` and dispatches Trit on empty-vs-nonempty output; `build_command` runs `serialize_source_to_khi` then writes via the new `write_bytes` builtin. New Rust VM builtin `WriteFileBytes` (wire ID 30 in `.triv` serde) writes `Vector<Integer>` to disk as raw bytes — Q4-A strict 2-state Trilean, any byte outside `0..=255` returns False. Stub added to `std/io/fs.tri` (item count bumped 3→4 in `demo_programs__module_system_demo.snap`). 3 new bootstrap tests in `crates/triet-bootstrap/tests/triet_in_triet_compile.rs`: `main_smoke_dispatches_args_correctly` (parse_args + dispatch sanity), `main_build_emits_readable_tripack` (Triết main(build) writes `.khi` decoded by Rust `read_khi`, asserts pkg_name + abi_version + non-zero hashes + embedded `triv` magic), `main_unknown_subcommand_returns_negative` (error dispatch path). Test scope per Q2: minimal — byte-identical comparison with Rust output defers to v0.7.9.5 because stdlib-preload divergence (v0.7.10) still makes direct comparison infeasible. **Triết-side gotchas pinned this commit:** (a) `0t+ / 0t- / 0t0` are balanced-ternary INTEGER literals per SPEC §1.5.1, NOT Trit — Trit literals are `1_trit / 0_trit / -1_trit` (suffix form). CLAUDE.md table at the language-conventions section misleads on this; corrected via helper functions `exit_ok() -> Trit = 1_trit` / `exit_err() -> Trit = -1_trit`. (b) Match-arm `s => SourceLoaded(StringPayload { value: s })` on a `String?` scrutinee binds `s: String?`, not String — use `_ => SourceLoaded(StringPayload { value: opt!! })` (the pack_writer precedent) until `~+ s` outcome-arm binding is wired through for nullables. 1416 workspace tests pass (was 1413 = 1411 + 2 loader regressions).
  - [x] **v0.7.9.5** — Drop NDJSON bridges + `bootstrap_self_compile.rs` end-to-end gate (closes v0.7.9 umbrella per ADR-0019 §A2: "v0.7.9 drops all bridges; Triết-side data flows in-memory"). Deletes 6 differential test crates (`lexer_differential.rs` / `parser_differential.rs` / `modules_differential.rs` / `typecheck_differential.rs` / `lowerer_differential.rs` / `ir_lowerer_dump_smoke.rs` — ~3070 LOC) + ~2440 LOC of `dump_*_ndjson` helpers across the 6 `compiler/*.tri` files. Replaces `dump_typecheck_errors_ndjson` usage in `compiler/main.tri::check_command` with direct `check_source(source)` → human-readable count. Adds new `compiler/factorial.tri` (~30 LOC iterative factorial — picked per Q1-A 2026-05-24 decision; recursive form blocked by `lowerer-incomplete-expr` for `IfExpr`, the `= { … }` form blocked by `parser-eq-block-body`). Adds `load_program_from_source_no_stdlib` (`triet-modules/src/lib.rs` + `loader.rs`) as the Rust mirror of Triết-side `lower_source`'s no-stdlib + no-typecheck shape — needed for the byte-identical comparison harness. New `crates/triet-bootstrap/tests/bootstrap_self_compile.rs` ships 3 tests: `factorial_self_compile_content_equivalent` (Rust ≡ Triết: same module count, function name, param count, block count, total instruction count — strict byte-identical deferred until the 3 follow-up bugs below close), `triet_emit_is_deterministic` (2 runs of Triết-side main(build) produce identical bytes — catches non-determinism independent of Rust mirror), `triet_emit_decodes_via_rust_reader` (envelope structure + embedded `triv` magic). **3 new Triết-side gotchas pinned during gate implementation:** (a) Enum-variant-with-struct-payload destructure loses inner Struct content (`SourceLoaded(StringPayload)` → `match … { SourceLoaded(p) => p.value }` returned empty String even though variant was constructed with non-empty content). Worked around in main.tri by switching `SourceResult { SourceLoaded(StringPayload), … }` → `SourceLoaded(String)` (primitive payloads round-trip correctly); tracked as `v0.7.x.runtime-fix.struct-in-enum-payload-identity`. Without this fix, every prior v0.7.9.4 `main(build)` invocation silently produced empty IR — the v0.7.9.4 test passed only because its assertions checked envelope shape, not IR content. (b) Triết-side `compiler/ir_lowerer.tri::lower_expression` has NO arms for `IfExpr` / `MatchExpr` / `MethodCallExpr` / `ElvisOpExpr` / `OutcomePropagateExpr` / `OutcomeDefaultExpr` (falls through to `_ => fresh_value(ctx)`). The deleted `lowerer_differential` test only validated structural shape (opcode + dest + operand count) so this semantic gap stayed hidden through v0.7.8. Tracked as `v0.7.x.runtime-fix.lowerer-incomplete-expr`. (c) Triết-side parser's `parse_function_body` accepts `function f() = expr` and `function f() { … }` but NOT `function f() = { … }` (the Rust-accepted form). Tracked as `v0.7.x.runtime-fix.parser-eq-block-body`. **2 byte-level encoder divergences blocking strict byte-identical:** (d) `v0.7.x.runtime-fix.write-function-table-module-prefix` — Triết emits `"."` where Rust emits `"crate."` for the function's qualified name namespace prefix. (e) `v0.7.x.runtime-fix.block-emission-order` — Triết and Rust diverge on the order `while_body` / `while_exit` / `while_unknown_panic` blocks land in the function table. Both are small fixes deferred from this commit's scope. 1316 workspace tests pass (was 1416 = -103 differential test cases + 3 new bootstrap_self_compile cases). Clippy `-D warnings` clean. (this commit)
- [x] **v0.7.x.identity** — Khí + Đạo identity rename (per [ADR-0024](docs/decisions/0024-khi-dao-identity-naming.md)). 5-stage commit series: (A) path keyword `crate` → `khi` (61860b4), (B) wire format `.tripack` → `.khi` (d88a943 + f004034), (C) CLI binary `triet` → `dao` (0faf63d), (D) manifest `triet.package` → `dao.package` + lockfile `triet.lock` → `dao.lock` + policy `triet.policy` → `dao.policy` (06df86d), (E) Vietnamese subcommand aliases `dao tao/kiem/chay/kho` (2094132). Hard cutover. Series closed — 6 commits total. **Next: v0.7.10.**
  - [x] **v0.7.x.identity.adr** — ADR-0024 commit (docs/decisions/0024-khi-dao-identity-naming.md, 8871ddb).
  - [x] **v0.7.x.identity.A** — Path keyword `crate` → `khi` (41 files, 61860b4).
  - [x] **v0.7.x.identity.B** — Wire format `.tripack` → `.khi` (43 files, d88a943 + f004034 fixup).
  - [x] **v0.7.x.identity.C** — CLI binary `triet` → `dao` (41 files, 0faf63d).
  - [x] **v0.7.x.identity.D** — Manifest `triet.package` → `dao.package` + lock `triet.lock` → `dao.lock` + policy `triet.policy` → `dao.policy` (27 files, 06df86d).
  - [x] **v0.7.x.identity.E** — Vietnamese subcommand aliases `dao tao/kiem/chay/kho` (1 file, 2094132). **Closes v0.7.x.identity series (6 commits).**
  - [x] **v0.7.x.identity.F** — Post-v0.7.11.4 final leftover sweep (this commit). Triết-side rename `module_path_crate_root` → `module_path_khi_root` + `module_path_is_local_crate` → `module_path_is_local_khi` (compiler/modules.tri + compiler/driver.tri + compiler/ir_lowerer.tri comment). Mirrors Rust impl's `ModulePath::khi_root()` + `is_local_khi()` (renamed in v0.7.x.identity.A, Triết-side missed the round). Doc-comment cleanup across 9 files: SPEC.md (capability namespace list `crate.*` → `khi.*`), `crates/triet-modules/src/{module,resolver,cycle,loader}.rs` (10 doc lines), `crates/triet-ir/src/lowerer.rs` (1 test comment), 3 bootstrap test files (`driver_smoke`, `modules_resolver_smoke`, `triv_serde_smoke` — doc lines referring to `crate.foo` / `ModulePath::crate_root()`). Zero-semantic; 1332 tests pass; clippy `-D warnings` clean.
- [x] **v0.7.10** — CLI wiring carry-over + Triết-side stdlib pre-load (5 stages, closes umbrella). Stage 1: `get_env` builtin wire ID 31 (692a0ce). Stage 2: Triết-side `load_stdlib()` + `load_program_from_source_with_stdlib()` in compiler/modules.tri — synthetic std root, multi-line string literal (f63b4fb). Stage 3: `PackageManifest::discover()` walk-up manifest discovery (0de28c5). Stage 4: `check_capabilities` wired into `build_program` — E2200/E2201 diagnostics (1df1dc5). Stage 5: `E2208.CapabilityDivergence` error variant in capability_link (1e0f651, **later reverted in v0.7.10.7**). Deferred to v0.7.11: full multi-file pipeline integration (main.tri switching from single-file `lower_source` to `load_program_from_source_with_stdlib` + `check_resolved` + `lower_program`), cross-package linking, caps table population, DevTtyPrompt runtime activation. **Post-audit follow-ups** in v0.7.10.6/7/8 close 6 audit findings (path-dispatch collision, JSON error code placeholder, speculative E2208, identity-rename leftovers, latent loader regression, missing tests) before opening v0.7.11.
  - [x] **v0.7.10.6** — Path-based builtin dispatch + E2108 duplicate-import (audit blocker #1 + new resolver guarantee). Adds `LowerCtx.import_paths: HashMap<String, String>` (bare name → full dot-path), 2-stage `resolve_builtin_name` (path-based primary via `resolve_builtin_by_path` mirroring Rust's `path_to_builtin` 31 arms; bare-name fallback preserved for `lower_source` smoke tests). New `lower_module_with_imports(arena, item_ids, import_paths)` overload — existing `lower_module` becomes a 1-line wrapper passing `hashmap_new()` for the single-source case. Closes the latent `"get"` collision between `VectorGet` (line 1897) and `EnvGet` (line 1908, added v0.7.10.1) that would have crashed any future `from std.env import get` call with a VM type mismatch. Resolver-side: `LoaderError::DuplicateImport` E2108 variant + `bind_import` helper wraps 5 `bindings.insert(...)` sites in `triet-modules/src/resolver.rs` (same-path rebind idempotent; distinct-path collision errors). User-visible guarantee: `from a import value` + `from b import value` errors at resolve-time so the lowerer always sees an unambiguous bare-name → AbsolutePath mapping. Triết-side resolver mirror deferred to v0.7.11 (functional-update pattern in `compiler/modules.tri` differs from Rust's mutable insert). 3 new resolver tests. Match-arm ordering caveat pinned: `~0` arm MUST be first (Triết match is first-match-wins). 1325 workspace tests pass. (5282ae3)
  - [x] **v0.7.10.7** — `capability_error_code` mapper + remove E2208 + v0.7.x.identity leftover fold (audit blockers #2/#3/#7). Adds `capability_error_code(&CapabilityError) -> &'static str` (`const fn` for clippy nursery) mapping `MissingCapabilityClaim` → E2200 + `SelfContradictoryCapability` → E2201, per CLAUDE.md keep-in-sync rule. Replaces `crates/triet-cli/src/main.rs:547` placeholder `"triet::capability::E22XX"` so JSON-parsing clients can filter by specific code. Exhaustive `match` is the compile-time gate forcing future variants to extend. Removes speculative `CapabilityLinkError::CapabilityDivergence` E2208 variant added in 1e0f651 — no construction site, defers to v0.7.11 cross-package linking where the divergence case actually fires. Folds 4 `v0.7.x.identity.A` leftovers: `crate.helper` → `khi.helper` in `triet-interpreter/src/interpret_resolved.rs:151,163` (was in user's WIP) + `;; module crate.test` → `;; module khi.test` in 3 IR snapshot files. Also catches and fixes pre-existing `filesystem_resolves_flat_child` regression from v0.7.10.1 (`module env` bumped stdlib arena count 14 → 15 but hardcoded `assert_eq!(.., 14)` was missed; Stage 1's "165 tests pass" claim was inaccurate). 1 new CLI unit test (`capability_error_code_maps_variants_to_stable_codes`). (e70b29e)
  - [x] **v0.7.10.8** — `EnvGet` VM smoke + `PackageManifest::discover` walk-up smoke + workspace fmt sweep (audit blocker #8 + cumulative cleanup). 2 new VM unit tests for `BuiltinName::GetEnv`: `vm_get_env_returns_string_when_set` uses `CARGO_MANIFEST_DIR` (cargo guarantees during test exec) to read a real env var without `std::env::set_var` (workspace `unsafe_code = forbid` blocks Rust 2024's now-unsafe set_var); `vm_get_env_returns_null_when_unset` uses PID-suffixed name to guarantee no parallel-test collision. 3 new `PackageManifest::discover` tests: base dir, walk-up from nested subdir (`cargo`-style ADR-0019 §8), None on filesystem root. `cargo fmt --all` applied to 8 files with cumulative drift (3 bootstrap tests + 2 CLI tests + 3 lib files). CLI cap-aware build E2E test **deferred** — capability resolver rejects `sys.*`/`dev.*` imports at resolve-time (E2102 ReservedNamespace) before `check_capabilities` runs, so meaningful E2E needs the synthetic ResolvedProgram path (already covered by `capability_pipeline.rs`'s 12 tests). Resolver-order refactor pairs with v0.7.11. Total: 1325 workspace tests pass / clippy -D warnings clean / `cargo fmt --all --check` zero drift. (1e37b0b)
- [ ] **v0.7.11** — Stage 1 → Stage 2 bootstrap script + CI integration (umbrella, 8 sub-tasks). Per ADR-0019 §A7 + v0.7.10 deferrals. Scope: (I) close `lowerer-incomplete-expr` 6 arms + `parser-eq-block-body` so Stage 2 can compile `compiler/main.tri`; (II) multi-file `lower_program` driver + per-module `import_paths` wiring; (III) Triết-side E2108 mirror; (IV) `main.tri` switch to multi-file pipeline; (V) caps table population (Rust-side `dao build`, Triết-side parser defers to v0.8); (VI) DevTtyPrompt loader integration + re-add E2208.CapabilityDivergence; (VII) cap-aware build E2E test + resolver-order refactor; (VIII) `bootstrap_loop.rs` Stage 1→Stage 2 in-process gate.
  - [x] **v0.7.11.1a** — ElvisOpExpr lowerer arm (`b14eb18`). Ports `lower_elvis_op` from `crates/triet-ir/src/lowerer.rs:1341` into `compiler/ir_lowerer.tri` — NullCheck + BrTrilean + 4-block canonical shape (entry / elvis_some / elvis_default / elvis_merge) + Phi merge. ADR-0010: null (Trit::Zero ≡ Trilean::Unknown) routes BrTrilean's `unknown_block` + `false_block` both to elvis_default. Closes 1/6 arms of `v0.7.x.runtime-fix.lowerer-incomplete-expr`. New `elvis_smoke_main()` in `compiler/ir_lowerer.tri` (6 asserts: block count, BrTrilean terminator, NullCheck before it, NullUnwrap in elvis_some, Phi at elvis_merge with 2 incoming, BrTrilean's unknown_block ≡ false_block). New `crates/triet-bootstrap/tests/ir_lowerer_elvis_smoke.rs` (1 test). Import `ElvisOpPayload` added to the `khi.parser` import block. 1325 → 1326 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.11.1b** — OutcomeDefaultExpr (`~:`) lowerer arm (`06ac48d`). Ports `lower_outcome_default` from `crates/triet-ir/src/lowerer.rs:1608` into `compiler/ir_lowerer.tri` — OutcomeDiscriminant + BrTrilean + 4-block canonical shape (entry / oc_default_success / oc_default_fallback / oc_default_merge) + Phi merge. BrTrilean reads the discriminator Trit: Positive→True (success arm), Zero/Negative both route to fallback (matches ADR-0020 §3.3: null and failure share default-evaluation block). Closes 2/6 arms of `v0.7.x.runtime-fix.lowerer-incomplete-expr`. New `outcome_default_smoke_main()` in `compiler/ir_lowerer.tri` (5 asserts: block count, OutcomeDiscriminant + BrTrilean in entry, OutcomeUnwrapValue in success, Phi with 2 incoming in merge, unknown_block ≡ false_block). New `crates/triet-bootstrap/tests/ir_lowerer_outcome_default_smoke.rs` (1 test). Import `OutcomeDefaultPayload` added. 1326 → 1327 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.11.1c** — OutcomePropagateExpr (`~?`) lowerer arm (`bf55a14`). Ports `lower_outcome_propagate` from `crates/triet-ir/src/lowerer.rs:1702` into `compiler/ir_lowerer.tri` — OutcomeDiscriminant + BrTrilean + 5-block canonical shape (entry + `oc_prop_success` / `oc_prop_null` / `oc_prop_failure` / `oc_prop_merge`) + Phi merge with EXACTLY 2 incoming edges. Per ADR-0020 §3.1: BrTrilean routes Positive→success (unwrap value, Br merge), Zero→null (Const(Null), Br merge), Negative→failure (OutcomeUnwrapError → bind capture → lower early_return → Ret — DIVERGENT, no Br to merge). Closes 3/6 arms of `v0.7.x.runtime-fix.lowerer-incomplete-expr`. New `outcome_propagate_smoke_main()` in `compiler/ir_lowerer.tri` (5 asserts: 5-block count, BrTrilean's 3 distinct targets, failure arm terminates in Ret, OutcomeUnwrapError as first failure inst, Phi with EXACTLY 2 incoming). New `crates/triet-bootstrap/tests/ir_lowerer_outcome_propagate_smoke.rs` (1 test). Import `OutcomePropagatePayload` added. **Smoke source note**: uses block-body form `function f() { … }` (no `=` before `{`) — the `function f() = { … }` form is blocked by `v0.7.x.runtime-fix.parser-eq-block-body` (closes in v0.7.11.1g). Match-arm caveat applied (`~0 → skip bind` first per first-match-wins). 1327 → 1328 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.11.1d** — MethodCallExpr lowerer arm (`56ec56e`). Ports `lower_expr Expr::MethodCall` from `crates/triet-ir/src/lowerer.rs:1165` into `compiler/ir_lowerer.tri`. v0.2 stdlib surface only (no full method-table lookup yet): (a) `<recv>.length()` zero-arg → `CallBuiltin(text_len)`; (b) `<recv>.unwrap_value(msg)` / `.unwrap_error(msg)` single-arg → `OutcomeUnwrapValue` / `OutcomeUnwrapError` per ADR-0020 §6 (msg lowered for source-contract parity, ignored at VM); (c) unknown methods fall through to receiver (matches Rust's current incomplete arm). Closes 4/6 arms of `v0.7.x.runtime-fix.lowerer-incomplete-expr`. New `method_call_smoke_main()` in `compiler/ir_lowerer.tri` (3 sub-tests: `text_len` builtin found, `OutcomeUnwrapValue` emitted, `OutcomeUnwrapError` emitted). New `crates/triet-bootstrap/tests/ir_lowerer_method_call_smoke.rs` (1 test). Import `MethodCallPayload` added. 1328 → 1329 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.11.1g** — `parser-eq-block-body` fix in `compiler/parser/parser.tri` (`f6090af`). Closes `v0.7.x.runtime-fix.parser-eq-block-body` deferred bug. After consuming the `=` in `parse_function_body`'s `Assign` arm, peek the next token — if `LBrace`, dispatch to `parse_block` and wrap as `BlockBody`; otherwise dispatch to `parse_expression` as before. Now `function f() = { … }` parses identically to the bare-`{` form. 2 new assertions in `parser/parser.tri::main()` smoke (covered by `parser_item_smoke`). **Test-source bug surfaced + fixed**: `typecheck.tri::control_flow_smoke_main` test 11 used `let c: Integer = 7 -c` which Pratt-parses as `let c = 7 - c` (binary minus). Pre-fix the `= { … }` form rejected entirely so this ambiguity was masked; post-fix the source disambiguates with explicit `;`. Test count unchanged at 1329 (only inline asserts in existing smokes); clippy `-D warnings` clean. Re-ordered before .1e/.1f per Q2-A — landing this first lets subsequent smoke tests use the natural `function f() = { … }` form without block-body workaround.
  - [x] **v0.7.11.1e1** — `IfExpr` parser variant + parse logic (`9908012`). Discovery during .1e investigation: Triết-side parser had NO `IfExpr` / `MatchExpr` variants in `Expr` enum at all (only the 17 v0.7.5.2 variants), so the v0.7.x.runtime-fix.lowerer-incomplete-expr deferred bug entry was actually a 2-side gap: lowerer's `_ => fresh_value(...)` fall-through never even saw `IfExpr` because parser couldn't produce it. Re-scoped .1e/.1f from 2 sub-tasks into 4 (.1e1 parser + .1e2 lowerer; .1f1 parser + .1f2 lowerer) per author Q1-A. New `IfExprPayload { condition, then_branch: Block, else_branch: Block?, treat_unknown_as_false: Trilean! }` declared AFTER `Block` (line ~412) because Triết requires Block-referencing structs to follow Block's declaration; moved the entire `Expr` enum to follow IfExprPayload accordingly. New `parse_if_expression` (`if cond { then } [else { else } | else if …]`) + `parse_if_else_branch` helper (returns `Block?` to signal no-else). `else if` chain wrapped as `Block { stmts: [], final: <inner-if> }` per Rust impl precedent — keeps grammar tail-recursive without dedicated `ElseIf` shape. Wires `IfKw` + `IfQKw` into `parse_prefix`. Extended `format_expr` with `(if cond then else)` / `(if? …)` shapes. 4 new assertions in `parser/parser.tri::main()`: bare-if-no-else, if-else, if?-else (Unknown→else per ADR-0010), else-if chain. **Helper-collision gotcha pinned**: existing 2-arg `previous_token_end(state, default_end)` already exists for stmt-level span closing; reuse it (passes `head.span_end` as default) instead of declaring a 1-arg variant. 1329 tests pass (no new bootstrap test — inline asserts run via existing `parser_item_smoke`); clippy `-D warnings` clean.
  - [x] **v0.7.11.1e2** — `IfExpr` lowerer arm + phi-stitch (`35b39ce`). Ports `lower_if_expr` from `crates/triet-ir/src/lowerer.rs:1931` into `compiler/ir_lowerer.tri`. Canonical 4-block shape (entry + then + else + merge) for `if?` (Unknown→else); 5-block shape for plain `if` (extra `if_unknown_panic` holding `Unreachable` per ADR-0010 / SPEC §7.1.1). Phi at merge for the if's value (always); per-mutated-var Phi when either branch writes. Closes 5/6 arms of `v0.7.x.runtime-fix.lowerer-incomplete-expr`. **Scope-discipline note**: Triết `bind_var` writes innermost-scope only (no `rebind_var` walk-up like Rust). Branches use explicit `push_scope` → `lower_block_inline` → snapshot via `resolve_var` → `pop_scope` so body-end values capture pre-pop, mirroring the v0.7.x.runtime-fix.while-body-scope-pop-order fix shipped in v0.7.9.5. Block emission order: entry → if_unknown_panic (if plain) → then → else → merge. Imports `IfExpr` variant + `IfExprPayload` struct. New `if_expr_smoke_main()` in `compiler/ir_lowerer.tri` (6 asserts: 5-block plain-if shape, BrTrilean terminator with 3 distinct targets, Phi at merge with 2 incoming, 4-block `if?` shape, `if?` BrTrilean's unknown_block ≡ false_block). New `crates/triet-bootstrap/tests/ir_lowerer_if_expr_smoke.rs` (1 test). 1329 → 1330 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.11.1f1** — `MatchExpr` parser variant + parse logic (`96829ee`). Adds `MatchArm { pattern: Integer, body: Integer }` + `MatchExprPayload { scrutinee: Integer, arms: Vector<MatchArm> }` structs declared after `Block` + `IfExprPayload` (the position-dependent payload zone). New `parse_match_expression` driven from `parse_prefix` on `MatchKw`. Parses `match scrutinee { pattern => body, pattern => body, … }` with optional trailing comma; arms are first-match-wins (no exhaustiveness check at parser tier — that lifts to typecheck). **Guards NOT supported at v0.7.11.1f1** (`pattern if cond => body`) — none of compiler/*.tri use them; lift when a future sub-task demands. **Outcome-flow refactor pinned**: inside the while-loop arm-collection, `~- err` arms must agree in type with their sibling `~+ ...` arms, so the "expect `=>`" check is wrapped in a single `ParserState~ParseError` outcome and `~? |err| ~- err` propagates errors up to the outer function (mirrors v0.7.5.3 `ParseLetAnnotation` pattern). Extended `format_expr` with `(match scr (arm pat body) …)` pretty-print shape. 3 new assertions in `parser/parser.tri::main()`: two-arm match with wildcard, trailing comma, identifier-binding pattern. 1330 tests pass (no new bootstrap test — inline asserts via existing `parser_item_smoke`); clippy `-D warnings` clean.
  - [x] **v0.7.11.1f2** — `MatchExpr` lowerer arm + multi-arm phi (`7c22519`). Closes 6/6 arms of `v0.7.x.runtime-fix.lowerer-incomplete-expr`. Ports `lower_match_expr` + `lower_pattern_test` + `bind_pattern_vars` from `crates/triet-ir/src/lowerer.rs:2565+`. Each arm becomes `test_block → arm_body_block → merge`; last arm is unconditional Br (typecheck enforces exhaustiveness). Pattern coverage: Wildcard, Identifier (with variant-name disambiguation via `variant_index` lookup), Null (NullCheck + Eq against Trit::Zero), Literal (Integer/Ternary/String/Trilean), EnumVariant (EnumTag + Eq against Integer variant_idx), OutcomeArm (OutcomeDiscriminant + Eq against expected Trit). Tuple/Or/Range patterns deferred — not exercised by compiler/*.tri. **`bind_pattern_vars`** covers Identifier (with variant-skip), EnumVariant-with-payload (EnumPayload extract + recurse), OutcomeArm Positive/Negative-with-payload (Outcome unwrap + recurse). Merge block emits one Phi for the result value carrying arm-count incoming edges. **Per-mutated-var phi OMITTED** at this sub-task because Triết-side `Expr` enum has no `BlockExpr` yet — match arm bodies are single expressions (no AssignStmt). When future sub-task adds BlockExpr, mutation-phi needs porting; tracked as `v0.7.x.runtime-fix.match-arm-mutation-phi-port` in deferred bugs. **`Trilean!` discipline gotcha pinned**: `hashmap_contains` returns generic `Trilean` and trips E1033 inside `if`; refactored 3 call sites to `match hashmap_get(...) { ~0 => …, _ => … }` pattern (same as existing lowerer code style). New imports: `MatchExpr` + `MatchExprPayload` + `MatchArm` + Pattern variants (`LiteralPat`/`NullPat`/`EnumVariantPat`/`OutcomeArmPat`) + LiteralPattern arms + payload structs. 4 new emit-const helpers (`emit_const_int`/`emit_const_trit`/`emit_const_trilean`/`emit_const_string`) + `emit_eq` for the pattern-test plumbing. New `match_expr_smoke_main()` (5 asserts: 2-arm literal match block count = 5, entry ends in BrTrilean, merge starts with Phi with 2 incoming, 3-arm outcome match block count = 7, merge with 3 Phi incoming). New `crates/triet-bootstrap/tests/ir_lowerer_match_expr_smoke.rs` (1 test). 1330 → 1331 tests pass; clippy `-D warnings` clean. **Closes v0.7.11.1 umbrella** (lowerer-incomplete-expr fully resolved). Stage 2 can now compile any function whose body uses these 6 expression variants (Elvis / OutcomeDefault / OutcomePropagate / MethodCall / If / Match) — last gap is BlockExpr support, surfacing when Stage 2 tries main.tri's match-arm-with-block bodies.
  - [x] **v0.7.11.2** — Multi-file `lower_program(ResolvedProgram) -> IrProgram` driver (`d1c60e2`). Mirrors `crates/triet-ir/src/lowerer.rs:35`. Lives in NEW file `compiler/driver.tri` (NOT in `compiler/ir_lowerer.tri`) — putting it in ir_lowerer.tri would force a `module modules;` declaration that cycles with `pack_writer.tri`'s `module ir_lowerer;` graph (loader trips `khi → ir_lowerer → khi` cycle). `driver.tri` declares both `module ir_lowerer;` and `module modules;` as siblings of its `khi` crate root, breaking the cycle cleanly. **API**: `lower_program(rp: ResolvedProgram) -> IrProgram` walks each Module in `rp.modules`, converts the module's `bindings: HashMap<String, AbsolutePath>` to `import_paths: HashMap<String, String>` via `bindings_to_import_paths` helper (uses `absolute_path_display` to render dot-paths), calls `lower_module_with_imports` per-module, then patches the result's `path_segments` from the source `ModulePath` (via the new `ir_module_with_path` public helper added to ir_lowerer.tri) so `write_function_table` keys entries correctly. **Scope limitation**: takes the LAST module's `ConstantPool` as the IrProgram-level shared pool — cross-module ConstId remap deferred to `v0.7.x.runtime-fix.cross-module-const-remap` (only matters when Stage 2 starts multi-module compile in v0.7.11.4+). Single-package use cases (the current state) preserved. New `driver_smoke_main()` (5 asserts: 1-function lowering, path_segments patched to `["khi"]`, function body contains Add instruction). New `crates/triet-bootstrap/tests/driver_smoke.rs`. 1331 → 1332 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.11.3** — Triết-side resolver E2108 `DuplicateImport` mirror (`8931efa`). Ports `bind_import` from `crates/triet-modules/src/resolver.rs:42` into `compiler/modules.tri`. New `DuplicateImportErr(DuplicateImportPayload { name, prior_path, span_start, span_end })` variant added to `LoaderError` enum (now 9 variants). `loader_error_code` / `loader_error_span` / `loader_error_message` extended. New `bind_import` helper wraps 5 import-resolving `insert(bindings, ...)` sites in `resolve_one_from_name` + the `from-import-by-path` fallback case; same-path rebind is silently idempotent (allows convenience re-imports), distinct-path collision raises E2108. Own-definition + child-module binding sites (lines 1766, 1786) NOT wrapped — same source, no collision possible. Helper sits AFTER `ResolveOneNameResult` struct declaration (forward-ref discipline). New `count_duplicate_import_errors` helper for smoke. 2 new assertions added to `resolver_smoke_main()`: case 9 (distinct paths → E2108 raised), case 10 (same path → idempotent, no error). 1332 tests pass (no new bootstrap test file — inline asserts via existing `modules_resolver_smoke`); clippy `-D warnings` clean.
  - [x] **v0.7.11.4** — `compiler/main.tri` switch to multi-file pipeline (`4136446`). Adds `serialize_program_to_khi(source, pkg_name) -> BuildResult` to `compiler/driver.tri` — runs the 6-stage pipeline: `load_program_from_source` (parse + child modules; **stdlib pre-load DEFERRED to v0.7.11.5** — see scope note) → `detect_cycles` → `resolve_names` → `check_resolved` → `lower_program` → `write_program` + `write_khi`. Returns `BuildResult { bytes, errors }`. `main.tri::build_command` switches from `serialize_source_to_khi` (single-file, no typecheck) to the new multi-file path; loader / cycle / resolver / typecheck errors all surface as diagnostic messages before the `write_bytes` call. `main.tri` declares `module driver;` to expose the new entry; imports `BuildResult` + `serialize_program_to_khi`. **Stdlib scope note**: `load_program_from_source_with_stdlib` was the original v0.7.11.4 target, but its CWD-relative `std/X.tri` filesystem reads fail when bootstrap_self_compile.rs stages source into a tempdir (CWD is workspace root but `std/` lookup happens from tempdir's perspective). Stdlib activation pairs with proper CWD discipline + caps-table population in v0.7.11.5. `compiler/factorial.tri` already avoids stdlib (per its v0.7.9.5 design comment) so the byte-identical gate continues to pass unchanged. 1332 tests pass (no test count change — bootstrap_self_compile still passes byte-identical with the multi-stage path); clippy `-D warnings` clean.
  - [x] **v0.7.11.5** — Caps table population in Rust-side `dao build` (`c2dfc77`). Rust-side `build_program` emits `.khi` format with caps section populated from `dao.package` manifest when found; `run_bytecode` now accepts `.khi` files. Default output extension `.triv` → `.khi`. 1332 tests pass.
  - [x] **v0.7.11.6** — DevTtyPrompt loader integration + E2208 `CapabilityDivergence` (`8d11c27`). **Re-adds E2208**: new `CapabilityLinkError::CapabilityDivergence` variant with `missing_from_binary` + `extra_in_binary` Vec<String> + `root_pkg`. New `check_cap_divergence(manifest_requires, binary_caps, root_pkg) -> Option<CapabilityLinkError>` compares source `dao.package` requires against the `.khi`'s caps section using (cap_path, level) identity. Re-exported + wired into `build_program` — divergence fires E2208 before `write_khi`, refusing to write an untrustworthy binary. **DevTtyPrompt runtime resolution**: new `resolve_deferrals(deferrals, resolver) -> (granted, denied)` in `capability_resolver.rs` iterates `DeferredCap`s from `check_link_capabilities`, creates `PolicyRequest` per requester, resolves via `CapabilityResolver` (with optional TTY callback). Re-exported from `lib.rs`. **CLI refactor**: `emit_link_error` helper extracted from `build_program` to keep line count under clippy limit; `run_capability_check` reverted to inline JSON/non-JSON arms (CapabilityError is a different type). 5 new divergence tests in `capability_link.rs` (agreement, missing, extra, level mismatch, both sides). 1332 → 1337 tests pass; clippy `-D warnings` clean; `cargo fmt --all` zero drift.
  - [x] **v0.7.11.7** — CLI cap-aware build E2E test + resolver-order refactor (`68c5654`). **Removes unconditional E2102 barrier from Rust resolver** (`crates/triet-modules/src/resolver.rs`): the two E2102 `ReservedNamespace` checks in `resolve_whole_import` and `resolve_from_import` are retired. `sys`/`dev`/`usr` roots are now governed by the v0.6 capability system — `check_capabilities` fires E2200 (missing claim) or E2201 (self-contradictory) based on the manifest. Imports that lack a matching grant are caught at build time. The `LoaderError::ReservedNamespace` variant and `loader_error_code`/`span`/`message` remain in place for backward-compat but are no longer emitted in the resolver path. **Test updates**: `reserved_namespace_errors` → `sys_import_no_longer_fires_reserved_namespace` — verifies E2102 is NOT emitted for `from sys.cap import read` and E2104 UnresolvedImport fires instead. Snapshot `cli_tests__reserved_namespace.snap` updated from E2102 → E2104 (now `unresolved import: sys.core`). **New CLI E2E test**: `sys_import_no_longer_blocked_by_e2102` (cli_tests.rs) — `from sys.io import ping` produces E2104 not E2102. 1337 → 1338 tests pass; clippy `-D warnings` clean; `cargo fmt --all` zero drift.
  - [x] **v0.7.11.8** — `bootstrap_loop.rs` Stage 1 → Stage 2 in-process gate (this commit). New `crates/triet-bootstrap/tests/bootstrap_loop.rs` with 3 tests. **`stage2_compiles_factorial_self_byte_identical`**: Stage 2 compiles `compiler/factorial.tri` inside the VM, output MUST be byte-identical to Rust reference build (ADR-0019 §6 Layer 3 assertion). **`stage2_factorial_emit_is_deterministic`**: two independent VM runs produce identical bytes — catches non-determinism independent of Rust mirror. **`stage2_factorial_decodes_via_rust_reader`**: Stage 2 output decodes cleanly via Rust `read_khi`. Uses shared `triet_build_fixture(fixture_name, pkg_name)` helper that stages source in a tempdir, invokes Triết-side `main.tri::main(["build", ...])` inside the VM, reads the emitted `.khi` bytes. **Self-compile of main.tri DEFERRED to v0.7.12**: Triết-side `serialize_program_to_khi` uses `load_program_from_source` (in-memory) which cannot resolve file-bound `module parser;` declarations. To self-compile the full compiler, either a filesystem-aware Triết loader is needed or all compiler files must be staged with correct relative layout. Both options pair with the Stage 2 → Stage 3 gate. 1338 → 1341 tests pass; clippy `-D warnings` clean. **Closes v0.7.11 umbrella** (all 8 sub-tasks complete).
- [ ] **v0.7.12** — Stage 2 → Stage 3 + bit-identical gate verify (umbrella, 7 sub-tasks per author Q1-A/Q2-A/Q3-A 2026-05-25). Scope: (.1) `read_dir_recursive` Rust VM builtin — unlocks filesystem-aware module loading from inside Triết VM; (.2) Triết-side `load_program(root_path)` using the new builtin; (.3) port `match-arm-mutation-phi` to Triết-side `lower_match_expr`; (.4) `BlockExpr` parser variant + lowerer arm; (.5) main.tri switches to filesystem `load_program` + Stage 2 self-compiles; (.6) Stage 3 candidate production; (.7) `cmp Stage 2 ≡ Stage 3` byte-identical gate.
  - [x] **v0.7.12.1** — `read_dir_recursive` Rust VM builtin (`df98ec7`). New `BuiltinName::ReadDirRecursive` variant + `std.io.fs.read_dir_recursive` dispatch in `vm.rs::resolve_builtin_name`. `.triv` wire ID 32 (after `GetEnv=31`). Implementation walks directory recursively, returns `Vector<Vector<String>>` where each inner is `[relative_path, content]` for `.tri` files only. POSIX `/` separator regardless of host OS, UTF-8 content, alphabetical sort for determinism. I/O errors + non-UTF-8 silently skipped (data-tier — caller sees truncated results). New `walk_tri_files` helper does depth-first traversal. Display impl extended. `std/io/fs.tri` Triết stub added (self-recursive body, real impl in Rust builtin). 2 new VM tests: `vm_read_dir_recursive_returns_sorted_tri_pairs` (3 .tri files including nested + 1 README filtered out), `vm_read_dir_recursive_missing_root_returns_empty`. `demo_programs__module_system_demo.snap` updated `std.io.fs: 4 items → 5 items`. 1341 → 1343 tests pass; clippy `-D warnings` clean; fmt zero drift.
  - [x] **v0.7.12.2** — Triết-side filesystem-aware build driver (`6c20804`). **Discovery**: `compiler/modules.tri::load_program(root_path)` already existed from v0.7.6.2 and uses `fs_read` builtin to walk the filesystem. The gap was `driver.tri::serialize_program_to_khi` calling in-memory `load_program_from_source(source)` — Stage 2 couldn't resolve `module parser;` sibling files because no path context was passed. **Solution**: add new public `serialize_program_path_to_khi(root_path, pkg_name) -> BuildResult` that takes a filesystem path and calls `load_program(root_path)` instead. The in-memory variant stays for callers with only the source string (smoke tests, REPL). **`compiler/main.tri::build_command` switched** from `serialize_program_to_khi(source, ...)` to `serialize_program_path_to_khi(args.source_path, ...)`. The `load_source_or_fail` pre-read is no longer needed at build_command — the loader does its own filesystem reads via `fs_read`. **v0.7.12.1 builtin still in use** by future stages (the simpler-API path for batch loading). `bootstrap_self_compile.rs::factorial_self_compile_byte_identical` still passes (factorial.tri is single-file, behavior unchanged). Stage 2 main.tri self-compile test deferred to v0.7.12.5 once blockers .3 (match-arm-mutation-phi-port) + .4 (BlockExpr) close. 1343 tests pass; clippy `-D warnings` clean.
  - [x] **v0.7.12.3** — `match-arm-mutation-phi-port` (`5cd45af`). Closes `v0.7.x.runtime-fix.match-arm-mutation-phi-port` deferred bug. **Walker helpers**: new `walk_expr_for_assigns(arena, expr_id) -> Vector<String>` recurses through `IfExpr` (then/else `Block`s) and `MatchExpr` (arm bodies) collecting `AssignStmt` target names. New `walk_block_for_assigns(arena, block)` extends `collect_assigned_vars` with recursion into stmt-level expressions (`LetStmt.value`, `AssignStmt.value`, `ExpressionStmt.expression`, `WhileStmt.body`, `ForStmt.body`, `LoopStmt.body`). New `vec_extend_unique(dst, src)` linear-dedup merge. **`lower_match_expr` modifications**: pre-loop collects `mutated: Vector<String>` from all arm bodies via `walk_expr_for_assigns` + snapshots `pre_vals` via `resolve_var`. Per-arm: snapshot `arm_mutated_vals` (flat layout `i × mut_total + m`) BEFORE `pop_scope` (mirrors v0.7.x.runtime-fix.while-body-scope-pop-order pattern). Post-merge-block-start: emit one Phi per mutated var carrying `arm_total` incoming edges, defaulting arms that didn't touch the var to pre-match value. Skip Phi when every arm left the var identical to pre-match. **Scope note**: currently dead code because Triết-side `Expr` has no `BlockExpr` and match arm bodies are single expressions — walker returns empty for arm bodies that don't contain a nested `IfExpr`. When v0.7.12.4 lands `BlockExpr`, this infrastructure activates. New `match_mutation_phi_infra_smoke_main()` in `compiler/ir_lowerer.tri` (2 asserts: 5-block shape preserved, merge starts with result-value Phi). New `crates/triet-bootstrap/tests/ir_lowerer_match_mutation_phi_smoke.rs` (1 test). 1343 → 1344 tests pass; clippy `-D warnings` clean; fmt zero drift.
  - [x] **v0.7.12.4** — `BlockExpr` parser variant + lowerer arm (this commit). **Closes the v0.7.12.3 mutation-phi infrastructure** — match arms `=> { stmt; stmt; expr }` now lower correctly with cross-arm phi merging. **Parser side**: new `Expr::BlockExpr(Block)` variant. New `parse_block_expression(state)` wraps `parse_block` + emits `BlockExpr(block_step.block)`. `parse_prefix` gets `LBrace => parse_block_expression(state)` arm. v0.7.11.1g `parse_function_body` peek-for-LBrace logic kept — `function f() = { … }` continues to produce `BlockBody` (not `ExpressionBody(BlockExpr)`); both lower to `lower_block`/`lower_block_inline` so observable IR is identical. `format_expr` extended: `BlockExpr(b) => format_block(arena, b)`. **Lowerer side**: imports `BlockExpr` variant. `lower_expression` dispatches `BlockExpr(b) => lower_block_inline(ctx, b)` — explicitly NO scope push/pop. **Design rationale** documented: Triết's `bind_var` writes innermost; if `BlockExpr` pushed its own scope, AssignStmt rebinds inside the block would die on pop, defeating the v0.7.12.3 mutation-phi snapshot. Using `lower_block_inline` lets AssignStmt rebinds reach the enclosing scope where mutation-phi snapshots them. Trade-off: BlockExpr-local `let` bindings leak to the parent scope at the IR level — harmless because typecheck enforces source-level scoping, and the parent scope itself pops shortly (match arm, function body, etc.). Full BlockExpr scope isolation requires Rust-style `rebind_var` walking semantics — deferred to a future sub-task. **`walk_expr_for_assigns` updated**: new `BlockExpr(b) => walk_block_for_assigns(arena, b)` arm activates the mutation-phi pass for match arm block bodies (was the missing piece during .3 development). **2 new parser asserts** in `parser/parser.tri::main()`: match arm with block body, nested BlockExpr as let RHS. New `block_expr_smoke_main()` in `compiler/ir_lowerer.tri` (3 asserts: match arm with block body lowers to 5-block shape, merge has 2 Phi instructions = result + mutated var, let RHS with BlockExpr lowers in a single block). New `crates/triet-bootstrap/tests/ir_lowerer_block_expr_smoke.rs` (1 test). 1344 → 1345 tests pass; clippy `-D warnings` clean; fmt zero drift.
- [ ] **v0.7.13** — Verify gate ADR-0009 §A/B/C/D + workspace version 0.6.0 → 0.7.0 + SPEC v0.6 → v0.7 + docs sync

### Deferred runtime bugs

- [x] **v0.7.x.runtime-fix.const-items** — Added `Item::Const` lowering arm in `crates/triet-ir/src/lowerer.rs`. New Pass 1c walks every module's items, evaluates literal-only initializers (Integer / Tryte / Trit / Long / Trilean / String / Null) via `try_eval_const_expr`, and interns the value into the shared `ConstantPool` keyed by `constant_items: HashMap<String, ConstId>`. `lower_identifier` consults this table after local-scope resolution, emitting a fresh `Const` instruction that loads from the pool. Non-literal initializers (e.g. `constant X = Y + 1`) silently skip — left as a post-v0.7 const-folding concern. 5 integration tests in `crates/triet-bootstrap/tests/const_item_smoke.rs` cover Integer value, arithmetic combination, String value, multiple distinct constants, and local-shadow semantics (`let X = 99` inside an `inner()` fn shadows top-level `constant X`). Unblocks `compiler/pack_writer.tri`'s `constant OPCODE_*: Integer = …` table for v0.7.9.2's instruction encoder.
- [x] **v0.7.x.runtime-fix.propagate-capture** — Fixed struct identity loss across if-expr phi merges in `crates/triet-ir/src/lowerer.rs`.
- [x] **v0.7.9 prep** — Investigated pre-flight bugs, removed the check-box, and confirmed design decisions.
- [ ] **v0.7.x.runtime-fix.loop-phi** — `lower_loop_stmt` in `crates/triet-ir/src/lowerer.rs` doesn't insert phi nodes for mutable rebinds (unlike `lower_while_loop` / `lower_for_loop` which got phi treatment in v0.7.8.3b / v0.7.8.4). Surfaced during v0.7.9.1 pack_writer port: `let mutable tags = new(); loop { … tags = push(tags, t); break }` produced empty `tags` and the VM hung in the next iteration. Workaround at every site: rewrite as `while cond { … }` form (uses a sentinel boolean if the original `loop {}` needed early break). Fix scope: extend `lower_loop_stmt` to call the same `collect_assigned_vars` + `emit_phi_placeholder` + `patch_phi_incoming` machinery `lower_while_loop` uses, plus phi-stitch the `break_target` edges. Tests: add `crates/triet-bootstrap/tests/loop_mutation_phi.rs` once fixed. Not blocking v0.7.9.2+ since the while-form workaround is mechanical.
- [ ] **v0.7.x.runtime-fix.enum-struct-eq** — `runtime_eq` in `crates/triet-ir/src/vm.rs` has no arm for `RuntimeValue::Enum { .. }` or `RuntimeValue::Struct { .. }`, so `==` between structurally-identical enum/struct values always returns `false` at runtime. Surfaced during v0.7.9.2 pack_writer port: `type_tag_eq(a, b)` via plain `a == b` always returned false → `constant_pool_intern` couldn't dedupe → constant pool grew with duplicates → `.triv` round-trip diverged because Rust's reader-side intern collapses duplicates. Workaround at every site: hand-roll structural equality with `discriminator + per-variant accessor` helpers (see `type_tag_eq` + `constant_eq` in `compiler/pack_writer.tri` and `compiler/ir_lowerer.tri`). Fix scope: extend `runtime_eq` arms for Enum (compare variant + recursive payload eq) and Struct (element-wise field eq); arity-and-variant-name comparison must be deterministic; for Struct the field-name order is the lowerer's declaration order. Once fixed, all hand-rolled `*_eq` helpers can collapse to plain `a == b`. Not blocking v0.7.9.3+ since the workaround is mechanical.
- [ ] **v0.7.x.runtime-fix.chained-get-unwrap** — `Expr::ForceUnwrap` arm in `crates/triet-ir/src/lowerer.rs` doesn't propagate `ValueKind::Nullable { inner_kind }`'s `inner_kind` onto the dest, so inline chains like `get(vec, i)!!.field` lose struct identity and the `.field` access falls back to `field_idx=0`. Surfaced during v0.7.9.2 pack_writer port: `length(get(program.modules, i)!!.functions)` returned 0 while the bound form `let m = get(...)!!; length(m.functions)` returned 1. Workaround: always bind the unwrapped value to a `let` with type annotation before field-accessing it. Fix scope: extend `lower_expr` for `Expr::ForceUnwrap` to read `kind_of_value(inner)` and propagate `inner_kind` via `set_value_kind(dest, ...)` — mirrors what `bind_pattern_vars` does for `Pattern::OutcomeArm(Positive)`. Tests: add `crates/triet-bootstrap/tests/chained_unwrap_field.rs` once fixed. Not blocking v0.7.9.3+.
- [ ] **v0.7.x.runtime-fix.struct-in-enum-payload-identity** — Destructuring an enum variant whose payload is a Struct loses the Struct's field content. Surfaced during v0.7.9.5 bootstrap_self_compile gate: `enum SourceResult { SourceLoaded(StringPayload), … }` constructed via `SourceLoaded(StringPayload { value: s })` with `text_len(s) == 28`, then `match res { SourceLoaded(p) => text_len(p.value) }` returned `0`. Worked around in `compiler/main.tri` by inlining the String as a direct enum variant payload (`SourceLoaded(String)` — primitive payloads round-trip correctly through pattern destructure). The bug stayed silent through v0.7.9.4 because the test only checked envelope shape (`pkg_name` / `abi_version` / hash-non-zero) — it never decoded the embedded IR to verify function count. Fix scope: trace `bind_pattern_vars` for `Pattern::EnumTuple` against the Struct payload's `value_kind` and propagate the `Struct { fields }` kind onto the bound name, mirroring what `Pattern::OutcomeArm(Positive)` does for outcome payloads. Tests: a regression case where `enum E { V(SomeStruct), … }` round-trips field reads. Workaround at every site: avoid Struct-typed enum payloads; use primitives directly or store the Struct in a separate `let m: SomeStruct = …` and pass references through (no references in Triết yet — copy semantics suffice).
- [ ] **v0.7.x.runtime-fix.lowerer-incomplete-expr** — Triết-side `compiler/ir_lowerer.tri::lower_expression` has no arms for `IfExpr` / `MatchExpr` / `MethodCallExpr` / `ElvisOpExpr` / `OutcomePropagateExpr` / `OutcomeDefaultExpr` — falls through to `_ => fresh_value(ctx)` which allocates a value id without emitting any instructions. Hệ quả: a function whose body uses any of these expression forms lowers to an empty body, which `write_program` Triết-side then encodes as a 0-function module (write_function_table's discriminator skips empty bodies → bytes round-trip back to "no module"). The v0.7.8.6 `lowerer_differential` shape gate only verified opcode + dest + operand count for the corpus cases it had, not semantic coverage of all expression variants. Discovered via v0.7.9.5 bootstrap_self_compile — recursive `if n <= 1 { 1 } else { n * factorial(n-1) }` produces 0-module IR; iterative `while`/`let mutable` form works because `while` is a Stmt (covered by `lower_while_loop` since v0.7.8.3b). Fix scope: port `lower_if` / `lower_match` / `lower_method_call` / `lower_elvis` / `lower_outcome_propagate` / `lower_outcome_default` from `crates/triet-ir/src/lowerer.rs`. ~500-800 LOC port across 6 arms + supporting helpers (phi-stitching for `lower_if`, exhaustiveness handling for `lower_match`, closure capture for outcome ops). Tests: each arm gets a corpus entry in `bootstrap_self_compile.rs` so the byte-identical gate widens incrementally. Once all 6 arms land, `examples/factorial.tri` (recursive `if?`) can join the gate fixture set.
- [ ] **v0.7.x.runtime-fix.parser-eq-block-body** — Triết-side `compiler/parser/parser.tri::parse_function_body` accepts two forms only: `function f() = expr` (the `Assign` branch dispatches to `parse_expression`) and `function f() { … }` (the `LBrace` branch dispatches to `parse_block`). The mixed `function f() = { … }` form (which Rust's parser DOES accept) hits the `Assign` branch and then `parse_expression` on the immediate `{` fails because `Expr` doesn't include `BlockExpr`. Discovered during v0.7.9.5 fixture creation — the iterative-factorial fixture initially used `function factorial(n) -> Integer = { … }` (mirroring the dominant style in `compiler/main.tri` itself, which is Rust-loaded so accepts the form). Fix scope: in the `Assign` branch of `parse_function_body`, peek after consuming `=` — if the next token is `LBrace`, dispatch to `parse_block` and wrap as `BlockBody`; otherwise dispatch to `parse_expression` and wrap as `ExpressionBody`. Workaround: drop the `=` before `{` in any Triết-compiled-by-Triết file (the current `compiler/factorial.tri` uses the bare-block form for exactly this reason).
- [x] **v0.7.x.runtime-fix.write-function-table-module-prefix** — Root cause was Triết-side `compiler/ir_lowerer.tri::lower_module` passing `let mod_path: Vector<String> = new()` (empty) to `ir_module_new`, whereas Rust-side `load_program_from_source_no_stdlib` seeds the crate root module with `ModulePath::crate_root()` → segments `["crate"]`. Then `compiler/pack_writer.tri::module_path_display` over empty segments yields `"."` (one byte: just the trailing dot Rust's `AbsolutePath::Display` appends as `"{module}.{name}"` with empty name), against Rust's `"crate."` for segments `["crate"]`. Fix: change line 3006 to `let mod_path: Vector<String> = push(new(), "crate")`. After fix, both sides emit identical 327-byte `.khi` size for `compiler/factorial.tri`. (this commit)
- [x] **v0.7.x.runtime-fix.block-emission-order** — Root cause was data-structure divergence: Rust uses `BTreeMap<BlockId, BasicBlock>` (sorted by id), Triết uses `Vector<BasicBlock>` with linear push from `start_block`. Triết's `lower_while_loop` calls `start_block(unknown_panic)` BEFORE `start_block(body)` because `BrTrilean` needs the unknown_block target available when the header emits. Block IDs themselves match Rust (sequential `fresh_block`), so the simplest fix is sort-on-write. Added `sort_blocks_by_id` helper (O(N²) insertion sort, block counts ≤ ~8 in practice) called from `write_code_module_funcs` before iterating into `write_blocks`. After this fix + `write-function-table-module-prefix`, the function-table + code-section bytes match between Rust and Triết outputs for `compiler/factorial.tri`. (this commit)
- [x] **v0.7.x.runtime-fix.while-body-scope-pop-order** — NEW bug surfaced after closing block-order + module-prefix. Triết-side `lower_while_loop` was popping the body scope BEFORE snapshotting body-end SSA values for phi-incoming patching. Then `resolve_var(name)` walked the outer scope where each mutated name still pointed to the header-scope phi-dest — phi-incoming for the body-end edge became a self-loop `Phi { dest: %3, incoming: [pre, %3@body] }` instead of the correct `[pre, %6@body]` (where %6 is the body's Mul/Add result). Compounded by Triết's `lower_block` doing its own `push_scope` / `pop_scope` internally — Rust's `lower_block` does neither, leaving scope management to each caller. Fix scope: (1) split `lower_block` into a public outer wrapper (push + body-lower + pop) and a new private `lower_block_inline` (body-lower only, no scope changes). (2) `lower_while_loop` now calls `lower_block_inline`, snapshots each mutated var's body-end value into a local `body_end_vals: Vector<ValueId>` BEFORE `pop_scope`, and uses those captured values for phi-incoming patching at step 8. Mirrors Rust's `lowerer.rs:2191-2197` (`post_body_vals` capture before `self.pop_scope()`). Tests: closes the v0.7.9.5 deferred byte-identical gate — `bootstrap_self_compile.rs::factorial_self_compile_byte_identical` (renamed from `*_content_equivalent`) now asserts strict byte-equality. (this commit)
- [x] **v0.7.x.runtime-fix.loader-nested-search** — Refactored `crates/triet-modules/src/loader.rs` from single `search_dir: Option<&Path>` to a `search_chain: Option<&[PathBuf]>` ordered most-specific first. Each child's chain starts with its own nested dir (`<matched>/<name>/`) and then inherits the slots from whichever ancestor's chain matched, so siblings further up the tree remain reachable. `resolve_external` tries flat-then-nested per chain slot; first hit wins. Conventional nested layouts (`std/collections.tri` → `std/collections/vector.tri`) keep winning because the nested slot sits at position 0 of the child chain. Unlocks the `compiler/main.tri` → `compiler/pack_writer.tri` → sibling `compiler/ir_lowerer.tri` pattern that v0.7.9.3 had to work around by co-locating tripack code inside `pack_writer.tri`. Added a `loading_files: HashSet<PathBuf>` cycle guard so sibling-fallback resolution can't recurse forever when a `module foo;` decl re-resolves to an already-loading ancestor file — the import-graph cycle detector in `crate::cycle` still surfaces the real cycle via `from crate.x` edges. 2 new regression tests in `loader.rs::tests` (`filesystem_sibling_imports_other_sibling` + `filesystem_nested_layout_still_preferred_over_sibling`). Snapshot `cyclic_import_error.snap` rút from E2101+E2100 → E2100 alone: the old E2101 was a side effect of always-nest failing to find the cycle-target file; with the chain it succeeds and the cycle guard short-circuits cleanly, leaving the import-graph detector as the single source of truth. 1411 workspace tests pass.
---

## How to update this file

- Mark sub-task `[x]` when its commit lands on `main`.
- Add commit short-hash next to completed sub-tasks for quick git reference.
- Keep order: **Shipped** (table format) → **In progress** (checkbox list) → **Pending** (checkbox list).
- When a whole phase ships, archive its summary into `ROADMAP.md` (changelog section) and delete detailed checkboxes here.
- Audit cadence: every 5-10 commits OR before major implementation phase, per `feedback_proactive_audit.md`.
